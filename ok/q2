# qqq-shell-v2 铁律（中度重构版）
整体体量压缩约 40%，合并重复章节、收拢公共表述、移除历史变更信息、集中常量定义，核心硬约束无遗漏。

---

## 公共约定
1. 标记「唯一真理源」的模块为唯一入口，禁止旁路调用、硬编码常量、新增第二入口。
2. 所有硬约束必须严格执行，违反即返工。
3. 废弃机制直接删除，不保留注释与兼容代码。
4. 同一规则仅在权威章节描述一次，其他场景引用对应条目。

## 一、文档总则
- 极简原则：只写结论，不写背景，单段不超3行。
- 仅记录 100% 落地的事实，未落地内容不写入。
- 同一规则仅存一处，过时内容直接删除。

## 二、四层架构与更新体系
### 2.1 四层不可逆架构
1. **C 启动器**（launcher/qqqide.exe）：100% 托管更新，运行于所有进程之前，可解锁替换全部文件。
   - Bootstrap Config：唯一硬编码 URL，下载配置文件驱动全部行为，服务端修改即可全量推送，无需重新下载绿色包。
   - 更新流程：校验版本文件 → 下载更新包 → 删除旧文件 → 解压覆盖，保证文件 100% 精确一致；失败保留旧版兜底。
   - 启动时写入系统环境变量 `QQQIDE_PYTHON_DIR` 并广播系统变更。
2. **壳层**（Electron 22, joker.exe）：仅负责窗口、IPC 与子进程启动，不承载业务逻辑。
3. **ghrun**（Rust）：负责进程管理、热更新、下载校验，操作系统单例。
4. **载荷**（server-app/）：承载全部 UI、业务逻辑与编辑器，热重载 Ctrl+R 生效；归属存疑的功能一律归到载荷层。

### 2.2 双模更新机制
- 壳层冷更新：启动时校验待更新目录，原子替换壳层文件。
- 载荷热更新：启动 20s 后后台自动检查，原子替换后重载，用户无感知。
- Electron 二进制版本永久锁定，不做升级。
- 版本跟踪通过三个固定文件实现，禁止增减。
- 服务端发布产物统一由发布脚本生成，禁止手动上传 CDN。

### 2.3 核心依赖版本锁定
- 固定版本：Electron 22.3.27、sql.js 1.11.0、Monaco 0.34.1、esbuild 0.19.12、TypeScript 5.0.4。
- 升级需同时满足：解决明确功能缺口 + 在 Electron 22 上全量测试通过。

## 三、进程管理与输出约束
- 所有子进程统一由 ghrun 启动，底层通过 Node child_process 兜底。
- 四重进程防御：
  1. 硬超时 2 小时
  2. 内存上限 2GB，首检 5 秒，后续每 15 秒巡检
  3. 系统级进程组隔离，父进程退出子进程自动终止
  4. 失速看门狗：15 分钟无输出则强制终止
- 全局超时参数仅在 `qz-spawn.ts` 一处定义修改。
- 统一使用 execFile 替代 execSync，避免 UI 冻结；空命令保护，守卫超时比进程超时多 10 秒。
- Shell 元字符自动按系统包裹执行器，管道等语法可正常透传。
- 输出截断：IPC 单条 64KB 上限；全文内容统一通过 ContentGateway 管控，默认单条 ≤50K，长内容取首尾各 20K；截断阈值仅在 `CTX_CAP_CHARS` 一处修改。

## 四、UI 基础规范
### 4.1 配色系统
- 唯一入口：`core/qqqide-theme.js`，所有组件禁止私自定义颜色。
- 色盘遵循 Solarized Light/Dark 16 色 + 语义变量，编辑器 token 配色对齐 VS Code solarized 主题。
- CSS 变量一次性注入根节点，零回流；强制禁用系统颜色调整。
- 主题切换通过统一 API 执行，同步修改 DOM 属性、编辑器与 iframe 内页面。

### 4.2 通知系统
- 全局通知唯一入口：qoast，全项目禁止使用 toast 命名。
- 统一调用接口：`qqqideQoast.show(message, {duration, type, action})`，默认展示 9 秒，设为 0 则常驻。
- iframe 内页面通过父窗口调用通知接口。

### 4.3 交互与防抖
- 任何场景、任何元素禁止修改 cursor 样式，全部使用浏览器原生交互状态。
- 全局按钮防抖：默认 800ms 冷却，关闭/取消按钮 400ms；可通过属性跳过或自定义时长。
- 发送逻辑同步锁：模块级同步锁防止异步竞态，所有 await 前同步加锁，所有路径 finally 释放。

### 4.4 国际化
- 唯一真理源：zh.json，开发者仅维护中文，其余语言由脚本自动差异翻译。
- HTML 使用属性标记，JS 使用统一翻译函数；英文编译包内置兜底。
- 语言设置持久化存储，切换时同步所有 iframe。

## 五、编辑器与 Diff
### 5.1 编辑器核心规则
- 仅使用 Monaco 原生 API，不引入 VS Code 扩展宿主与相关命名空间。
- 全面禁用 LSP 与内置诊断：关闭校验装饰、悬浮提示、自动补全，仅保留语法高亮、选中高亮与就近括号匹配。
- 语法检查采用三级方案：一级自动轻量校验、二级上下文诊断、三级全仓扫描。

### 5.2 Diff 窗口
- 独立窗口展示，使用 Monaco 并排对比，单文件单例，重复调用复用窗口。
- 提供仅差异视图、编辑模式切换；编辑模式仅右侧可编辑，支持差异块单向复制。
- 编辑保存与普通编辑共享冷却与去重逻辑；编辑器同样禁用 LSP 与智能提示。
- Monaco 加载设置 15 秒超时保护。

## 六、AI 网关与流式处理
### 6.1 网关架构
- 采用 Worker 纯代理 + Go 服务端计费的架构：客户端 → Cloudflare Worker（纯透传）→ Go 服务（唯一扣款权威）→ 模型供应商。
- Worker 仅做路由透传，禁止计费、模型映射、修改请求体；Go 服务单步完成扣费。
- 客户端统一入口：`AiGateway`，所有 AI 调用必须经过此网关，禁止直接请求第三方接口。
- 客户端不暴露模型名、厂商名、接口地址，仅通过层级数字传参。
- 全功能双线路：对话、视觉、生图、嵌入、搜索均支持主备线路自动切换。

### 6.2 传输与超时
- 视觉接口采用 SSE 长连接，禁止轮询；多图片并行请求，上下文断开自动取消上游。
- 所有超时参数仅在 `content-gateway.js` 定义，其他模块统一引用，禁止硬编码。
- 线路切换规则：同一 URL 重试 3 次后切换线路，最多切换 2 次防止乒乓；HTTP/2 错误优先切线路。
- 服务端 8444 端口禁用 HTTP/2，避免 SSE 协议转换错误。

### 6.3 异常自愈
- 孤儿工具调用双向扫描自愈，每次网关调用前预检，请求失败后被动修复并重试。
- 致命失败采用 ACTIVE/CAPPED 二态模型：
  - ACTIVE 态：红框展示错误信息与「继续任务」入口，可累积错误行。
  - CAPPED 态：移除继续入口，错误楼层密封不再追加内容。
  - 发送按钮在任何失败场景下永不禁用。
  - 楼层中断后立即密封，点击继续任务创建新楼层，错误信息累积到原失败楼层。

### 6.4 流式渲染
- 流式渲染与历史恢复共用同一渲染函数；渲染规则采用 flag 白名单，新增消息默认不渲染。
- API 返回的完整 content 为最终唯一真理，流式输出仅作实时预览，楼层完结时用完整内容覆盖。
- 流式状态由 agent 实例持有，跨面板共享。

### 6.5 模型与计费
- 客户端与网络响应中不暴露真实模型名与厂商，仅返回不透明标签。
- 计费逻辑唯一收敛于服务端 billing 包，所有接口统一引用，禁止本地硬编码定价。
- 对话计费加入每 house 独立随机因子，层级倍率叠乘于随机因子之上。
- 汇率每 6 小时自动更新，基础定价人工定期核对更新。

## 七、三面板架构与状态管理
### 7.1 三面板基础
- 左中右三面板为独立 iframe，同窗口内运行；左右翼可开关，切换时窗口自动伸缩。
- 每面板独立窗口 ID，存储于会话存储；项目锁仅中面板申请，三面板共享。

### 7.2 跨面板同步
- 同步消息通过主进程中继广播到所有窗口，接收方过滤自身消息；禁止使用 BroadcastChannel。
- quest 索引中心为父窗口共享数组，零拷贝；仅主面板扫盘写入，侧面板直接读取。
- 新建 quest 30 秒宽限期内不删除磁盘无对应目录的条目。

### 7.3 工作空间与所有权
- 单窗口对应一个工作空间，绑定一个主文件夹，不同工作空间通过 IPC 频道物理隔离。
- 切换主项目时先销毁旧工作空间再初始化，并发锁防止竞态。
- quest 所有权由父窗口注册表统一管理，切换 quest 时先释放旧所有权再申领新所有权。

### 7.4 Card 队列与渲染
- 单个 quest 对应一张 Card，创建后 DOM 结构不变，切换 quest 仅通过 CSS 显隐控制。
- 单 Card 最多保留 16 条已封顶楼层 + 1 条在建楼层，超量删除最老 DOM；面板池上限 10 张，LRU 驱逐。
- 单渲染函数负责实时渲染与历史恢复；楼层状态全部归属 agent 实例，禁止全局变量。

### 7.5 建楼状态机
建楼状态由父窗口注册表统一管理，单 quest 独立沙箱，状态转换严格遵循下表：

| 当前状态 | 允许转换到 |
| :--- | :--- |
| idle | sending、fatal |
| sending | stopping、fatal、idle |
| stopping | idle、fatal |
| fatal | sending |

- 时钟状态机：工作中绿色、切换段过渡色、停止态黑白；启动立即设为绿色，终止立即复位。
- 切换 quest 时启用全屏蒙板保护，拦截所有交互与消息。

### 7.6 发送管线
- 执行顺序：前置校验 → 草稿升级 → 空值守卫 → 不变量检查 → 设置发送状态。
- 草稿升级在所有 agent 操作前完成，升级后保证 agent 实例非空。
- 模块级同步锁防止草稿升级异步竞态，所有入口加锁，所有路径释放。

## 八、持久化与文件系统
### 8.1 持久化入口
- 六类持久化入口：全局数据库、对话数据库、偏好数据库、项目元信息文件写入，禁止直接调用桥接状态、本地文件 API、localStorage。
- 全局数据存应用目录 Data 下，项目数据存项目 `_qqq` 目录下。
- 程序退出信号触发自动同步刷盘，保证数据完整。

### 8.2 原子写入
- 所有写入操作采用先写临时文件再重命名的原子方案，重命名失败则降级为复制替换，绝不先删除目标文件。
- 启动时清理残留临时文件，临时文件新于主文件则自动恢复。
- 数据库损坏时自动隔离损坏文件并创建空库，不丢失数据不阻断运行。

### 8.3 项目与资产
- AI 视口首个目录为主文件夹，带 ★ 标记，不可删除；AI 首轮注入主项目上下文标记与提示。
- 主文件夹写入锁文件，30 秒心跳，60 秒过期，防止同一项目多窗口打开。
- 项目资产（辅文件夹、编辑器标签、布局状态）唯一存储于偏好数据库，随项目目录迁移。

### 8.4 源码目录
- webapp 源码唯一目录：`server-app/`；打包输出目录禁止编辑，已加入 git 忽略。
- TypeScript 源码位于 `shell/`，编译产物位于 `shell-out/`，二者严格分离，禁止直接修改编译产物。

### 8.5 附件与文件落盘
- 附件采用 ASCII 双引号包裹路径的分隔符，与 Windows 文件名规则天然无碰撞。
- 编辑框允许多次插入同一文件，发送构建上下文时自动去重，同一文件仅投喂一次。
- 粘贴文件统一落盘到当前文件所在目录的 `_qqqvault/` 子目录，不写入项目元数据目录。
- 附件锚点格式固定：`📎{sha256前12位}:{文件名}`，前缀前必须加换行，防止拼接歧义。

## 九、Goods 组件系统
### 9.1 核心协议
- 宿主唯一入口：`core/gaea-host.js`，所有组件通过统一接口注册。
- 单个组件可提供侧边栏面板、标签页、后台服务、命令；移除时必须清理所有注册项。
- 第一方组件使用直接 DOM 操作，第三方组件使用 iframe 隔离。
- 统一打开入口：`qqqGaea.open(id)`，根据组件配置自动路由，禁止硬编码展示调用。

### 9.2 单例机制
- 默认禁止多开，三级检测 + 脚本自保双重防多开。
- 三级进程检测：内存注册表 → 本地 PID 文件 → 系统级状态文件，心跳 10 秒刷新，30 秒过期，跨运行模式全同步。
- 单例冲突冷却 30 秒，防止跨实例启动风暴。

### 9.3 进程控制
- 状态指示点与启停开关完全解耦：指示点实时反映进程存活状态，可独立点击启停；开关控制自启动看门狗。
- 进程崩溃且看门狗存活时自动拉起进程。
- 组件数据统一存储在系统本地目录下，保证开发、打包、独立进程读写同一路径。

### 9.4 组件管理
- 组件定义唯一真理源：`manifest.json`，包含安装路径、二进制、校验参数、源地址。
- 分两级组件：0 级启动自动下载，1 级后台静默或手动下载。
- 启动时逐组件校验，缺失则按顺序下载；使用直接调用二进制做冒烟校验。
- 新增组件必须定义冒烟校验，可选自愈校验。

### 9.5 Python 环境
- 绿色包自带 Python 解释器与预装依赖，零系统依赖；优先级：内置 Python > 启动器环境变量 > 系统 PATH。
- 子进程自动注入内置 Python 到 PATH，命令直接写 python 即可。
- 包安装到绿色包内置目录，多实例完全隔离；禁用用户目录安装。
- 冻结依赖清单作为权威版本，新增/升级包必须同步更新清单。

### 9.6 图像处理
- 图像处理全部使用云端专用工具，禁止本地编写 Python 图像处理脚本。
- 云端服务失败直接告知用户功能不可用，禁止降级到本地脚本。
- 用户明确要求编写脚本处理的场景除外。

## 十、上下文与专家框架
### 10.1 上下文背包
- 上下文背包仅保留一条动态摘要与一条事实条目，保证唯一性；去重时先合并孤儿内容再删除。
- 每层楼完结后触发重建，建楼过程中不触发。
- 压缩分三档：绝对压缩（本地纯文本剥离）、仅编辑（保留工具头）、AI 事实提取（调用模型提取核心事实）。
- 事实块独立追加，日常自动压缩不触碰事实条目；背包顺序固定。

### 10.2 E-Flow 专家框架
- 四状态机：未启用 → 待引导 → 标准模式 → 自定义模式。
- 新项目首次启动自动创建专家文档骨架，自动切换为标准模式；仅中面板首层触发，防抖 5 分钟。
- 启动时自动扫描检测模式，已锁定模式不回退。

## 十一、时间线与版本管理
- 文件操作钩子捕获前后版本写入时间线；编辑器失焦或保存时捕获差异写入。
- 编辑类快照共享主进程冷却与去重逻辑：100 秒冷却 + SHA256 去重，行尾自动归一化。
- 存储采用压缩 blob + 数据库 + 备份的多层结构；楼层完结仅打包哈希值入载荷。
- 提供 Git 标签页与状态面板；重命名仅修改数据库记录，启动关闭时惰性扫描修正。
- 工具调用孤儿消息双向扫描自愈；服务端双重防御保证单用户异常不影响全局。

## 十二、发布、安全与运维
### 12.1 开发与安全
- 一站式开发脚本：集成构建、开发服务、监听、Electron 启动。
- AI 输出禁止包含内部域名、IP、端口、接口路径、服务器名；工具失败仅返回通用不可用提示。
- 零配置原则：无用户可见配置文件，生产地址硬编码，仅环境变量/命令行可覆盖。
- 服务器采用前台 + 隐身节点架构：前台节点公网入口带 DDoS 防护，隐身节点仅白名单可访问。

### 12.2 登录体系
- 主进程单例管理认证状态，安全存储凭证，定时拉取余额与等级，广播同步所有窗口。
- 主通道：外部浏览器打开登录页，完成后通过自定义协议回调；主进程轮询兜底。
- 启动器与 Electron 双层单实例保证。

### 12.3 发布管道
- 所有官方发布产物统一通过发布脚本一键上传到指定分发地址，双存储写入，CDN 自动调度。
- 新增产物必须同步添加生成函数、双地上传、CDN 缓存清理三个环节。
- 禁止手动上传分发文件，禁止绕过发布管道的 CDN 地址。

### 12.4 开发约束
- 批量编辑仅适用于等长替换，变长编辑必须单独提交；混合批量会导致后续编辑错位。
- 静态文件修改必须同步升级 Service Worker 缓存版本号，否则旧缓存不生效。
- 状态栏提供构建戳验证，可确认当前运行代码是否为最新。

## 十三、命名与编码规范
### 13.1 命名铁律
- 变量名禁止包含特定敏感字符；JSON 字段使用固定命名，禁止混用别名。
- 产品级固定英文名：上下文背包 Context Backpack、格子 Grid，所有场景统一使用。

### 13.2 源码与设计原则
- TypeScript 源码与编译产物严格分离，禁止直接修改编译产物。
- 零摩擦原则：IDE 仅负责消除工具摩擦，不做 AI 行为引导、语义检测、提示词微调。

### 13.3 质量保障
- 启动时全量模块健康检查，关键模块导出异常则顶部红色告警，热重载可修复。
- 文件编辑后端内置语法检查：JS 使用虚拟机编译校验，JSON 使用原生解析；失败则还原文件并返回错误。

---

## 附录：固定常量表
### 表1：核心依赖固定版本
| 依赖 | 固定版本 |
| :--- | :--- |
| Electron | 22.3.27 |
| sql.js | 1.11.0 |
| Monaco | 0.34.1 |
| esbuild | 0.19.12 |
| TypeScript | 5.0.4 |

### 表2：核心常量
| 常量 | 值 | 说明 |
| :--- | :--- | :--- |
| 进程硬超时 | 2h | 子进程最长运行时间 |
| 内存上限 | 2GB | 单进程内存阈值 |
| 失速看门狗 | 15min | 无输出自动终止 |
| 单例心跳 | 10s | 进程状态刷新周期 |
| 单例过期 | 30s | 状态文件失效时间 |
| 项目锁心跳 | 30s | 项目锁刷新周期 |
| 默认按钮防抖 | 800ms | 全局按钮冷却 |
| 网关主线超时 | 180s | AI 主线路超时 |
| 内容截断上限 | 50K | 单条内容最大字符 |

### 表3：固定路径与文件
| 类别 | 路径/文件名 |
| :--- | :--- |
| 全局数据库 | Data/alphal/global.sq3 |
| 项目数据库 | _qqq/alphal/{quest,only}.sq3 |
| 粘贴文件目录 | 当前文件目录/_qqqvault/ |
| 版本跟踪文件 | gh555.com/.version、Data/shell-version、Data/webapp-version |
| 组件清单 | engines/manifest.json |

需要我把这份重构版输出为无格式纯文本，方便你直接复制使用吗？