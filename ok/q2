# qqq-shell-v2 架构（精简版）
> 代码若与此冲突，以本文档为准

## 命名与归一
- 统一命名：`qqqide`（全小写），不再区分 Tier
- 全链路归一完成时间：2026-07-07，零旧兼容

---

## 四层模型
### 1. C 启动器层（launcher/qqqide.exe）
- Win32 原生，约 44KB，零外部依赖
- 核心职责：100% 托管更新 + 启动 joker.exe
- Bootstrap Config：唯一硬编码 URL，下载 `launcher-config.json`，所有行为由配置驱动（host、路径、超时、重试、重定向）；配置本地缓存，离线用缓存+默认值兜底，服务端改配置用户无需重下安装包
- 更新流程：拉取 latest.txt → 下载更新包 → 删除旧目录 → 解压覆盖，保证 100% 精确一致

### 2. 壳层（shell/）
- Electron 22.3.27，入口 joker.exe，启动文件 bootstrap.js → main.js
- 核心职责：窗口管理、IPC 通信、桥接层、热更新检查
- 源码：main.ts + 12 个子模块，esbuild 编译约 35ms，产物输出 shell-out/
- 冷更新：启动时原子替换 shell-out/ 目录

### 3. 引擎层（Rust ×2）
- `q_win_x64.exe`：IO 引擎，负责文件系统、SQLite、IPC
- `ghrun.exe`：进程管理（LSP 守护已冻结）
- 兜底机制：qz-spawn 两层架构（ghrun → Node）
- 同样支持 Bootstrap Config 配置驱动更新，采用「先启动程序、后台下载、重启生效」策略，永不阻塞用户
- 更新覆盖整个 gh555.com/ 目录（二进制、壳层、引擎、Web 应用、依赖库）

### 壳层冷更 + 载荷热更
- 壳层冷更：bootstrap.js 检测 staging 目录，原子替换 shell-out
- 载荷热更：启动 20s 后后台检查 Web 应用更新，下载解压到 staging，下次启动原子替换
- 壳层与 Web 应用版本独立持久化，支持语义化版本比较，防止降级
- 提供手动更新按钮，复用同一套更新管线

---

## 核心机制
### 持久化层级
严格遵循 6 层命名体系，禁止使用同义词（turn/session/round）：
1. global (qgs)：跨项目数据（最近文件夹、主题、语言、剪贴板）
2. project (qg)：项目内配置（编辑器标签、偏好、锁）
3. quest：单次对话会话
4. floor：单次用户键入（可包含多次 API 调用）
5. house：单次 API 调用（可包含多次工具调用）
6. room：单次工具调用

### qz spawn 管线 + 四重防御
- 管线：ghrun（Rust 主力）→ Node（child_process 兜底），runner.py 已从主调用链移除
- 针对进程内存爆炸的四重防御：
  1. 硬超时 2 小时，入口强制上限
  2. 内存守卫：Node 层异步检测，ghrun 层通过系统 Job Object 原生限制
  3. 系统级进程组隔离：Windows 用 Job Object + 进程组，POSIX 用进程组信号
  4. 失速看门狗：15 分钟无输出强制终止；输出上限 64KB，防止 IPC 洪水

### AI 网关双线路 + 故障切换
- 主线路：`direct-cn.gh555.com`（国内直连，约 50ms 延迟）
- 备线路：`direct.gh555.com`（Cloudflare CDN 转发美国节点，最后兜底）
- 国内节点 AI 请求全部本地处理，图像生成全链路本地完成
- 覆盖 6 类 AI 端点（对话、视觉、图像生成、向量、搜索等），内置自动重试+切线路逻辑
- 同 URL 最多重试 3 次，单轮最多切 2 次线路，防止乒乓切换；不同错误码对应不同重试/切线策略
- 故障先尝试自动修复，失败再向用户报错，尽量无感切换

### 服务端系统提示词与密钥隔离
- 系统提示词由服务端统一注入，客户端不再发送，提升安全性与缓存命中率
- 双 API Key 隔离存储于服务端，客户端零密钥；JWT 负责身份认证与计费，密钥槽负责路由，完全解耦
- 服务端密钥欠费自动切线路，用户余额不足直接终止请求，不浪费切换资源

---

## 登录与成长体系
### IDE 登录认证
- 主进程单例认证中心：统一管理登录态、余额、等级，跨窗口毫秒级同步
- 主通道：外部浏览器登录 + 系统协议回调，登录成功自动回调客户端
- 兜底：主进程轮询服务端，协议回调失败时 3 秒内自动接管
- 降级方案：提供复制登录链接功能，适配极端环境；120 秒超时报错
- 双单实例防护：C 启动器系统互斥量 + Electron 单实例锁

### 等级（LV）体系
- 结算周期：UTC 自然周，周一零点重置
- 升级规则：每 10 ge 非免费消费升 1 级，免费冲正不计入等级
- 免费预算采用随机档位制，分 2 小时和 24 小时两档概率分布
- 客户端 3 层血条展示，事件驱动更新，跨窗口秒级同步
- 支持升级动画、音效、粒子效果，启动即显示，无需等待服务端数据
- 提供历史总排行、上赛季排行两个榜单

---

## 基础设施规范
### Nginx 线路规则
- 国内 443 端口 AI 请求转发至本地 8080 服务
- 美国节点仅作为 AI 备用线路，经 Cloudflare 回源
- 8444 端口强制 HTTP/1.1，用于内部访问，规避 HTTP/2 转 SSE 的协议 Bug

### 内容网关（ContentGateway）
- 所有工具结果、附件内容统一走此入口，一处管控截断与超时规则
- 截断规则：5 万字符以内全文返回，超出保留首尾各 2 万字符
- 指定行范围读取文件可跳过截断，信任 AI 意图
- 长超时配置，适配大文件与慢请求场景

### 孤儿 tool_calls 自愈
- 对话双向扫描自动修复残缺工具调用，请求前、保存前、自动保存时三重校验
- 最多 10 轮修复循环，修复成功重置错误状态
- 服务端同步做消息校验，形成双向防线

---

## 扩展与组件体系
### gaea goods 协议
- A 区面板宿主，支持面板类与后台进程类两种扩展
- 生命周期分「随主窗口」和「独立运行」两种模式
- 音频分 IDE 统一音量和独立音量两套体系
- 单实例扩展通过 PID 文件 + 脚本自保双重防多开
- 进程状态指示与自启动开关双控解耦：圆点指示进程存活，开关控制自动重启

### Python + A 区数据架构
- 系统级 SQLite 作为唯一数据真理源，保证所有窗口、进程共享同一份数据
- 读写分离：Python 负责写入，前端通过 IPC 读取，SSE 仅推送变更事件，零数据载荷
- Python 进程异常时前端降级为轮询，核心功能不受影响
- 跨版本、跨安装包状态同步，统一进程管理管线
- Qt 弹窗屏蔽系统缩放，保证像素级尺寸一致

### 组件系统
- 服务端 manifest.json 驱动，修改配置客户端自动重组，无需发版
- 两级完整性校验：自愈能力校验 → 冒烟测试，支持组件自修复
- 组件分等级：
  - rank0：IDE 自动管理，后台静默下载，不阻塞启动（含 ripgrep、Python、ffmpeg、git）
  - rank1：后台下载或手动触发，不阻塞启动（含 ffprobe、yt-dlp）
- 支持目录自动迁移、原子写入、断电保护、下载冷却与重试
- LSP 功能已冻结，替代为三级语法检查：本地零消耗基础检查 → AI 单文件诊断 → AI 全仓诊断

---

## 编辑器与交互
### 搜索引擎
- 基于 ripgrep 14.1.1 单引擎，Rust 实现，支持 mmap、SIMD、PCRE2 JIT
- 流式解析结果，多路径查找二进制，打包时内置分发
- 集成于 X 区搜索面板与 AI 视口搜索按钮

### WYSIWYG 粘贴子系统
- 核心理念：文本存纯文本锚点标记，内存映射表管理渲染，零正则扫描
- 四层管线：剪贴板中心 → 粘贴路由 → 锚点映射表 → 视图控件渲染
- 图片/视频用空气行展示，文件/目录用图标框展示，支持缩略图缓存
- 附件文件落盘到项目目录下的 `_qqqvault/`，与项目元数据分离

### Monaco Worker 加载
- 基础 Worker 直接加载，语言 Worker 走 Blob 拼接
- ESM 转 AMD 脚本处理依赖补全，自定义协议实现资源查找与降级

---

## UI 与交互架构
### 布局体系
- 三面板结构：中区（A 区扩展 + X 区编辑器 + 中间 AI 面板）+ 左右翼面板
- 翼面板默认关闭，通过灯泡按钮切换，宽度固定 389px
- 窗口最小尺寸随翼面板开关动态调整，保证中间面板最小 1100×800

### 菜单栏
- 顶部两个菜单：qqqide + 的梦gaea
- 「的梦gaea」三态交互：悬停弹出、点击固定、长按 800ms 打开外部官网

### Card Queue AI 面板架构
- 一个对话对应一张卡片，DOM 永久保留，切换仅做显隐，零销毁竞态
- 单张卡片最多 16 层历史 + 1 层在建，超限移除最老楼层 DOM
- 卡片池上限 10 张，LRU 淘汰；支持搜索高亮、滚动条标记
- 三面板共享同一 agent 实例，切换对话无状态不同步问题

### q2 单行导航 + 彗星电子钟
- 对话招牌行极简展示，悬停展开详情，支持搜索与快速新建
- 跨面板独立计时，绑定各自对话 ID，不串号；对话删除后自动清除残影

---

## 数据与存储
### 持久化入口
提供 6 种持久化入口，适配不同场景：
- qgs / qgs.simple：全局 SQLite，跨项目共享
- qg / qgf：项目级文件系统存储，JSON 格式原子写入
- qgs.project：项目级 SQLite，存对话数据
- onlyStore：项目级偏好与资产缓存，内存+批量落盘

### 家目录划分
- 项目级：`{项目根目录}/_qqq/`，随项目迁移，存对话、版本、项目配置
- 程序级：`{安装目录}/Data/alphal/`，绿色包自带，零 C 盘写入，存全局配置

### 文件版本时间线（Timeline）
- 独立差异对比窗口，基于 Monaco Diff Editor
- 存储：内容 blob 不可变（SHA256 + gzip）+ 增量日志 + SQLite 快照 + 备份，四重保障
- 触发时机：AI 写工具、命令执行、编辑器保存、差异窗口编辑
- 编辑器保存与差异编辑共享去重+冷却逻辑，避免冗余快照
- 历史版本永不删除，支持长期回溯

---

## AI 能力与工具
### 20 个 AI 工具 · 双包装盒体系
- 温柔包装盒（15 个）：结果可复现，仅保留一行头信息，压缩时不丢失可追溯性
  - 包含：文件读写删改、搜索、文件列表、语法诊断、网页抓取、版本回溯等
- 绝对包装盒（5 个）：结果不可复现，特殊标记包裹，压缩时保留头部、剥离主体
  - 包含：命令执行、图像生成、抠图、图像分析、视觉上下文
- 所有写工具返回自动注入 SHA256 哈希与追踪标识，支持随时考古
- 支持读取历史版本 blob 进行搜索、诊断

### Agent 状态机
- 四状态流转：空闲 → 发送中 → 停止中/致命错误
- 带状态转移校验，非法转移告警不阻断
- 致命错误支持「继续任务」恢复，新建楼层继续，不原地重试
- 永不锁定发送按钮，错误态用户可自由操作

### 流式渲染双层模型
1. 流式阶段：按段落增量渲染，代码围栏感知拆分，永不清空重绘
2. 完结阶段：首条回复全量渲染替换流式预览，后续回复追加渲染
3. 崩溃兜底：对话数据全量重建 HTML，保证数据不丢
- 双层看门狗：60 秒无数据断流、15 分钟无有效产出终止，防止假死

### 上下文压缩
- 每层封顶自动重建上下文，建楼中禁止触发
- 三按钮压缩模式，事实系统消息独立保留，日常压缩不触碰
- 工具结果分两类包装盒差异化处理，保证关键信息不丢失

### 子弹按钮
- 一键将剪贴板内容写入项目临时目录，自动生成锚点注入对话
- 单文件、目录双容量上限，FIFO 轮转淘汰，用完即抛不占用长期上下文

---

## 工程与运维
### 目录骨架
- 源码：launcher（C 启动器）、shell（壳层 TS）、server-app（载荷层）、shell-build（打包脚本）、do/拓扑（架构文档）
- 绿色包：qqqide.exe（入口）、launcher 配置、更新包、gh555.com 托管目录（全部自动管理）

### 构建与打包
- 开发：一键启动 esbuild 监听 + 开发服务器 + Electron
- 编译：C 启动器用 gcc 编译，壳层用 esbuild 编译
- 打包：生成单 zip 绿色包，解压即用，自动清理中间产物

### IPC 与主进程安全
- 主进程统一处理文件操作，渲染层优先走主进程，失败降级直连
- 文件读取 50MB 守卫，超限返回分页提示
- 文件编辑三级匹配引擎，主进程与渲染层双份实现
- 所有跨窗口消息发送前检测窗口是否已销毁，全局捕获异常

### 工作空间与多项目隔离
- 同一窗口同一时刻仅一个项目，切换项目完整清理旧状态
- 同步通道按项目路径隔离，不同项目物理上互不干扰

### qwr 全局写机器
- 五层防御：单文件串行队列、全局读写屏障、写前重读校验、外部修改检测、快照自更新
- 保证零脏写、零丢失更新、零死锁，覆盖所有 AI 写工具与命令执行

### 统计上报
- 主进程异步上报，不阻塞 UI
- 装机一生一次，在线按用户去重，陪伴时长跨重启累计
- 失败指数退避，首次上报随机抖动防雪崩

### 零摩擦原则
- IDE 核心目标是消除工具使用摩擦，不干预 AI 思考逻辑
- 新增功能优先判断是否在降低使用成本，而非增加 AI 引导

---

## 已知架构漏洞
- 🔴 待修复：时钟/饼图跨面板不可见；SSE 断线无跨面板自愈
- 🟡 待修复：面板生命周期脆弱，DOM 事件与定时器存在累积风险
- 🟢 其余历史漏洞均已修复