# qqq-shell-v2 架构
代码若与此冲突，以本文档为准。

## 公共约定
1. 标记「唯一真理源」的模块为唯一入口，禁止旁路调用、硬编码常量、新增第二入口。
2. 所有硬约束必须严格执行，违反即返工。
3. 废弃机制直接删除，不保留注释与兼容代码。
4. 同一规则仅在权威章节描述一次，其他场景引用对应条目。

## 一、命名与总则
- 全链路命名统一为 `qqqide`（全小写），零旧兼容代码。
- 数据层级严格遵循：`global → project → quest → floor → house → room`，字段统一使用 `floor_id`，禁用 `turn/session/round` 等同义词。
- 零配置原则：无用户可见配置文件，生产地址硬编码，仅环境变量与命令行参数可覆盖。
- 零摩擦原则：IDE 仅负责消除工具摩擦，不做 AI 行为引导、语义检测与提示词微调。

## 二、四层不可逆架构与更新体系
### 2.1 四层架构
四层职责边界清晰，功能归属存疑时一律归入载荷层。
1. **C 启动器**（launcher/qqqide.exe）：Win32 原生程序，体积约 44KB，零外部依赖。运行于所有进程之前，负责 100% 托管全量更新与启动壳层。
   - Bootstrap Config：唯一硬编码 URL，下载配置文件驱动全部行为（地址、路径、超时、重试等），配置本地缓存，离线使用默认值兜底。服务端修改 JSON 即可全量推送，用户无需重新下载绿色包。
   - 启动流程：加载配置 → 应用暂存更新 → 立即启动壳层 → 后台静默下载更新包 → 下次启动生效，全程不阻塞用户。更新覆盖整个 `gh555.com/` 目录，保证文件 100% 精确一致，失败保留旧版兜底。
   - 启动时写入系统环境变量 `QQQIDE_PYTHON_DIR` 并广播系统变更。
2. **壳层**（shell/，Electron 22.3.27，joker.exe）：仅负责窗口管理、IPC 通信、桥接接口与冷更新检查，不承载任何业务逻辑。TypeScript 源码经 esbuild 编译输出到 `shell-out/`，启动时原子替换。
3. **引擎层**（Rust 双进程）：`q_win_x64.exe` 负责 IO、SQLite 与 IPC；`ghrun.exe` 负责进程管理。进程调用采用两层兜底：ghrun 为主力，Node child_process 为备用。
4. **载荷层**（server-app/）：承载全部 UI、业务逻辑与编辑器，支持热重载，Ctrl+R 即时生效。

### 2.2 三级更新机制
- **C 启动器全量更新**：后台静默下载，下次启动时生效，覆盖二进制、壳层、引擎、载荷、依赖全量文件。
- **壳层冷更新**：启动时校验待更新目录，原子替换 `shell-out/` 目录，需重启生效。
- **载荷热更新**：启动 20s 后后台自动检查，原子替换后重载，用户无感知。
- 版本跟踪：`shell-version`、`webapp-version` 独立持久化，采用语义化版本比较，防止版本降级。
- 核心依赖版本永久锁定：Electron 22.3.27、sql.js 1.11.0、Monaco 0.34.1、esbuild 0.19.12、TypeScript 5.0.4。升级需同时证明存在明确功能缺口且在 Electron 22 上全量测试通过。

## 三、进程管理与组件系统
### 3.1 qz spawn 进程管线
- 所有子进程统一由 ghrun 启动，Node child_process 兜底。
- 四重进程防御：
  1. 硬超时 2 小时
  2. 内存上限 2GB，首检 5 秒，后续每 15 秒巡检
  3. 系统级进程组隔离（Windows Job Object / Unix setrlimit），父进程退出子进程自动终止
  4. 失速看门狗：15 分钟无输出则强制终止；单条输出 64KB 上限，防止 IPC 洪水
- 全局超时参数仅在 `qz-spawn.ts` 一处定义修改；统一使用 execFile 替代 execSync，避免 UI 冻结；Shell 元字符自动按系统包裹执行器。

### 3.2 组件系统（服务器驱动）
- 唯一真理源：`engines/manifest.json`，服务端修改即可驱动客户端自动重组组件。
- 两级组件分级：rank0 启动自动下载（ripgrep、python、ffmpeg、git）；rank1 后台静默或手动下载（ffprobe、yt-dlp）。
- 自愈完整性校验：两级校验机制——self_heal 验证自愈能力，smoke 验证基础运行能力。启动时全量检查，失败则降级运行或触发灾备重下。
- 目录自动迁移：服务端修改安装路径，客户端自动原子重组目录，无需重新下载组件。
- Python 环境三级优先级：绿色包内置 > 启动器环境变量 > 系统 PATH；内置环境完全隔离，禁用用户目录安装，冻结依赖清单为权威版本。

### 3.3 qwr 全局写保护
五层防御保证文件操作零脏写、零丢失更新、零死锁：
1. 单文件串行队列，同文件排队执行，不同文件并行处理
2. 全局命令屏障，读写操作双向等待
3. 写操作 CAS 重读校验，不依赖 AI 旧快照
4. 外部修改检测，通过 mtime + size 校验文件一致性
5. 快照自更新，操作完成后同步更新内存缓存
覆盖 edit/write/create/delete/run_command 五大文件操作。

## 四、AI 网关与服务端架构
### 4.1 双线路架构
- 主线路：`direct-cn.gh555.com`，直连 CN 本地节点，低延迟；备线路：`direct.gh555.com`，经 CF CDN 转 US 节点，容灾兜底。
- CN 节点本地处理全部 AI 能力（对话、视觉、生图、嵌入、搜索），图像生成全链路本地闭环。
- Cloudflare Worker 仅做纯透传，不执行业务逻辑与计费；Go 服务端为唯一扣款权威，单步完成扣费。
- 客户端统一入口 `AiGateway`，不暴露模型名、厂商名与接口地址，仅通过层级数字传参。

### 4.2 故障切换引擎
- 同一 URL 最多重试 3 次，重试耗尽切换线路，单次请求最多切换 2 次防止乒乓效应。
- HTTP/2 连接错误优先切换线路；502/503 错误前半段重试同 URL，后半段切换线路。
- 400/422 错误跳过重试进入自愈流程；402 错误按欠费类型区分处理；AbortError 直接切换线路。
- 延迟报错机制：先尝试自动自愈，成功则用户无感，失败再抛出错误。
- Nginx 8444 端口禁用 HTTP/2，避免 SSE 协议转换引发的错误。

### 4.3 服务端核心机制
- **系统提示词甲壳化**：唯一真理源为服务端文本文件，内存缓存，请求前注入；客户端不传输提示词文本，保障缓存命中率与安全性。
- **双 API Key 隔离**：主备密钥独立存储，JWT 认证与密钥路由完全解耦；服务端密钥欠费自动切换线路，用户无感知。
- **计费幂等**：同一次调用仅扣费一次，基于用户+楼层+请求哈希生成幂等键。
- **CF 来源校验**：生产环境 AI 接口强制 Cloudflare 回源，防止直连攻击。

### 4.4 服务端部署架构
- **CN 前台节点**：阿里云 ECS，公网入口，自带 DDoS 防护，本地运行 Go 服务、PostgreSQL、Redis，处理全部业务与 AI 请求。
- **US 隐身节点**：iptables 白名单机制，仅放行 CN 节点、WireGuard 与 Cloudflare 回源，其余全部拒绝；运行备份服务与搜索引擎，重启自动恢复防火墙规则。
- DNS 调度：直连线路仅做 DNS 解析，主站走 Cloudflare 代理防护。

## 五、认证、等级与计费
### 5.1 登录体系
- 主进程单例管理认证状态，安全存储凭证，每 60 秒拉取余额与等级，广播同步所有窗口。
- 主通道：外部浏览器打开登录页，完成后通过自定义系统协议回调；主进程轮询兜底，协议回调失败自动接管。
- 3 秒未登入展示永久通知与复制登录链接按钮，120 秒超时提示错误；C 启动器与 Electron 双层单实例保证。

### 5.2 等级与免费体系
- 等级计算：每 10 ge 非免费消费升 1 级，免费冲正部分不计入等级。
- 免费预算按自然周发放，采用随机预算制；上赛季等级向下取整提供加成，过期未使用作废。
- 事件驱动更新：计费事件驱动血条动画、余额乐观扣减，服务端数据定期校正覆盖。
- 三层血条视觉：轨道层、即时层、追赶层，10 秒赛贝尔曲线动画；跨窗口秒级同步。
- 排行榜：历史总排行与上赛季排行各展示 25 名。

### 5.3 计费规则
- 服务端 billing 包为唯一计费真理源，所有接口统一引用，禁止本地硬编码定价。
- 对话计费加入每 house 独立随机因子，层级倍率叠乘于随机因子之上。
- 免费预算申领与扣费在同一事务中完成，保证原子性。

## 六、UI 与三面板架构
### 6.1 布局基础
- 左中右三块独立 iframe 面板，中面板常驻，左右翼可开关，切换时窗口自动伸缩。翼面板宽度固定 389px。
- 窗口最小尺寸：中面板 1100×800，翼面板开启时同步扩展最小宽度。
- 菜单栏双菜单设计，支持 hover 弹出、点击固定、长按跳转外部页面三态交互。

### 6.2 三面板同步与隔离
- 跨面板同步：消息通过主进程中继广播到所有窗口，接收方过滤自身消息；quest 索引为父窗口共享数组，零拷贝；仅主面板扫盘写入，新建 quest 30 秒宽限期防误删。
- 工作空间隔离：单窗口对应一个主文件夹，不同工作空间通过专属 IPC 频道物理隔离；切换项目先销毁旧工作空间再初始化，配备并发锁防止竞态。
- quest 所有权：由父窗口注册表统一管理，同一 quest 仅能在一个面板打开，切换时先释放旧所有权再申领新所有权。

### 6.3 Card 队列与状态机
- 单个 quest 对应一张 Card DOM，创建后结构不变，切换仅通过 CSS 显隐控制，零 DOM 销毁。单 Card 最多保留 16 层已封顶楼层 + 1 层在建楼层，面板池上限 10 张，LRU 策略驱逐。
- 建楼状态机，状态转换严格校验：

| 当前状态 | 允许转换到 |
| :--- | :--- |
| idle | sending、fatal |
| sending | stopping、fatal、idle |
| stopping | idle、fatal |
| fatal | sending |

- 致命失败二态模型：ACTIVE 态红框展示错误与重试入口，可累积错误行；CAPPED 态密封移除重试入口，不再追加内容。发送按钮在任何失败场景下永不禁用，点击继续任务创建新楼层，错误信息累积到原失败楼层。
- 彗星电子钟：基于共享 agent 实例计时，各面板独立渲染，不跨面板串号。

### 6.4 发送管线
执行顺序严格遵循：前置校验 → 草稿升级 → 空值守卫 → 不变量检查 → 设置发送状态。
- 草稿升级在所有 agent 操作前完成，升级后保证 agent 实例非空。
- 模块级同步锁防止草稿升级异步竞态，所有入口加锁，所有执行路径 finally 释放。

## 七、持久化与文件系统
### 7.1 持久化六入口
- 全局级：qgs/qgs.simple → global.sq3，存储跨项目共享数据
- 项目级：qg → 文件系统原子写项目元信息；qgs.project → quest.sq3 存储对话数据；onlyStore → only.sq3 存储项目偏好与资产
- 禁止直接调用桥接 API、本地文件 API 与 localStorage 绕过标准入口。
- 所有写入采用「临时文件 + 重命名」原子方案，失败降级为复制替换，绝不先删除目标文件；启动自动清理残留临时文件，损坏数据库自动隔离重建。
- 程序退出信号触发自动同步刷盘，保证数据完整性。

### 7.2 目录结构规范
- 程序级数据：`{应用目录}/Data/alphal/`，零 C 盘写入，完全绿色便携。
- 项目级数据：`{项目根目录}/_qqq/`，随项目迁移，包含锁文件、对话库、资产库、专家框架、时间线数据。
- 对话正文存储于文件系统，元数据存储于 SQLite；时间线采用「压缩 blob + WAL 日志 + SQLite 快照 + 备份」的多层存储，保障十年级数据可靠。
- 项目锁：30 秒心跳，60 秒过期，防止同一项目多窗口打开。

### 7.3 附件与粘贴系统
- 四层粘贴管线：剪贴板中心 → 粘贴路由 → 锚点映射表 → 视图渲染。
- 锚点固定格式：`📎{sha256前12位}:{文件名}`，文本文件仅存纯锚点，内存映射表负责渲染，零正则扫描。
- 粘贴文件落盘规则：统一存入当前文件所在目录的 `_qqqvault/` 子目录，不写入项目元数据目录；零路径兜底，无法获取目录则报错不落盘。
- 编辑框允许多次插入同一文件，发送构建上下文时自动去重，不浪费上下文空间。

### 7.4 时间线与 Diff
- 五种触发时机：AI 写工具、命令执行、编辑器失焦/保存、Diff 窗口编辑保存；编辑类操作共享 100 秒冷却 + SHA256 去重。
- 独立 Diff 窗口，单文件单例，并排对比，支持编辑模式；编辑保存同步生成快照。
- Git 集成：X 区标签页展示，支持多仓库，配备独立 Diff 窗口。

## 八、Goods 扩展系统
### 8.1 核心协议
- 宿主唯一入口 `core/gaea-host.js`，统一注册接口，支持侧边栏面板、标签页、后台服务、命令四种能力。
- 生命周期分两类：随主窗口生死、独立程序运行；音频分两类：IDE 统一音量控制、独立音量体系。
- 统一打开入口 `qqqGaea.open()`，根据组件配置自动路由，禁止硬编码展示调用。

### 8.2 单例与进程控制
- 默认禁止多开，采用三级检测（内存注册表 → 本地 PID 文件 → OS 级状态文件）+ 脚本自保双重防多开机制。
- 10 秒心跳、30 秒过期，跨开发版/绿色包/打包版全同步；单例冲突 30 秒冷却，防止跨实例启动风暴。
- 双控解耦：状态指示点实时反映进程存活状态，可独立点击启停；自启动开关仅控制看门狗，不直接终止进程。进程崩溃且看门狗存活时自动拉起。

### 8.3 数据架构
- 采用 OS 级标准路径存储数据，不使用 Electron 劫持的环境变量，保证多窗口、多进程、多版本读写同一文件。
- 读写分离架构：Bridge 负责读取、Python 进程负责写入，SSE 推送变更通知；Python 进程异常时 Bridge 兜底轮询，核心功能保持可用。
- Qt DPI 免疫：启动前设置缩放因子为 1，不受系统显示缩放影响；音频融合：按 IDE 全局音量统一控制组件音量。

### 8.4 图像处理铁律
- 图像处理全部使用云端专用工具，禁止本地编写 Python 图像处理脚本。
- 云端服务失败直接告知用户功能不可用，禁止降级到本地脚本；用户明确要求脚本处理的场景除外。

## 九、AI 面板核心机制
### 9.1 流式渲染
- 双层渲染模型：流式输出仅作为实时预览，API 返回的完整 content 为最终唯一真理，楼层完结时全量覆盖，零调和补丁。
- 渲染规则采用 flag 白名单，新增消息类型默认不渲染，安全优先。
- 双流看门狗：60 秒无数据中断、15 分钟无产出中断，保障链路健康。
- 文本工具调用统一解析回生，支持多种格式自动识别。

### 9.2 AI 工具体系
- 20 个 AI 工具全覆盖，支持项目相对路径、历史快照回溯、SHA256 溯源。
- 双包装盒体系：15 个温柔类工具（结果可复现）、5 个绝对类工具（结果不可复现，特殊标记包裹），上下文压缩时按类别分层剥离。
- 统一内容门 `ContentGateway`：所有工具结果与附件统一管控，≤50K 字符全文返回，>50K 取首尾各 20K；截断阈值仅一处定义。

### 9.3 上下文压缩
- 每层楼完结后触发重建，建楼过程中不触发；单条动态摘要 + 单条事实条目，严格保证唯一性。
- 三档压缩能力：绝对压缩（本地纯文本剥离）、仅编辑模式（保留工具头行）、AI 事实提取（调用模型提取核心事实）。
- 事实块独立追加，日常自动压缩不触碰事实条目；背包顺序固定，持久化采用原子写入。

### 9.4 异常自愈
- 孤儿 tool_calls 双向扫描自愈，每次网关调用前预检，请求失败后被动修复并重试。
- 三面板 auto-save 机制，内置三重守卫防止重复写盘；主面板正常时侧面板去重跳过，主面板崩溃侧面板自然接管。

## 十、开发、构建与发布
### 10.1 目录骨架
- 源码目录：`launcher/` C 启动器、`shell/` 壳层 TypeScript、`server-app/` 载荷层、`shell-build/` 打包脚本、`do/拓扑/` 架构文档。
- 绿色包目录：C 启动器为唯一入口，`gh555.com/` 为全托管目录，用户零手动操作。

### 10.2 构建与打包
- 开发：一站式脚本集成构建、开发服务、文件监听、Electron 启动。
- 源码与产物严格分离：TypeScript 源码与编译产物分离；webapp 源码与打包输出分离，输出目录已加入 git 忽略，禁止直接编辑。
- 静态文件修改必须同步升级 Service Worker 缓存版本号，否则旧缓存不生效；状态栏提供构建戳验证，可确认当前运行代码版本。

### 10.3 统计上报
- 主进程异步上报，不阻塞 UI；覆盖装机、在线、陪伴时长多维度统计，登录后触发。
- 首次上报随机抖动，失败指数退避；程序退出前持久化累计时长。

## 十一、已知架构漏洞
- 高危待修复：时钟/饼图跨面板不可见、SSE 断线无跨面板自愈。
- 中危待修复：面板生命周期脆弱，DOM 事件、定时器、观察者易无限累积。

---

## 附录：固定常量表
### 表1：核心依赖固定版本
| 依赖 | 固定版本 |
| :--- | :--- |
| Electron | 22.3.27 |
| sql.js | 1.11.0 |
| Monaco | 0.34.1 |
| esbuild | 0.19.12 |
| TypeScript | 5.0.4 |

### 表2：核心常量
| 常量 | 值 | 说明 |
| :--- | :--- | :--- |
| 进程硬超时 | 2h | 子进程最长运行时间 |
| 内存上限 | 2GB | 单进程内存阈值 |
| 失速看门狗 | 15min | 无输出自动终止 |
| 单例心跳 | 10s | 进程状态刷新周期 |
| 单例过期 | 30s | 状态文件失效时间 |
| 项目锁心跳 | 30s | 项目锁刷新周期 |
| 内容截断上限 | 50K | 单条内容最大字符数 |
| 单例冲突冷却 | 30s | 防跨实例启动风暴 |
| 流看门狗 | 60s | SSE 无数据中断阈值 |
| 产出看门狗 | 15min | 无有效产出中断阈值 |

### 表3：固定路径与文件
| 类别 | 路径/文件名 |
| :--- | :--- |
| 全局数据库 | Data/alphal/global.sq3 |
| 项目数据库 | _qqq/alphal/{quest,only}.sq3 |
| 粘贴文件目录 | 当前文件目录/_qqqvault/ |
| 组件清单 | engines/manifest.json |
| 启动器配置 | launcher-config.json |