qqq-shell-v2 架构 — 代码若与此冲突，以本文档为准。
===========================================================
命名规范: Tier1 外宣 qqq IDE / Tier2 编码 qqqide（全小写）
归一完成: 2026-06-04，shell/ + server-app/ 零旧名残留

三层模型
--------
┌──────────────────────────────────────┐
│ 壳 (shell/)                          │
│ Electron 22.3.27 · Win7+ · frame:false│
│ 入口: bootstrap.js → main.js          │
│ 职责: 窗口 + IPC + bridge + 热更新检查 │
│ 源码: main.ts + 11 子模块 (≤290行/个)  │
│ 产物: shell-out/ (esbuild ~35ms)      │
│ 热更: bootstrap 启动时原子替换 shell-out/ │
└──────────┬───────────────────────────┘
           │ spawn at boot (×2)
┌──────────▼───────────────────────────┐
│ 引擎层 (Rust ×2)                      │
│ q_win_x64.exe — IO引擎(fs/SQLite/IPC) │
│ ghrun.exe     — 进程管理(qz)（LSP守护已冻结）│
│ 兜底: qz-spawn 两层 (ghrun→Node)      │
│ 新增: Windows Job Object 内存硬限制(B2)│
└──────────┬───────────────────────────┘
           │ bridge IPC (preload暴露)
┌──────────▼───────────────────────────┐
│ 载荷 (server-app/)                    │
│ 纯前端: HTML+CSS+JS · Monaco 0.34.1   │
│ 热更: Deploy→服务器→客户端自动拉取     │
└──────────────────────────────────────┘


持久化层级 — global → project → quest → floor → house → room
------------------------------------------------------------------------
· global (qgs): 跨项目共享（最近文件夹/主题/语言/剪切板历史）
· project (qgs.project / qg): 单项目内共享（editor tabs/项目偏好/lock）
· quest (quest.sq3): 一次对话会话
· floor (楼层): 一次用户回车（可能含多次 API 调用）
· house (房子): 一次 API 调用（可能含多次 tool call）
· room (房间): 一次工具调用
· 铁律: 变量/字段命名遵循此层级，禁同义词(turn/session/round)。JSON: floor_id 非 turn_id


qz spawn 管线 — 两层 + 四重防御（2026-06-04 加固）
-------------------------------------------------------
· 管线: ghrun (Rust, 主力) → Node (child_process, 兜底)
· runner.py: 代码保留于 engines/runner.py，调用点已从 qz-spawn.ts 删除

四重防御纵深（针对 40GB PowerShell 内存爆炸事件）— 2026-06-11 调参:
  ① 硬超时 2h: SYSTEM_MAX_TIMEOUT=7200000，所有命令统一硬天花板，入口强制 cap
  ② 内存守卫: nodeTier 专属，异步 execFile（非阻塞），首检 5s→后续 15s
     ghrunTier 跳过 JS 守卫（Rust Job Object 已覆盖）
     execSync→execFile 修复了 0.2-2s UI 冻结 bug
  ③ ghrun 原生 Job Object: spawn.rs memLimitMb 字段，Windows CreateJobObjectW →
     SetInformationJobObject(JOB_OBJECT_LIMIT_PROCESS_MEMORY) → AssignProcessToJobObject
  ④ 失速看门狗: stallMs=15min 无输出→杀；output cap=64KB 防 IPC 洪水

· ghrun 反卡死: deadline watchdog + stall watchdog (100ms) + kernel 级进程组隔离
  Windows: CREATE_NEW_PROCESS_GROUP + CREATE_NO_WINDOW + taskkill /F /T
  POSIX:   process_group(0) → killpg (SIGTERM→2s宽限→SIGKILL) + cwd 预校验
· guardMs 修复: 原 timeout=0 时 guard=24h（形同虚设），现恒为 timeout+10s
· 内存守卫 killReason: 'mem-guard'（新增），与 deadline/stall/disconnect 并列
· 跨平台(2026-06-11): Win=JobObject / Linux+mac=setrlimit(RLIMIT_AS) via pre_exec
  JS层异步内存守卫双路径(Win=PowerShell WMI / Unix=ps -o rss)
· ghrun CI: gh555com/qqq-IDE → build-ghrun2.yml → 三端编译


AI 网关双线路 + 故障切换引擎（2026-06-13 加固，2026-06-23 402+双Key 增强）
---------------------------------------------
· 双线路架构:
  主线路: https://direct.gh555.com:8444/api/v3/ai/chat (直连 HTTP/1.1，87ms，无中间人)
  备用线路: https://gh555.com/api/v3/ai/chat        (CF Worker 透传 → Go → 推理引擎)
· 声明位置: system-prompt.js（唯一声明 GATEWAY_URL_PRIMARY / GATEWAY_URL_FALLBACK）
· 切换引擎: agent-loop.js _callGateway 内建 retry+switch 管线
  - 同 URL 最多重试 3 次（MAX_RETRIES=3），防瞬态抖动误切
  - 重试耗尽 → 切线路（主→备或备→主），单次最多切换 2 次（MAX_LINE_SWITCHES=2 防乒乓）
  - HTTP/2 连接级错误（ERR_HTTP2_PROTOCOL_ERROR / Failed to fetch）优先切线路
  - 502/503 前半段重试同 URL（可能瞬态恢复），后半段切线路
  - AI- AI 上游错误: 400/422（孤儿 tool_calls）跳过重试→走 auto-repair; 402（key 欠费）直接切线路（另一把 key 可能有钱） AbortError（超时）切线路恢复（主超时→切备，备超时→切主）
· 延迟报错: _callGateway 不直接调 onError，设 _lastGatewayMessage 后返回 null
  → agent loop 的 auto-repair 先尝试修复 → 成功则继续（用户无感）→ 失败才报错
· 跨面板协同: panel-state.js 监听兄弟面板 qqq-gw-status → 自动调整 _gwFallbackAt

双 Key 隔离 + JWT 解耦（2026-06-23）
---------------------------------------------
· 两把 API key 完全隔离于服务器 .env（客户端零密钥）:
  API_KEY_MAIN         → 主线
  API_KEY_FALLBACK → 备用线
· 线路→Key 路由: 客户端通过 X-Key-Slot header 告知 Go 选哪把 key
  主线: X-Key-Slot 不传 → Go PickAIKey()        → 主线消费
  备线: X-Key-Slot: 1    → Go PickFallbackAIKey() → 备用消费
· JWT 仅管认证（我是谁+ge扣费），X-Key-Slot 仅管路由（走哪把 key）
  两个身份体系完全解耦，互不穿透。
· SSE 402（服务端 key 欠费）→ 自动切线路→另一把 key → 用户无感
  HTTP 402（用户 ge 不足）→ 直接终止，不浪费线路切换
  双 key 都欠费 → MAX_LINE_SWITCHES=2 兜底 → "AI 服务暂时不可用"edis 封禁检查 — 已彻底移除（2026-06-23）
---------------------------------------------
· 旧机制: CF Worker 调 GET /api/v3/ai/check?th=hash → Go 读 Redis banned:th → 永远空(零写入端)
· 移除: AICheckHandler 函数、路由 /v3/ai/check、CF Worker banned check 段、sha256hex 函数、fix_worker.py
· 封禁唯一真理源: middleware_ban.go 查 doers.banned_until（PostgreSQL），CF Worker 不做预检
· 验证: grep 全代码库 banned:th CHECK_URL AICheckHandler → 零匹配

CF 来源强制（2026-06-23，P1）
---------------------------------------------
· EnforceCFOrigin 中间件 → 仅生产环境(APP_ENV=production)拦截 /v3/ai/chat
· 无 CF-Ray 头（直连服务器 IP）→ 403 DIRECT_ACCESS_DENIED
· 开发/测试环境(APP_ENV≠production) → 自动跳过，不拦截
· 公共端点(/__lbprobe、/api/time、/px) 不受影响

计费幂等（2026-06-23，P0）
---------------------------------------------
· CheckAITurnIdempotent → INSERT INTO idempotency_keys ON CONFLICT DO NOTHING
· 同一 floor goroutine 重复触发 → 仅扣费一次（防重放双扣）
· DB 故障 → 安全降级不扣费（保守安全）
· key 格式: ai_turn:{doerID}:{floorID}，复用现有 idempotency_keys 表（24h 自动清理）

Nginx 备用线路铁律（2026-06-13）
---------------------------------------------
· port 8444 必须纯 HTTP/1.1（listen 8444 ssl; 禁 http2 关键字）
· 原因: Nginx HTTP/2 ↔ Go HTTP/1.1 SSE 长连接存在协议转换 bug → ERR_HTTP2_PROTOCOL_ERROR
· sites-enabled/ 目录只保留 gh555 一个文件（include * 匹配所有文件，.bak 也会生效）
· 验证: openssl s_client -connect :8444 -alpn h2 → "No ALPN negotiated"

ContentGateway 超时常量（唯一真理源，2026-06-13）
---------------------------------------------
· 所有超时数字只在 content-gateway.js 定义，agent-loop.js 全部引用 ContentGateway.*
· FETCH_DEADLINE_PRIMARY_MS  = 180000 (180s, 直连主线无限制，可调大)
· FETCH_DEADLINE_FALLBACK_MS = 98000  (98s, CF Worker 备用，CF 100s 留 2s)
· STREAM_WATCHDOG_MS         = 180000 (180s, 深度推理容忍度)
· AI_OUTPUT_WATCHDOG_MS      = 900000 (15min, AI 无产出→死循环)
· 改超时只改 content-gateway.js 一处。

孤儿 tool_calls 自愈系统（2026-06-14 加固）
---------------------------------------------
· 问题: 模型硬要求 assistant.tool_calls 后紧跟等量 tool 消息，缺→400
· 自愈: _repairOrphanedToolCalls() 双向扫描 conversation（200 轮，方向 A/B 日志记录）
  ① 请求前主动预检（_callGateway 开头，conversation 未变则跳过）
  ② 楼层保存前修复（_saveAgentQuestData → _a4BuildCompleteFloorPayload 归一，发现腐蚀打 [CRITICAL] 日志）
  ③ 自动保存前修复（auto-save 每 5s，同上））
· 恢复阶段循环修复至干净（最多 10 轮 × 200 次 = 2000 次扫描）
· 成功后重置 _floorOnErrorCalled
· Go 服务端 sanitizeMessages 双向校验（方向 A+B，gaea 源码已更新）


gaea goods 协议
-----------------
A 区宿主: core/gaea-host.js (完整 panel/tabs/services/commands/provides/uses)
注册接口: window.qqqGaea.register({ id, title, ... })
goods 切换按钮: 菜单行2 (qqq-goods-bar, 由 gaea-host renderTabBar 渲染)

已注册 goods (goods/ 下):
  rage — q3 全部功能 (rage.html A区面板 + Roam tab + 5 Monaco 服务)
  wysiwyg — 所见即所得面板
  navigator — 文件导航
  paste-image — 粘贴图片
  file-explorer — 文件浏览器
  search — 高性能项目搜索 (X区tab, ripgrep引擎)
  git — Git操作 (A区面板 + X区 Git tab)

搜索引擎 — ripgrep 单引擎（2026-06-25）
---------------------------------------------
· 引擎: ripgrep 14.1.1 (Rust · mmap · SIMD · PCRE2 JIT)
· 入口: shell/ipc-search.ts spawn rg.exe --json → 流式解析
· 查找: _findRipgrep() 三路 (userData/components/ → resourcesPath/ → engines/ripgrep/ → PATH)
· 打包: package.json build.files 含 engines/**, 打包前 components.py ensure ripgrep
  发 mac 包 → 在 mac 上跑 pack:mac-arm64, 二进制自动带在 engines/ripgrep/rg 下
· UI: goods/search/ (search.js 注册 + search-ui.html X区标签), ai-viewport.js 🔍 按钮

Monaco 五件套 (codelens/decoration/paste/viewzone/video) 已搬入 goods/rage/services/。

Monaco Worker 加载
------------------
两类 Worker：
  · 基础 worker (workerMain.js)：自带 AMD loader (Y/J 函数)，用 new Worker('qqq-asset://...') 直接加载 →
    self.location 正确，importScripts 相对路径正确解析。
  · 语言 worker (tsWorker/jsWorker/jsonWorker/htmlWorker/cssWorker)：裸 define() 模块，无 loader。
    走 blob：fetch loader.js + require.config(paths) + worker 代码拼接。

依赖补全：Monaco 0.34.1 min 构建将所有模块捆入 editor.main.js，无独立 AMD 文件可用。
  · convert_monaco_esm.py：ESM→AMD 转换器，将 esm/vs/ 796 文件转为 AMD define() 格式 → cache/monaco-deps/vs/
  · shell/main.ts qqq-asset:// 协议：monaco 根先查 node_modules/min/vs/；缺文件时 fallback 到 cache/monaco-deps/vs/

组件管理
-----------
外部清单: engines/manifest.json — 5个组件 (ffmpeg/yt-dlp/python/git/ripgrep)
  改URL不需重编译ghrun（LSP 组件 2026-06-23 已删除）
三层加载: userData/manifest.json (用户覆盖) → engines/manifest.json (随发布) → manifest.rs 硬编码兜底
安装脚本: op/components.py — ensure(下载+解压) / upload(灌装CDN) / list(查看状态)
  ★ ripgrep 安装: python op/components.py ensure ripgrep → 自动下载当前平台二进制
  ★ 打包: package.json build.files 含 engines/**，打包时自动携带 engines/ripgrep/ 下当前平台二进制
LSP 相关已冻结（2026-06-23）: 交互面板④/install_lsp.bat/components.py LSP 部分均失效，
  manifest.json LSP 条目已删除，壳层 lsp-bridge.ts 零引用，仅源码保留于磁盘。

  替代：三级语法检查（2026-06-24 落地）
    L1 自动（0 ge）：每次 edit_file/write_file/create_file 后系统自动运行 node --check / py_compile / JSON.parse，
      结果拼接到工具返回值末尾 ([SYNTAX OK] 或 [SYNTAX ERROR])，代码在 tools-exec-write.js _autoSyntaxCheck()
    L2 手动（~50 ge）：AI 调用 get_diagnostics 工具 → 复用 _autoSyntaxCheck，代码在 tools-exec.js
    L3 全仓（~200 ge）：AI 重大重构后跑 run_command("find . -name '*.js' -exec node --check {} \;")，prompt 引导在 system-prompt.js

目录结构
--------
qqq-shell-v2/
├── shell/             壳层TS（入口 main.ts → 13 子模块）
│   ├── main.ts              入口：组装+启动
│   ├── boot.ts              启动配置/健康检查/壳层热更/启动序列
│   ├── window-manager.ts    窗口创建/缩放/边界/全局快捷键
│   ├── asset-protocol.ts    qqqide-asset:// 协议/资产根/磁盘空闲
│   ├── ipc-state.ts         共享状态（qwr机器/Python路径/跳过列表）
│   ├── ipc-boot.ts          启动信息/重试/探测 IPC
│   ├── ipc-fs.ts            文件系统 IPC（读写列表状态等）
│   ├── ipc-ai-tools.ts      AI工具 IPC（search_text/find_files/list_files）
│   ├── ipc-search.ts        搜索引擎 IPC（ripgrep 单引擎，qqqide:search）
│   ├── ipc-edit.ts          编辑工具 IPC + qwr 保护
│   ├── ipc-media.ts         已删除（2026-06-22，图像全走 Go 代理）
│   ├── ipc-timeline.ts      Timeline 版本时间线 + Diff窗口 IPC
│   ├── ipc-misc.ts          窗口/对话框/资产根/磁盘 IPC
│   ├── shutdown.ts          安全加固/退出处理器/崩溃兜底
│   ├── preload.ts           预加载桥
│   ├── qz-spawn.ts          进程生成（四重防御）
│   ├── lsp-bridge.ts        LSP 诊断桥（冻结，零引用）
│   ├── state-sqlite.ts      SQLite 引擎
│   ├── state-store.ts       状态存储门面
│   ├── qg.ts                FS 原子写
│   └── ...                  其他服务（engines/audio/monaco-host 等）
├── server-app/        载荷层(热更)
│   ├── index.html
│   ├── core/           IDE 平台 (shell/theme/editor/qoast/cooldown-guard/state-sdk/gaea-host/tab-manager/...)
│   ├── locales/        13 语言文件 (zh.json 唯一真理源)
│   ├── goods/          goods (rage+navigator+paste-image+wysiwyg+file-explorer+search+git+_shim)
│   └── ai-panel/       AI面板（模块化拆分，index.html 仅 HTML/CSS 壳）
│       ├── index.html         壳文件（~400行 HTML+CSS + script src 引用）
│       ├── panel-state.js     全局状态 + 前向声明 + 同步桥
│       ├── panel-search.js    搜索逻辑 + CSS Highlight API
│       ├── panel-input.js     输入处理 + tier 选择 + 图片粘贴
│       ├── panel-render.js    消息渲染 + Markdown→DOM
│       ├── panel-quest.js     quest 生命周期 + 所有权
│       ├── panel-floor.js     楼层管理 + all.txt 归档触发
│       ├── panel-quest-ui.js  quest UI 交互 + 下拉列表
│       ├── panel-clock.js     电子钟 + 饼图 + 自动保存
│       ├── panel-alltxt.js    all.txt 流写入 + reasoning.txt 审计（从 all.txt 截断生成）
│       ├── panel-a4.js        A4 文件快照（executeTool 拦截 + 瀑布列表 + 持久化）
│       ├── panel-send.js      发送逻辑 + 事件绑定 + DOMContentLoaded
│       ├── panel-floor-indicator.js 滚动楼层指示器（豆腐块+探针）
│       ├── card-pool.js       Card Queue 引擎（切换零 DOM 销毁，16+1 视口）
│       ├── quest-store.js     多任务 SQLite 持久化
│       ├── agent-loop.js      核心 Agentic 循环（send/stop/stall/repair/inject/render, ~1300行）
│       ├── agent-envelope.js  信封剥离 + UTF-8 截断（_utf8Trunc, EnvelopeStripper）
│       ├── agent-gateway.js   网关调用（双线路+重试+故障切换, _callGateway）
│       ├── agent-sse.js       SSE 流解析 + 文本工具调用回生（_parseSSE）
│       ├── agent-vision.js    视觉预分析（_analyzeImages + _callVision）
│       ├── agent-exec.js      并行工具执行引擎 + 分层调度（_executeToolCallsParallel等）
│       ├── agent-context.js   上下文压缩（三专家架构, _compressContext）
│       ├── tools-defs.js       AI 工具定义 + 分类 + 常量 + 辅助函数
│       ├── tools-exec-write.js WRITE 类工具（edit/write/create/delete）
│       ├── tools-exec.js       工具调度中心 + READ 类工具（read/search/list/find/diag/fetch）
│       ├── tools-exec-effect.js EFFECT 类工具（run_command/search_smart/generate/analyze）+ Embedding
│       ├── embedding-service.js Embedding 向量化服务
│       ├── content-gateway.js 内容安全网关
│       ├── ai-gateway.js     统一 AI 模型调用包装器（禁直接调用第三方端点）
│       ├── only-store.js      项目偏好 SQLite
      ├── panel-expert.js    E-Flow 引擎（复杂度评估+框架推荐+自动维护）
      └── expert-template/   标准 expert 文档框架模板
├── engines/           二进制 + manifest.json (5组件: ffmpeg/yt-dlp/python/git/ripgrep)
│   └── ripgrep/rg.exe  ripgrep 14.1.1 (Windows 已安装; mac/linux 二进制同目录)
├── op/                运维脚本 (ky.py翻译 + components.py组件 + git_snapshot.bat)
├── do/拓扑/           架构文档


构建与打包（shell-build/）
-----------------------------------
· 开发: qide-shell.py 按 Q → esbuild build → dev-server :8090 + esbuild watch + electron
· 编译: shell/*.ts → esbuild → shell-out/（~35ms）。npm run build 一次性编译。
· 打包: npm run pack:win-x64 → dist-pack/qqq-shell-win-x64.zip
· ★ gh555.com/ = npm run pack 中间产物（Electron 运行时 + resources/app/ 副本）
  已在 .gitignore。dev 模式完全不经过它。AI 禁止编辑 gh555.com/ 下任何文件。
· 绿色包 = 最终 zip 包，用户解压即用。pack.js 打包后会自动 rm -rf unpacked 中间目录。
· 教训: search_replace 跨行替换时需确保匹配文本唯一，否则会合并相邻行损坏代码。

UI 布局 — A/X/AI + 左右翼（三面板）
-------------------------------------------------------------------------------
  外嵌 BrowserWindow 僚机已删除。左右翼改为同窗口 iframe（灯泡开关控制）。
  中区     .qqq-middle        flex row: A区 + X区 + 中AI(389px)
  A区      #qqq-a-zone       gaea goods 宿主
  X区      #qqq-x-zone       编辑器 + tab groups + output
  中AI     #qqq-ai-zone      中面板(panel=1)，始终可见
  左翼     #qqq-wing-left    左面板(panel=0)，灯泡开关，默认关闭，宽度=AI_W
  右翼     #qqq-wing-right   右面板(panel=2)，灯泡开关，默认关闭，宽度=AI_W
  灯泡     #qqq-bulb-1/2     菜单行2 红色圆点，toggle 开关 + 窗口伸缩

菜单栏 (shell-menu.js + menu-schema.js) — 2026-06-25
----------------------------------------------------------
· 顶部两菜单："qqq IDE" + "的梦gaea"（无 i18n，纯文本标签）
· 子菜单 hover 高亮：IIFE 捕获行引用修复闭包 bug（原 var hoist 导致高亮错位到末行）
· "开新窗口" 行特殊：min-width:210px（比其他行宽 30px），右侧 20×20px ▶ square button
· hover ▶ → 延迟 200ms 弹出最近主文件夹下拉（数据源=qgs recent_folders，与 AI 视口+号共享）
· 点击下拉文件夹 → 保存当前窗口快照 → 新窗口以 ?restore=1 模式打开

窗口快照系统 (win_snap:{normalizedPath} in qgs global) — 2026-06-25
------------------------------------------------------------------------
· 保存时机：beforeunload / file.exit / file.newWindow / 从最近列表打开新窗口前
· 快照内容：mainFolder + auxFolders[] + editorTabs[{path,groupIdx,active,preview}]
             + editorPositions{filePath:{lineNumber,column}} + layout + theme
· AI 面板状态不重复存储——onlyStore (only.sq3) 已完整记录 ai.panel.*.activeQuestId
  + ai.uiStates.{panelId}，新窗口 initQuests() 自动从 onlyStore 恢复
· 光标位置：editor.js 新增 getAllEditorPositions() + setEditorPosition(fp,pos) API
· 还原流程：tab-manager._restoreTabsFromSnapshot 设置 qqqPendingEditorPositions
  → 分发 qqq-file-open → openInPane 创建 editor 后自动消费 pending 位置
· URL 参数：bridge.window.new(folderPath) → ?restore=1&folder=xxx（无 folderPath → ?fresh=1）
· 锁检查：ipc-misc.ts 窗口创建前检查 project lock，已锁 → 返回 locked:true

Card Queue — 终极 AI 面板架构（2026-06 重构，2026-06-10 加固）
-------------------------------------------------------------------
· 一张 quest = 一张 Card（完整 DOM 子树），创建后永不 innerHTML 清空
· 切换 quest = 纯 CSS display 显隐，零 DOM 销毁，零竞态
· timer 跑在 agent 上，切 quest 不停 timer，切回零干预（§36）
· 每张 Card 最多持有 16 层已封顶 + 1 层在建 DOM（FLOOR_CAP_CAPPED=16 / BUILDING=1）
· 超限 → 最老楼层 DOM remove() 彻底删除（conversation JSON 保留于内存）
· Card Pool 上限 10 张，LRU 驱逐（CARD_POOL_MAX）
· 搜索/选中同词高亮 + 滚动条标记叠层（暗红 5px 直角滚动条 + 青绿/黄色刻度线）
· 滚动：上滚立即停，下滚到底才恢复自动跟；scrollTop = scrollHeight 无 scrollIntoView
· 楼层 az 区（每层楼底部信息区，从上到下）：
    A2 财宝块 — 当前楼层 AI 挖掘的 treasures（来自信封），无财宝时隐藏
    A4 文件快照块 — 实时文件变更瀑布列表（before/after + +N -M），点击开 diff
    A1 楼层统计 — Floor/House/Room 计数 + FILE/ROW 变更 + 审计/翻译按钮
    A3 时钟行 — 左=时钟+饼图，右=GE 成本（gh555.com 精确扣款）
· A2 铁律：始终在 A1 上方（_initTreasureBlock → insertBefore(a1Block) 无条件）
· A1 铁律：始终在 A3 上方（_initA1Block → insertBefore(clockBlock) 无条件）
· 财宝去重：每条 treasure 以 text 前 80 字符为 key，全局只注入上下文一次
· A1 行2 持久化：floorPayload.fileStats 入 SQLite，重启不归零
· q3 楼层导航已删除（翻页需求由 search_quest.txt 全文检索替代）
· _makeName UTF-8 截断修复（回退到完整序列起点，零乱码）
· 所有跨模块引用走 window.*（questStore/agentPool/cardPool/9 个函数）
· 配置常量位于 card-pool.js 头部，改一处全生效

q2 单行 + hover 展开（2026-06 重构）
-----------------------------------------
· 招牌行：有 quest → "q{n}.{title}"；无 quest → "~ New quest ~"（斜体灰）
· hover 展开下拉 → 虚线框 + 效果 q（左金边+渐变+外发光+悬浮层统一流光）
· 悬浮层流光：单个 .q2-shimmer-overlay DOM 元素覆盖整区，pointer-events:none，零布局干扰
· 下拉列表：2 行弹性高度、默认 20 条（滚轮扩容 40→60）、模糊子序列匹配
· 搜索与新建整合进下拉头部（+ 按钮 + 搜索框），不再有独立 q2x 行
· 三面板各自独立持久化：关闭前 quest/光标/滚动/输入草稿全部保存（ai.uiStates.{panelId}）
· q2 与面板焦点完全解耦：焦点不改变 q2 任何样式，焦点仅用于快捷键+postMessage 广播
· 快捷键（仅焦点 + 非编辑区）：1/2 上下滚 35%视口、q/w 前后楼层跳转
· 统一高亮色：豆腐块 hover/编辑笔 hover/下拉行 hover/搜索框 focus 全用 #fdf6e3（暗 #3f3830）
· 一个 quest 同时只能在一个面板打开（所有权系统 + 跨面板 sync 广播互斥）

持久化 — qgs + qgs.simple + qg + qgs.project + onlyStore + qgf 六入口
------------------------------------------------------------------------
渲染层六个独立入口:
  window.qgs('my-ns')                → SQLite 全局状态（云同步、可 SQL 查询）
  window.qgs.simple('my-ns')         → 同 qgs 但零配置（省 v/form/cloud 参数，默认 doc）
  window.qg(rootDir, 'my-ns')        → FS 项目元信息（qqq/qg/ 原子写、零依赖）
  window.qgs.project(dbPath, ns)     → SQLite 项目级（qqq/alphal/quest.sq3、对话数据）
  onlyStore (only-store.js)          → SQLite 项目级偏好（qqq/alphal/only.sq3、快照/偏好/状态）
  window.qgf(rootDir, 'my-ns')       → FS 项目级 KV（qqq/qgf/ 原子写、零依赖、预留未用）

背后引擎 + 物理路径:
  · qgs / qgs.simple / qgs.project  (state-sqlite.ts): sql.js WASM → 内存SQLite → 批量落盘
    全局: Data/alphal/global.sq3 (跨项目共享: 窗口快照/最近文件夹/语言/设置/主题)
    项目: qqq/alphal/quest.sq3 (AI 对话索引)
  · qg                              (shell/qg.ts):      tmp+rename 原子写 → JSON/brotli/NDJSON
  · qgf                             (shell/qgf.ts):     同 qg 引擎，建 qqq/qgf/{ns}.json 原子读写
  · onlyStore                       (only-store.js):    内存缓存+自动批量刷盘管线
    物理: qqq/alphal/only.sq3 (项目偏好: 搜索/主题/面板状态/字号)

何时用哪个：
  · 全局共享、跨项目、云同步                           → qgs
  · 全局共享、零配置（不关心 v/form/cloud）              → qgs.simple
  · 项目级元信息、零配置、不想建 DB                      → qg
  · 项目 AI 对话历史（quest.sq3）                        → qgs.project
  · 项目偏好/快照/面板状态/编辑器字体大小                  → onlyStore
  · 项目级 KV、零配置、不让用户看到 DB 文件（预留）        → qgf
  · ★ 渲染层不要直接用 bridge.state.* / bridge.fs 绕过入口

onlyStore 管线：set→内存缓存(dirty)→500ms空闲刷盘→3s强制刷盘→beforeunload兜底

已接入: editor tabs (qgs.simple:qqqide) 、shell layout (bridge.state→qgs:qqqide)、
        rage 剪切板 (qgs:rage.clip)、cache meta (qgs:qqq.cache)、
        AI quests (qgs.project:qqq.ai)、AI 面板状态 (onlyStore:ai.*)、
        主题 (onlyStore:theme)、编辑器字体大小 (qgs.simple:qqqide)、
        ★ 窗口快照 (qgs:qqqide, key=win_snap:{normalizedPath}) — 含主/辅文件夹+editor tabs+光标位置+layout+theme

家目录 — 项目级 ({项目}/qqq/) 与程序级 ({程序}/Data/alphal/)
-----------------------------------------------------------------------
  项目级: {project_root}/qqq/（纯项目数据，跟项目走，拷到另一台机器自带）
  程序级: {appRoot}/Data/alphal/（零 C 盘写入，绿色包解压即在，惰性创建）
  ★ 项目偏好 (搜索/主题/面板) → {项目}/qqq/alphal/only.sq3
  ★ 全局共享 (语言/最近文件夹/窗口快照) → {程序}/Data/alphal/global.sq3
  ★ {程序}/Data/databases/ 是 Chromium 内置 IndexedDB 目录，非本项目产物

  {项目}/qqq/
    ├── .lock              30s心跳/60s过期文件锁，防多窗口打开同一项目
    └── alphal/
        ├── quest.sq3         AI 对话索引 SQLite（questStore: 楼层元数据 ~200B/层）
        ├── only.sq3          项目偏好 SQLite（onlyStore: 搜索偏好/主题/tabs/字号）
        ├── rule/project.txt  E-Flow custom 模式规则文件
        ├── expert/           E-Flow standard 模式文档框架（index.md + arch/*.md）
        └── .lock             项目锁文件
  qqq/quests/             AI 对话正文（FS）
    ├── _index.json       quest 索引（跨面板共享、30s宽限期防竞态）
    └── q{n}.{title}/     每个 quest 一个目录
        └── f{n}.{question}/
            ├── all.json  机器用（conversation + 计费 + 快照 blob_hash）
            ├── all.txt   人类读（thinking + 工具调用 + 结果 + 回答完整母本）
            └── reasoning.txt  审计用（all.txt 截断工具结果到300字）
  qqq/timeline/           文件版本时间线
    ├── blobs/{sha256[:2]}/{sha256}.gz  内容不可变（SHA256+gzip）
    ├── timeline.wal       NDJSON 增量日志（≤99行）
    ├── timeline.db        SQLite 全量快照（每100条压缩）
    └── timeline.db.bak    备份（损坏自动恢复）
  qqq/qg/                 qg 写入目录（JSON/brotli/NDJSON 原子写）
  qqq/qgf/                qgf 写入目录（预留，零消费者）

i18n — 唯一真理国际化机器
---------------------------
语言文件: server-app/locales/{lang}.json (13 种语言)
唯一真理源: zh.json (开发者只写中文)
占位符: data-i18n / data-i18n-placeholder / data-i18n-title / data-i18n-html, JS: _i(key, fallback)
特殊: contenteditable div 用 data-i18n-placeholder-attr (iframe translateUi 处理)
翻译机器 + 面板: op/qky.py (PySide6 GUI, 翻译模型, 差异翻译, 全并发)
前端运行时: core/i18n.js (autoload, detectLang -> loadLang -> updateDom)
英语内嵌兜底: en.json 编译进 _EN_BUILTIN 常量, 离线永不显示 key
语言持久化: qgs('qqq.i18n').set('lang', code)
语言切换器: 菜单行1 中字按钮 → 13 语言下拉 → i18n.setLang() + postMessage 同步 AI iframe
AI iframe 桥接: translateUi() 调用 parent.window.i18n.updateDom(document), 监听 message('qqq-lang-change')

IPC 管线 — 文件操作都在主进程
------------------------------
主进程 handlers: bridge.ai.* (8个工具 + hover)
  search_text / find_files / list_files  → 主进程 fs (零 IPC 序列化)
  read_file / edit_file / create_file / delete_file / write_file → 主进程 fs (1 IPC)
  hover → 主进程 (轻量, 无大文件序列化)
  fs:remove 静默 ENOENT（文件已不存在=移除目的已达）
渲染层 fallback: 所有 execute* 优先走 bridge.ai.*，失败回退 fs 直接调用
engineHost 裁剪: Rust 引擎仅实现 ping，fs.read/write 已移除 engineHost 中转
read_file 主进程 50MB 守卫: stat()→超限返回错误提示 paginate
edit_file 三级匹配引擎: L1精确/L1b CRLF归一化/L2空白归一化/L3逐行 —— 主进程+渲染层双份
编译管道: shell/main.ts (唯一真理源) → esbuild → shell-out/main.js (215KB, 47ms)
铁律 §20: 禁编译产物混入源码目录，AI 改代码只改 *.ts，改完跑 npm run build

主进程窗口安全
--------------
  所有 webContents.send 调用前检查 win.isDestroyed() + win.webContents.isDestroyed()。
  sync:broadcast / project-path 推送 / state:changed 推送均已加固。
  global uncaughtException handler 拦截 Object has been destroyed 防止 crash。
  fs:remove 静默 ENOENT，不再刷错误日志。

工作空间（Workspace）— 多项目隔离（2026-06-07 重构）
-----------------------------------------------------
定义: 一个工作空间 = 一个主文件夹 + 其全部运行时状态。同一窗口同一时刻只有一个工作空间。
唯一标识: _workspaceRoot = 主文件夹路径（normalized）
生命周期:
  bindMainProject() → 同 root 跳过 | 不同 root → _teardownWorkspace() → _initWorkspace(root)
  _teardownWorkspace: 保存 UI 快照 → 释放 quest 所有权 → 销毁 Card Pool → 清空 agentPool
    → 重置 questStore/onlyStore → 清空 _workspaceRoot → 清理 DOM
  _initWorkspace: 获取锁 → questStore.setProjectRoot → 新建 Card Pool → 迁移 alphal
    → 加载 questUIStates → initQuests
IPC 频道隔离:
  _syncChannel() = 'qqq-sync:' + sanitize(_workspaceRoot)  // 非字母数字→_
  _broadcast 和 handleSyncMessage 订阅均走 scoped channel，不同主文件夹物理隔离
Card Pool: workspace 切换时 destroy() → 中止所有 agent + 移除所有 card DOM + 清空 pool
questStore: setProjectRoot(null) 清空 _rootDir/_qgs/_index；clearOnChange() 防回调累积

跨面板通信 — IPC sync 桥（三面板互斥）
---------------------------------------------
  _broadcast 走 parent.qqqideBridge.sync.broadcast → 主进程 relay 到所有窗口。
  主进程 sync:broadcast 必须发到所有 webContents（含发送者），否则同窗口三面板无法互斥。
  _handleSyncMessage 第一行过滤 msg.windowId === _windowId 跳过自身广播。
  三面板 _windowId 必须含 _panelId（sessionStorage key 独立），否则所有权系统失效。

qwr 机器 — 全局唯一真理写机器
-------------------------------
位置: shell/main.ts (源码) / shell-out/main.js (编译产物)
5层防御:
  ① per-file 串行队列 (_qw Map<path, Promise链>): 同文件排队，不同文件并行，零轮询
  ② 全局命令屏障 (_co/_ac 双向门): 命令等写完成才执行，写出等命令完成才写
  ③ edit_file CAS 重读: 队列内重新读文件匹配，不依赖 AI 旧快照
  ④ 外部修改检测: read_file 记录 {mtime,size}，write_file 对比不匹配则拒绝
  ⑤ 快照自更新: edit/create 写入后更新快照，delete 清除快照
命令路径全入 _qgc: qz:spawn / ghrun:exec / engine:invoke(spawn) 3入口统归管
涉及: edit_file / write_file / create_file / delete_file / run_command (5个)
保证: 零脏写、零丢失更新、零死锁、零误报

Timeline 文件版本时间线 (2026-06-13 钩子Q+X重构，2026-06-21 终极存储)
--------------------------------------------------------------
独立 BrowserWindow + Monaco Diff Editor。旧系统全部删除。

存储（极简，零延迟，三文件）: {projectRoot}/qqq/timeline/
  blobs/{sha256[:2]}/{sha256}.gz — 内容不可变，永不删除（SHA256 寻址 + gzip，git 内核）
  timeline.wal — NDJSON 增量日志（每快照追加一行 ~100B，最多 99 行）
  timeline.db — SQLite 全量快照（每 100 条压缩一次，db.export→.bak 同步备份）
  timeline.db.bak — 索引备份（压缩后同步更新，.db 损坏时自动恢复）
  ★ SHA256 行尾归一化（CRLF/LF/CR→\n），同内容必同 hash，零幽灵版本
  ★ 写入: 每条快照→内存 INSERT + append .wal（零延迟）；第 100 条→压缩 .db + .bak→清 .wal
  ★ 恢复: 加载 .db（损坏→.bak→空库）→ 回放 .wal（≤99行）→ 完整
  ★ 退出时强制压缩一次。无 2s debounce，无 archive 目录。，无篡改风险）

触发: 5种时机（3 种 source）—
  ① 钩子 Q — AI写工具(edit/write/create/delete) → _a4PersistToTimeline，source='q'
  ② 钩子 Q — run_command → (用户可开关, 默认关) captureChanged → source='run-command'
  ③ 钩子 X — X区 editor blur / Ctrl+S / 主编辑器 save() → source='editx'
  ④ Diff窗口编辑保存(blur/Ctrl+S) → source='diff-edit'
  ★ editx + diff-edit 共享主进程真理机：100s 冷却 + SHA256 去重（改 ipc-timeline.ts 一处全局生效）
  ★ 钩子 Q（q / run-command）不走冷却
  ★ 钩子 Q 已记全 before+after，点击 A4 不再补记；楼层完结不再补记

Diff窗口: 独立 BrowserWindow，一个文件单例（Map），再次点击复用
  Monaco 0.34.1 side-by-side + minimap + 手动差异折叠(setHiddenAreas) + 自动换行
  下拉标签格式: "#N 时间 +N -M xx [first] [before]"，xx=溯源串(q / editx / cmd / diff edit)
  动作栏+状态栏已删除(2026-06-24)，空间全部给编辑器
  底部居中有三按钮：☐仅差异 | 编辑/取消编辑 | ✕关闭
  编辑模式: 右下拉替换为"编辑中 未保存/已保存/已保存 且打快照至：#N"，右侧可编辑，左侧永远只读
  编辑模式每差异块显示→箭头，点击复制左侧到右侧
  编辑保存(blur/Ctrl+S)触发快照(source='diff-edit', 100s冷却+去重，主进程真理机)
  退出编辑模式不保存，取消编辑清空脏标记
  偏好: 仅差异模式项目级持久化(only.sq3:qqq.timeline.diffOnly)，默认全文对比
  编辑器极致精简: 零LSP/零波浪线/零智能，只留 tokenization
  防并发: _renderToken 竞态令牌 + _loading 重入锁 + model 显式 dispose
  Monaco 加载 15s 超时保护，防永久白屏
  编辑器极致精简: 零LSP/零波浪线/零智能，只留 tokenization

IPC: qqqide:timeline:{record,versions,content,stat,readCurrent,captureChanged}
  + qqqide:open-diff-window + qqqide:diff:update

跨十年保证: SHA256 blob 不可变 + .wal 增量日志 + .bak 双备份 + 永不删 → 时间戳/内容永久锁定

AI 工具 (16个) — 2026-06-04 强化，2026-06-10 三项优化，2026-06-16 search_smart
------------------------------------------------------------
文件: read_file, edit_file, create_file, delete_file, write_file (★ 主进程 1 IPC, qwr机器保护)
搜索: search_text (regex), search_content (多关键词OR, 新增), find_files (glob), list_files,
      search_smart (★ BM25语义+Embedding混合重排, 三路并行引擎, 新增)
  · search_text 二进制过滤: SKIP_EXTS 覆盖 70+ 种二进制格式
  · search_text 目录跳过: SKIP_DIRS 排除 node_modules/.git/dist/__pycache__ 等
  · search_content: 接收 keywords[] 数组，自动转义拼接为 regex OR，支持 case_sensitive
    解决 AI 用 PowerShell 搜多关键词的 40GB 问题，route 走 search_text 主进程管线
  · search_smart: BM25倒排索引 + 符号匹配(定义/导出/引用) + Embedding语义重排
    三路并行: 文件内容分词搜索 / 符号名精确匹配 / 目录名glob匹配
    Embedding管道: BM25 top10 → 向量模型 → cosine重排(alpha=0.3) → Hybrid输出
    符号图: 每个匹配符号附定义文件/导出文件/导入引用关系（P3 符号图引擎）
    静默降级: Embedding失败→保留BM25结果；无token→纯BM25
诊断: get_diagnostics (Monaco markers)
终端: run_command (qz-spawn, 硬超时 2h, stallMs=15min, ★ qwr _qgc 命令屏障保护)
  · 新增 reason 字段: AI 可选填写使用原因，仅 console.log 审计，不拦截
网络: fetch_webpage (curl 绕过 CORS, 15s 超时, ≤8000 chars)
视觉: get_vision_context (★ 并行预分析: 多图片并发视觉模型, Promise.all, 4-12s→3-8s)
  · 生成: fetch IMAGE_GEN_URL → Go → 模型供应商 → SSE 结果流 → curl 下载
  · 分析: fetch VISION_URL → Go → 模型供应商 → SSE 结果流
  · 旧 Python 脚本和 ipc-media.ts 已删除（2026-06-22）
SSE 复用 (★ 零行代码): fetch 新建流复用已有 ReadableStream· 入口: tools-exec-effect.js search_smart → _tryEmbeddingRerank() → embedding-service.js速度倍数），
AI 有充分知情权选择最优工具。system-prompt 新增 🔴 FILE SEARCH PRIORITY 铁律段。

Embedding 混合搜索管道 — BM25 + 语义重排（2026-06-16）
--------------------------------------------------------------
· 入口: tools.js search_smart → _tryEmbeddingRerank() → embedding-service.js
· 管线: query + BM25 top10 snippets → 向量模型 (1024d) → cosine → alpha=0.3 融合h token / API 失败 / 向量异常 → 保留原始 BM25 结果，用户无体感
· 开关: window._EMBEDDING_LOG=true 开启全链路日志（DevTools 实时控制），默认关闭零开销
· 底层: embedding-service.js 双线路(fallback)+归一化+15s超时，暴露 window._qqqEmbedding
· 符号图: P3 引擎为每个匹配符号提供 defines/exports/imported-by 三向引用（见下文符号图引擎节）
· 状态: 代码+编译已就绪，待线上验证

Agent 三态状态机 — idle / sending / stopping（2026-06-16）
--------------------------------------------------------------
· 三态: idle(空闲) → sending(发送中) → stopping(用户点击Stop) → 全部中止后 → idle
· agent._stopState 单一真理源，agent-loop.js finally 块无条件复位 _stopState='idle'
· 停止按钮: Stop 点击 → _stopState='stopping' → 中断流+拒绝后续house → finally 复位
· 防重复: _stopState 已是 stopping 时禁止并发停止操作
· 启动发送: _callGateway 前检查 _stopState==='stopping' → 直接return，零house创建

AI 悬浮预览层
--------------
  表格展开: 两层容器分离（clipBox 固定视口 + wrapper 自然尺寸无 max 约束），
  zoom 缩放不触发 reflow，保持原始列宽和换行。D-pad 平移在 clipBox 内自由移动。
  图片展开: transform translate+scale 统一缩放+拖拽。
  关闭: Esc / 右键 / 点击背景 / 关闭按钮（tooltip 含 i18n）。
  跨窗口协调: 打开 overlay 时广播，其他窗口自动关闭自己的 overlay。

AI 面板增强
-----------
- 楼层模型: 一次回车 = 一栋楼(floor)，楼内多次 API 调用 = 多个房子(house)，
  房内一次工具调用 = 一个房间(room)。计费按楼累计，压缩按楼层保底(≥6层)。
- 持久化: 对话→all.json（唯一真理源），索引→quest.sq3（SQLite 轻量索引），偏好/快照→only.sq3
- 📎 文件附件截断: panel-send.js 内建 50KB 硬帽(MAX_SIZE=50×1024)。
  超过时保留首尾各 20KB，中间截断标记「... [truncated: X bytes] ...」。
  二进制文件(.exe/.png/.zip 等 40+ 扩展名)不读内容，只记录 type/size/mtime。
- E-Flow 文档框架引擎：q1首层楼100%触发，后续楼层10%概率触发。四态机(none→pending→standard/custom)，自动检测并维护每项目的 expert 文档体系。详见 panel-expert.js。
- 项目锁: onlyStore.claimLock() 统一管理（30s 心跳/60s 过期/自动释放），零维护
- 无主文件夹守卫: 三层防线（UI capture管线+函数兜底+持久层铁闸），引导蒙版
- quest 切换防丢: 流式中断回复保存（_fullText+_truncated）+ 互斥锁 + snapshot 副本
- 引导注入: injectGuide() 立即 abort 当前 house → 确认回合(Flash/noTools) → AI 回复 "✅ 已收到引导" → pop 指令 → 继续工作。不中断楼层，确认来自模型。空闲时降级为普通 inject。
- 排队信封: 任何时候可添加，AI 回复完自动消费下一条。暂停/继续/清空控制栏。_debounceSaveQueue 500ms 合并写盘。最大 50 条。
- 楼层归档（2026-06-19 清洁分工）：
    all.json  →  机器用（conversation[] + 计费元数据 + 文件快照），仅存结构化数据，
                houses 不含 reasoning/tools/toolResults，禁 reasoning_content 入 conversation
    all.txt  →  人类读（完整母本：thinking + 工具调用 + 结果 + 回答），建楼期间每5s流式写盘，
               完结时 generateFloorTxt 最终覆盖写入（含 stats + az 行），前台/后台 agent 均覆盖。
    reasoning.txt → 审计用（all.txt 截断工具结果到300字），从 all.txt 直接截断生成，不再依赖 SQLite
    quest.sq3 →  纯元数据索引（question + costWge + _fDir + 时间戳），~200字节/floor，不含正文
- 悬浮层预览：图片（智能2x尺寸+拖拽+滚轮缩放）+ 表格（transform保持长宽比+十字D-pad+初始适配）
- 时钟：分钟(48px)+冒号秒(24px)，动态色随 agent._floorTiming 变化
- 饼图：canvas 112px，懒重绘，数据不到不显示

AI 上下文窗口防护（三层纵深 + 参数集中化，2026-06-10 落地）
--------------------------------------------------------------
背景：模型硬限制 prompt+completion ≤ 1,000,000 tokens，长对话叠加 max_tokens(393K) 超限导致零响应。

参数唯一真理源：content-gateway.js
  CTX_MAX_TOKENS(1,048,565) / COMPRESS_THRESHOLD(200k, 可配置 100-1000k 通过设置面板) / MAX_RESPONSE_TOKENS(393216) / MAX_TOKENS_SAFETY(10k) / CHAR_PER_TOKEN(2.7)
  + COMPACT_FACTS_TOKENS(32k) / COMPACT_NARRATIVE_TOKENS(32k) / COMPACT_ARCHIVE_TOKENS(32k) / ARCHIVE_MAX_CHARS(1M) / COMPACT_DEBUG / COMPACT_THINKING_ENABLED(false)
  MAX_RESPONSE_TOKENS=393216（~394k）→ 全部 6 档 tier 统一读取，Flash/Pro 不区分。
  换模型只改这一处。消费方全部引用，不硬编码。
  按钮显示+压缩阈值第一层 — 上下文压缩引擎（主防线，2026-06-24 加固）：
  · 每间 house 前检查 _lastApiTotalTokens > 用户设置的阈值(默认200k) → 阻塞压缩。每层楼最多压一次。
  · 三专家并行 Promise.all: ①facts(32k) ②narrative(32k,含硬保证+缩水检测) ③archive(32k,仅新楼层)。
  · thinking=disabled：压缩是 JSON 提取，不需要推理（ContentGateway.COMPACT_THINKING_ENABLED=false）。
  · 经 AiGateway 统一出口（chatFetch compact 路径），token 走 key pool，无超时限制。
  · SSE 响应累积解析：遍历所有 data: 行累加 delta.content → 稳健 JSON 三策略提取。
  · 全成功→splice conversation+平移 _floorStartIdx/_floorMeta 索引（防重建时切片为空）。
  · 任一败→快照回滚。KEEP_TARGET=总tokens×0.1动态伸缩。facts 冷文本截断 200k chars。
  · 手动/自动共用 _renderCompressStart/_renderCompressResult（唯一真理渲染机器）。
  · 手动压缩：send "请帮我压缩上下文" 建楼层，az 区显示压缩耗时，不调 AI house。下文（不检索）。
  · 手动触发：左下角按钮，仅限空闲时，≥50k即可。压缩期间发送按钮⏳+disabled。

第二层 — 孤儿tool_calls自愈（持久化根治）：
  · _saveAgentQuestData 修复后按_floor标签重建所有楼层，逐层saveFloor写回sq3。
  · 三条防线: ①请求前(agent-gateway.js _callGateway) ②保存前(_saveAgentQuestData) ③自动保存前(auto-save)。

第三层 — Go 服务端 HTTP/1.1 隔离：
  · dsTransport 独立Transport，ForceAttemptHTTP2:false，每请求独立TCP。
  · 序列化后用 len(bodyBytes)/3 估算兜底帽(≥1024)。
  · 独立 dsTransport(ForceAttemptHTTP2:false) → 每请求独立 TCP，防 RST 级联
  · nginx client_max_body_size 6200m (8444 端口)

显示：左下角按钮优先用 _lastApiTotalTokens（API 精确值），fallback 到本地 chars/3 估算

上下文占用拆解面板（hover 弹出，2026-06-22 v3）:
  原则：只展示可验证的精确数据。单位统一为 tokens（chars/3 估算标记 ≈）。
  顶部标题行：Context Usage: {used}k / {total}k ({pct}%) — used=API total_tokens
  分类行（全英文，零 i18n）:
    · Permanent System Block — msg[0] 的 6 子组件（Vision/Time/System Prompt/Global Rules/Project Rules/Reminder）的 chars/3
    · User × N = XXk — 非 persistent 的 role=user 消息 chars/3
    · Tool Results × N = XXk — role=tool 消息 chars/3（工具调用返回结果）
    · Compressed Summary — ctx.narrative + ctx.facts 的 chars/3
    · Completion (all AI output) — API 精确 accumulatedCompletionTokens（跨所有 house 累计）
    · Available — CTX_MAX_TOKENS − actualTotalUsed（精确）
  数据源优先级：API total_tokens > API prompt_tokens > 本地 chars/3
  每间 house 后自动刷新（updateCtxBtn → renderCtxBreakdown），不依赖 mouseenter
  _accumulatedCompletionTokens: agent 新增字段，每间 house 累加 completion_tokens（跨楼层）
  不再展示 Tool Definitions（固定 ~3.2k tokens，无需用户关注）

all.json 写入系统（2026-06-25 重启恢复加固）
---------------------------------------------
· 唯一增量路径: auto-save（面板级持久 timer，_ensureAutoSave 模块加载即启动，永不停止）。
  _startAutoSave / _stopAutoSave 已彻底删除（面板级单例，不随 quest 切换启停）。
· beforeunload: _saveAllBeforeUnload → _saveAgentFloor(force=true) 无条件全量刷盘。
· 数据流: agent._houses / _a4Snapshots / _lastFloorTimingRecord ─→ _a4BuildCompleteFloorPayload ─→ all.json
· 原子写入: bridge.fs.write → ipc-fs.ts _atomicWrite（tmp+rename），进程崩溃零数据损坏。
· 写后清理: _writeFloorFile 每次写完立即清扫本目录 all.json.tmp.*（_cleanTmpInDir），零累积。
  兜底: _cleanStaleAllJsonTmp 启动时全量扫描（覆盖进程崩溃残留）。
· 重启恢复两层防线（2026-06-25）：
  ① _restoreAgentFromStore 从 allFloors 回填 _houses / _floorCostWge / _lastFloorTimingRecord
     → 重启后 az 区数据完整恢复
  ② _saveAgentFloor 安全网：_houses 为空且 _lastAutoSaveLen !== 0 时拒绝写盘
· 跨面板写保护（2026-06-25）：
  ③ _saveAgentFloor：ag._stopState !== 'sending' 时拒绝 auto-save（非建楼面板禁止写盘）
  ④ generateFloorTxt：同守卫（禁止非建楼面板覆写 all.txt）
  → 三面板独立 agent，只有正在建楼的那个有权写盘。force(=beforeunload) 和
    floorCompletedCleanly(=刚建完) 例外放行。零跨面板覆写。
· 跨面板状态隔离（2026-06-25）：
  _sending 复位加 _activeAgent === _capturedAgent 守卫（后台 agent 不复位前台发送管道）
  _qqqCurrentTrace questId 改走 ag._questId（per-agent，非全局 questActiveId）
· 旧 A4 增量刷盘系统（2026-06-23 已删除）：_a4MarkIncrementalDirty→_a4FlushCompleteFloor ~100行

AI 面板 UI 增强
--------------
- 编辑器字体大小: 菜单行1, +/- 按钮之间, 等宽字体显示当前字号(如13), 纯展示非交互
  窗口 UI 永远锁死 1.00。+/- 和 Ctrl+=/Ctrl+-/Ctrl+0 只改 X 区 editor 和
  Timeline Diff 弹窗 editor 的文字大小（8-48）。
  持久化: shell.js 存 qgs.simple('qqqide') 的 editorFontSize.{root} key → 每项目独立记忆。
  主进程广播 qqqide:zoom:changed（值域 [8-48]），editor.js + diff-window.js 实时 updateOptions。
- q按钮区: 83%原始尺寸, tier-btn 25x25px/10px, sel 12px bold, 上下文血条 53px min-width
- 编辑框永不禁用: streaming 期间 contentEditable 不变, 用户可边看流式回复边写下一封
- 引导 streaming 中可用: 立即 abort+确认回合，AI 即时回复确认
- 排队 streaming 中可用: 随时添加到队列，AI 回复完自动发送
- 上下文血条: ctx-btn, 固定底板渐变绿(#7d967d)->红(#c28888), --ctx-pct CSS变量控制填充截断, 只显示token数(234k), 右侧未填充叠25%黑遮罩, hover减淡
- 无进展终止: agent-loop 内置 stall 计数器（见下方"无进展终止"节）

Write-Aware 约束系统 — T1/T2/T3 三层阶梯 + 写入感知（2026-06-17 终极重构）
----------------------------------------------------------------------------
核心原则：一切约束感知写入操作。对文件 X 的写入使关于 X 的所有旧状态作废。
不改写架构形状，只补上缺失的「写入感知」——同一套代码适用一万年。

三层阶梯分流（agent-loop.js _checkStall, ~100 行）：
  预判: 本轮工具结果全部 dead（[ALREADY READ]/空/Error/No matches）→跳过 T1，交 T3
  T1 工具循环: 同工具+同参数连续 3 间 house → 触发；8 次连续 → 终止楼层
    结果增量检测: 同参数不同结果（相似度 < 70%）→ 有进展 → 重置计数，不进 T1
    跨非 T1 楼层断链: 中间有进展（不同工具）→ _lastT1Fingerprint 清零，不跨楼累积
  T2 搜索停滞: 连续 5/10/15 次纯搜索有结果但不读 → L1温和→L2引导→L3终极（永不终止）
  T3 真正空转: 全部 dead 累积 5 次→警告, 8 次→强制。有进展立即归零

Read 约束（tools-exec.js executeReadFile, ~90 行）：
  全文读→全覆盖标记；分段读→区间合并去重。同范围再读阻 2 次后放行（上下文丢失）。
  硬封顶: 同文件 20 次/楼层。每层楼复位。
  写入感知: edit_file/create_file/delete_file/write_file 成功后，自动重置该文件追踪。
  搜索缓存写入感知: READ 工具（search_text/search_content/find_files）结果缓存，
    写入后自动清除含该路径的缓存 key，防旧结果误导 AI。

写入通知机制（tools-exec.js executeTool, ~17 行）：
  WRITE 成功 → ① 删 _qqqReadFilesThisFloor[path] ② 遍历 _qqqToolCacheThisFloor 失效含路径 key
  零侵入：只对 WRITE 类工具生效，run_command 不感知（不确定影响哪些文件）

502 自动修复 + 降级恢复 (2026-06-11 加固)
----------------------------------------------
  · Worker 502/503 → _gwSwitch(true) 瞬间切 Direct → 重试。两条线都死 → 优雅结束楼层。
  · 铁律：绝不 window.location.reload()。网络耗尽只结束当前楼层，DOM 完整保留。
  · 5 分钟后 _gwTryPrimary() 自动回探 Worker，活了就切回。
  · 断头楼层看门狗：finally 块检测 onDone/onError 均未触发 → 持久化计时数据 → 提示重发。
    不再 reload。重启窗口后 A3 时钟/饼图从 clockTiming 恢复，不归零。

Quest 切换完整性 (2026-06-10 重构，2026-06-21 加固)
----------------
  · 切 quest = 纯 display 开关。零 DOM 操作、零 timer 干预。
  · timer 跑在 agent 上（ag._floorTimerId/ag._floorStartPerf），切 quest 不停 timer。
  · 后台楼层完结后 generateFloorTxt(ag, questId) 全覆盖写入 all.txt（含 stats + az 行），不再区分前台/后台 agent。
  · allTxt 状态全部 per-floor：agent._allTxtPath（路径）、aiDiv._allTxtOpenInEditor（编辑器标志）、aiDiv._a1Block（A1 DOM）。
  · 面板级单例：_allTxtPollTimer / _allTxtBlocked / _sending / streaming。
    ★ _sending / streaming 必须是 panel-state.js 顶层 var（禁经 _activeAgent defineProperty 重定向），
    否则切 quest 时 _activeAgent 变更→旧楼层 _sending 栅栏穿透→同面板并发建楼→旧楼层僵尸。
  · ★ _streaming 持久化顺序：onDone 内 _capturedAgent._streaming = false 必须在 _saveAgentQuestData 之前，否则 all.json 永久记录 _streaming:true。
  · allTxtPath 精确存入 floor payload，不再事后重建。
  · 切回时 card 的 DOM 原样保留，无需 resume/重挂/重建。
  · switchQuest / _unloadQuest 中禁调 stopFloorTimer / completeBuildingFloor。
  · 时钟持久化: floorPayload.clockTiming，_buildFloorDOM 优先读 fData.clockTiming。

流式渲染 — 一次渲染 终身不变 + 增量解析器（2026-06-11 落地）
-----------------------------------------------------------------------
铁律：封顶不重建 DOM。流式阶段 appendChild 的节点即为最终节点，永不 innerHTML 替换。
  · onDone 仅做：去 stream-para 类 + 移除空 _lastParaEl。零 DOM 重建。
  · 引导确认期间：doStreamRender 完全跳过（模型可能输出 XML），
    仅 agent-loop handler 更新绿条标记。确认结束后清空 _buf/_paras 防污染主回复。
  · 引导 Ack XML 清洗：第一个 < 字符处截断（零漏过可能）。

增量解析器（代码围栏感知）：
  · 拆分前扫描 ^``` 行首标记奇偶性。奇数→围栏未闭合→禁止拆分，回退到 _buf 等待闭合。
  · 围栏内 \n\n 是代码内容，不可拆为段落。闭合后整段推入 _paras。
  · 流式半成品代码块：_lastParaEl 用 <pre><code> 实时显示（剥离首行围栏标记）。
  · 拆分边界：\n\n = Markdown 段落边界，围栏是唯一例外，已覆盖。

增量拆分修复（2026-06-23）：
  · 旧问题：onToken 每轮对 _buf 全量 split → 历史段落重复推入 _paras → 重复渲染。
    静态段落用 appendChild 追加出现在 _lastParaEl 下方 → 用户看到 4321 倒序跳动。
  · 修复：_splitCursor 只扫描上次位置后的新增内容 + _doStreamRender 用 insertBefore(_lastParaEl)
    静态段落始终插入实时打字区上方 → 永远 1234 正序。

_lastParaEl 全量 _buf 致前半重复修复（2026-06-24）：
  · 旧问题：onToken 完成段 push 到 _paras 后 _buf 不清（保持累加全文），
    _doStreamRender 用 _rm(_buf) 全量渲染 _lastParaEl → 已完成段落既在独立 div 中又在 _lastParaEl 中→流式时前半重复。
    onDone flush 同理→完结后 DOM 仍有残影。重启恢复走 _buildConversationFlowHtml 不复现。
  · 修复：_doStreamRender + onDone 非围栏路径改为 _buf.slice(_splitCursor) 只取尾部未完成段。
    围栏路径 _buf 已在 onToken 重写为纯围栏内容（splitCursor 跨坐标系失效），仍用全量 _buf。

段缓冲/事件驱动/1fps: 同上。renderMarkdown 列表占位符保护 <ul> 免 <br> <p> 污染。

双层流看门狗（agent-loop _parseSSE）：
· 流看门狗 60s：无任何 SSE 数据 → abort 死连接。
· 产出看门狗 15min：无 delta.content 或 tool_calls → AI 思考死循环 → abort。
    纯 reasoning_content 不重置。
· SSE error 处理（2026-06-15修）：_sseError提到while外层throw，不再被JSON.parse的catch吞掉。
· usage-only响应(无type字段)→返回null→触发正常错误路径。

文本工具调用回生引擎（2026-06-13）
--------------------------------
· 问题：部分模型在 delta.content 中输出工具调用文本（而非原生 delta.tool_calls），旧逻辑剥离丢弃 → 楼层秒完零执行
· 方案：EnvelopeStripper.finalize() 统一解析三种格式 → 汇入 textToolCalls[] → _parseSSE 优先执行
· 覆盖格式：· 覆盖格式：XML invoke 块（<invoke name="x"><parameter ...>）、XML 自闭合 tag、ReAct 格式（Action: ...\nAction Input: {...}）转：文本工具调用被回生为可执行 tool_calls，不再丢失

AI 网关 — Worker 纯代理 + Go 权威计费（2026-06-10 设计，06-10 晚强制落地）
--------------------------------------------------------------------
线路: Client ──线路: Client ────SSE────▶ Worker(cf edge, 纯透传) ────▶ Go ────▶ 推理引擎  (零计费)              (ban查询+自动failover)   (唯一权威扣款)

Worker (cf/ai-gateway.js, ~67行):
  · 禁运查询: fetch CHECK_URL?th=xxx → Go banned 检查，402 直接封
  · 纯透传: fetch(GO_CHAT_URL) 直连 Go 端口 8444，原样回传 SSE 流
  · 自动 failover: primary→fallback 域名切换，零感知
  · 限流废除: 经济账本即唯一限流（ge≤0→禁运），无需并发/速率限制
  · 铁律: Worker 禁任何计费/模型映射/body 改写。只做 banned 检查+透传。

Go (唯一计费权威):
  · POST /v3/ai/chat: 认证 + GKLimit + 直接 ChargeAITurnWithMeta 扣款（单步完成）
  · GET /v3/ai/check: CF Worker banned 查询（公开端点，无需认证）
  · GET /v3/ai/usage: 余额查询
  · POST /v3/ai/vision: 视觉识别 · GET /v3/ai/vision/:id/stream: SSE 结果推送
  · POST /v3/ai/generate-image: 图像生成 · GET /v3/ai/generate-image/:id/stream: SSE 进度
  · 视觉/绘图: goroutine 后台扣费，独立于聊天计费体系

计费体系 — project→quest→floor→house→room
  · Go 为唯一扣款权威，不再有 lodge/flush 双轨计费
  · 删除: POST /v3/ai/lodge、POST /v3/ai/billing/flush、handlers_ai_lodge.go、LODGE_HMAC_SECRET
  · 删除: cf/deploy_go.py、cf/go_deploy.py（旧 lodge 一次性部署脚本）
  · JSON字段: floor_id 不可用 turn_id（命名铁律）

饼图 + 时钟
-----------
- 时钟每秒更新: setInterval 1s, fmtTime Xm:Ys 格式
- 饼图每秒更新: 直接读 agent.- 饼图每秒更新: 直接读 agent._floorTiming (agent-loop 实时累加), 已归因(network橙色/AI绿/tool黄)+未归因(wait灰#888), 零值切片自动跳过不画ming 缓存，四字段全等跳过 drawPie，消灭无效 Canvas 重绘
- hover tooltip: 2x放大(22px字号, 20px圆点), 4色+时间, 固定黑色半透明底

输出截断体系 — 两层语义（单一真理源）
-------------------------------------
SAFETY NET 层 (永远不需要改):
  ghrun spawn.rs:    65536  纯防内存爆炸，非 AI 面限制
  qz-spawn.ts:        65536  命令输出安全网
OUTPUT 面 (改 content-gateway.js 一处，消费方全引用):
  OUTPUT_CAP_DEFAULT   = 200000  AI 视野默认上限（单次工具结果字符数）
  OUTPUT_CAP_MAX       = 800000  maxOutput 参数上限（AI 可传参突破默认值）
  OUTPUT_CAP_FETCH     = 8000    网页抓取文本提取
  OUTPUT_CAP_FETCH_ERR = 500     网页抓取错误信息
  READ_FILE_CAP_BYTES  = 200000  read_file 单次返回字节上限（~195KB，超则截断+分页提示）
  READ_FILE_CAP_KB     = 195     system-prompt.js + tools-defs.js 动态读取，AI 自感知

  tools-defs.js 工具描述 + system-prompt.js 系统提示词全动态引用 KB 值，不硬编码。
  安全网值远高于 AI 面，永不成为短板。
  OUTPUT_CAP_FETCH 等 tool 描述OUTPUT_CAP_FETCH 等 tool 描述动态引用常量，模型自动感知。timer 跑在 agent 上（ag._floorTimerId/ag._floorStartPerf），每 quest 独立，切 quest 不停 timer。
· 工作中: startFloorTimer(aiDiv, ag)· 工作中: startFloorTimer(aiDiv, ag) 初始设绿色 (#859900 AI 活跃色) 活跃段切换: AI 活跃→绿 / Tool 活跃→黄 (#e6b800) / Network 活跃→橙 (#cb4b16) 设停止态（亮色#000 / 暗色#fff）
· 中断/异常: finally 块用 _capturedAgent 属性设停止态，杜绝残留绿色
· 历史楼层重建: _buildFloorDOM 从 fData.clockTiming 直接恢复，回退 questTimings 数组
· 饼图: 初始隐藏，数据到后显示；懒重绘（四字段全等跳过）

A4 文件快照 — 每层楼私有文件变更追踪 (2026-06-13 钩子Q+X重构)
--------------------------------------
· 唯一入口: panel-a4.js 钩子 Q（拦截 executeTool） + editor.js 钩子 X
· 钩子 Q 触发: edit/write/create/delete_file + run_command
· 钩子 Q 流程: 捕获 before → 执行工具 → 捕获 after → _a4PersistToTimeline(both, trace, diff stats)
  trace 格式: q{id}/f{num}/h{num}/r{num}（quest/floor/house/room，agent-loop 埋点）
  ★ floorNum 唯一真理源: ag._currentFloorNum（panel-send.js 创建楼层时分配，panel-floor.js 恢复时从 DB 重建，agent-exec.js 打 trace 时读取）。禁止回退 _ctx.torun_command: ★ 用户可开关（默认关，设置→高级），开启后 → captureChanged 扫描变更 → 噪声过滤(.log/node_modules/__pycache__/VCS/OS垃圾) → 逐文件记录 after（trace 同享）ter（trace 同享）
· 钩子 X 触发: X区 editor blur (auto-save) / Ctrl+S (manual-save)，60s 冷却
  流程: 读上一版本内容 → _a4DiffStats 计算 +N -M → timeline.record(content, source, diff stats)
· 读原始内容: bridge.fs.read（不用 ai.read_file，避免分页头污染）
· 状态归属: agent._a4Snapshots（§36 体系），内存累积(最早before+最新after)
· DOM: aiDiv._a4Block（A4 豆腐块，A2 与 A1 之间）
· UI: 瀑布式文件列表（文件名 + 颜色编码 + +N -M），实时更新
· 点击: _a4OpenDiff → 读内存 snapEntry.beforeBlobHash/afterBlobHash → openDiffWindow（不补记！钩子 Q 已记全）
  历史楼层: _a4OpenHistoricalDiff → 读 floor payload 中 blob_hash → openDiffWindow
· 持久化: 钩子 Q/X 实时写 timeline (SHA256+gzip+SQLite)；floor 完结仅打包内存 blob_hash 入 payload
· 旧文件系统快照 + _a4IncrementalPersist + gzip 编解码等全部删除
· 容量: 512KB/文件上限，超限只记元数据

Git Goods — 第一方 git 操作
----------------------------
· 位置: goods/git/git.js + goods/git/git-ui.html
· X 区 Git tab: status / diff / stage / unstage / commit / log / branch / stash
· A 区面板: git 快速状态预览
· Timeline 已完全迁出至独立 BrowserWindow (server-app/timeline/)
· git.js 不再监听 A4 消息，不再有 timeline tab，只保留 git tab + panel


qh 滚动真理机器 — 接入标准（非独立模块）
--------------------------------------
· 标准文档: do/拓扑/qh滚动真理机器接入标准.md
· 核心铁律: 滚屏与滑轨必须是兄弟，同挂在一个不滚动父容器下
· 接入方式: 按文档复制 ~90 行 JS（滑轨+滑块+同步+hover+拖拽+点击跳转）
· 默认参数: 2px常态/12px展开/距右~10px/亮暗自适应
· 效果清单: 常态细条 + hover变粗贴边 + 滑轨点击跳转 + 拖拽 + 内容自适应
· 完整接入者: AI 视口下拉（ai-viewport.js _wrapScrollContainer）+ AI 面板聊天区（panel-quest-ui.js 末尾）
· qh-scroll.js 已删除，原因: 无法做到对既有 DOM 零侵入的"一行接入"ntainer）+ AI 面板聊天区（panel-quest-ui.js 末尾）
· qh-scroll.js 已删除，原因: 无法做到对既有 DOM 零侵入的"一行接入"