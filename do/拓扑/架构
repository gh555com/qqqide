qqq-shell-v2 架构 — 代码若与此冲突，以本文档为准。
===========================================================
命名规范: Tier1 外宣 qqq IDE / Tier2 编码 qqqide（全小写）
归一完成: 2026-07-03，qqq-app→qqqide 全链路消灭，零旧兼容


三层模型
--------
┌──────────────────────────────────────┐
│ 壳 (shell/)                          │
│ Electron 22.3.27 · Win7+ · frame:false│
│ 入口: bootstrap.js → main.js          │
│ 职责: 窗口 + IPC + bridge + 热更新检查 │
│ 源码: main.ts + 11 子模块 (≤290行/个)  │
│ 产物: shell-out/ (esbuild ~35ms)      │
│ 热更: bootstrap 启动时原子替换 shell-out/ │
└──────────┬───────────────────────────┘
           │ spawn at boot (×2)
┌──────────▼───────────────────────────┐
│ 引擎层 (Rust ×2)                      │
│ q_win_x64.exe — IO引擎(fs/SQLite/IPC) │
│ ghrun.exe     — 进程管理(qz)（LSP守护已冻结）│
│ 兜底: qz-spawn 两层 (ghrun→Node)      │
│ 新增: Windows Job Object 内存硬限制    │
└──────────┬───────────────────────────┘
           │ bridge IPC (preload暴露)
┌──────────▼───────────────────────────┐
│ 载荷 (server-app/)                    │
│ 纯前端: HTML+CSS+JS · Monaco 0.34.1   │
│ 热更: Deploy→服务器→客户端自动拉取     │
└──────────────────────────────────────┘


双轨自动更新系统 — shell 冷更 + webapp 热更
---------------------------------------------------------------------
· 壳层冷更: bootstrap.js 启动前检查 staging/shell-out-next/ → 原子替换 shell-out/
  触发: bootSequence→checkAndDownloadShellUpdate 自动下载到 Data/Cache/staging/
· 载荷热更: bootSequence 完成后 20s → backgroundCheckWebappUpdate → 下载 server-app.tar.gz
  → 解压到 Data/webapp-staging/ → 下次启动 ensureLocalWebapp 原子替换 Data/webapp/
· 按钮更新: update-ui.js 手动触发 upgradeShell() / apply()（同一条管线）
· 版本跟踪: shell-version / webapp-version 两文件独立持久化
· 版本比较: version.ts decideUpdate 语义比较，防降级


持久化层级 — global → project → quest → floor → house → room
------------------------------------------------------------------------
· global (qgs): 跨项目（最近文件夹/主题/语言/剪切板）
· project (qgs.project / qg): 项目内（editor tabs/偏好/lock）
· quest (quest.sq3): 一次对话会话
· floor: 一次用户回车（可能含多次 API 调用）
· house: 一次 API 调用（可能含多次 tool call）
· room: 一次工具调用
· 铁律: 变量/字段命名遵循此层级，禁同义词(turn/session/round)。JSON: floor_id 非 turn_id


qz spawn 管线 — 两层 + 四重防御
-------------------------------------------------------
· 管线: ghrun (Rust, 主力) → Node (child_process, 兜底)
· runner.py 调用点已从 qz-spawn.ts 删除，仅保留代码于 engines/

四重防御（针对 40GB PowerShell 内存爆炸）:
  ① 硬超时 2h: SYSTEM_MAX_TIMEOUT=7200000，入口强制 cap
  ② 内存守卫: nodeTier 专属，异步 execFile（非阻塞），首检 5s→后续 15s
     ghrunTier 跳过 JS 守卫（Rust Job Object 已覆盖）。execSync→execFile 修复 UI 冻结 bug
  ③ ghrun 原生 Job Object: spawn.rs memLimitMb，Windows CreateJobObjectW / Linux+mac setrlimit(RLIMIT_AS)
  ④ 失速看门狗: stallMs=15min 无输出→杀；output cap=64KB 防 IPC 洪水
· ghrun 反卡死: deadline watchdog + stall watchdog (100ms) + kernel 级进程组隔离
  Windows: CREATE_NEW_PROCESS_GROUP + taskkill /F /T
  POSIX:   process_group(0) → killpg (SIGTERM→2s→SIGKILL)
· guardMs 修复: 恒为 timeout+10s（原 timeout=0→guard=24h 形同虚设）
· 内存守卫 killReason: 'mem-guard'（与 deadline/stall/disconnect 并列）
· ghrun CI: gh555com/qqq-IDE → build-ghrun2.yml → 三端编译


AI 网关双线路 + 故障切换引擎
---------------------------------------------
· 双线路（2026-06-29 更新）:
  主: https://direct-cn.gh555.com (直连 CN 47.105.67.51, 中国 ~50ms, 绕过 Cloudflare 延迟)
  备: https://cnk.gh555.com (Cloudflare CDN → CN nginx → WireGuard → US Go, 全球 DDoS 防护)
· 旧 US 直连 direct.gh555.com:8444 已弃用 — US 防火墙仅允许 CN 通过 WireGuard 访问
· ★ 全路由覆盖（6 个 AI 端点，2026-06-26）
  chat / vision / vision stream / image-gen / image-gen stream / embedding / search
  所有端点经 AiGateway._URLS 双 URL 声明 + _postJsonWithFailover/_pollStream 自动切换
· 声明位置: ai-gateway.js _URLS（唯一真理源，禁 system-prompt.js 重复声明）
· CF Worker (gaea/cf/ai-gateway.js): 全路由透传，禁业务逻辑，禁计费
· 切换引擎: agent-loop.js _callGateway 内建 retry+switch 管线
  - 同 URL 最多重试 3 次（MAX_RETRIES=3）
  - 重试耗尽→切线路，单次最多 2 次（MAX_LINE_SWITCHES=2 防乒乓）
  - HTTP/2 连接级错误 优先切线路
  - 502/503 前半段重试同 URL，后半段切线路
  - 400/422（孤儿 tool_calls）跳过重试→auto-repair; 402（key 欠费）直接切线路; AbortError 切线路
· 延迟报错: _callGateway 不直接调 onError，设 _lastGatewayMessage 后返回 null
  → auto-repair 先尝试修复 → 成功则用户无感 → 失败才报错
· 跨面板协同: panel-state.js 监听兄弟面板 qqq-gw-status → 自动调整 _gwFallbackAt

服务端甲壳系统提示词（2026-06-27）
---------------------------------------------
· 唯一真理源: gaea/guard/system-prompt.txt
· Go 加载: gaea/internal/guard/guard.go sync.Once 读取预序列化，内存缓存，零每请求开销
· 注入点: handlers_ai_chat.go §6 — sanitizeMessages 之后 prepend 到 messages[0]
· 客户端: agent-loop.js _refreshRules() 不再发送 SYSTEM_PROMPT
· 甲壳固定 → AI prefix cache 100% 命中，费用等价
· 安全: 客户端网络包中不再出现甲壳文本
· 部署: 改 system-prompt.txt 后只需 ky.py deploy go（无需重新编译）

双 Key 隔离 + JWT 解耦（2026-06-23）
---------------------------------------------
· 两把 API key 隔离于服务器 .env（客户端零密钥）: API_KEY_MAIN / API_KEY_FALLBACK
· X-Key-Slot header: 不传→PickAIKey()(主线) / 传1→PickFallbackAIKey()(备用)
· JWT 仅管认证（我是谁+ge扣费），X-Key-Slot 仅管路由（走哪把 key），完全解耦
· SSE 402（服务端 key 欠费）→ 自动切线路 → 另一把 key → 用户无感
  HTTP 402（用户 ge 不足）→ 直接终止，不浪费线路切换
· Redis 封禁检查 — 已彻底移除（2026-06-23）。旧 CHECK_URL banned:th 永远空。
  封禁唯一真理源: middleware_ban.go 查 PostgreSQL doers.banned_until
· CF 来源强制: EnforceCFOrigin 中间件，仅生产环境 /v3/ai/chat 拦截（无 CF-Ray→403）
  开发环境自动跳过。公共端点（/__lbprobe、/api/time、/px）不受影响
· 计费幂等 (2026-07-01 修): INSERT INTO idempotency_keys ON CONFLICT DO NOTHING
  同 house 重复触发→仅扣费一次。key 格式: ai_turn:{doerID}:{floorID}:{bodySHA256前4hex}
  旧缺陷: 用 floor_id 做 key→同楼层的后续 house 全被跳过（已修复）

IDE 登录认证 — 纯内存 JWT + qqqide:// 协议推送 + safeStorage 持久化（2026-06-29）
---------------------------------------------
· 流程: 浏览器 gh555.com/login → ide-finalize 生成 JWT(DoerID+DeviceID,HS256,10y) → qqqide:// 协议推送
· 主通道: 浏览器端 window.location → qqqide://auth?token=JWT&phone=xxx → Electron 主进程拦截 → IPC→渲染层
· 持久化: safeStorage OS 级加密存 Data/alphal/auth.enc。启动自动恢复，主动登出才删除。重启不掉线
· 服务端: authenticateIDEToken 解析 JWT→查 devices 表验证设备存活
· 兜底: 协议不可用（系统未注册）时自动退化为轮询（最长 120s）
· 已删除: UUID token、auth.json 磁盘读写、_loadAuthToken/_saveAuthToken 全部旧代码
· 铁律: 单一路径(qqqide://push→内存→Bearer)，轮询仅兜底

LV 赛季系统 + 免费预算引擎（2026-06-30，2026-07-03 客户端重构）
-----------------------------------------------------------------
· 赛季: UTC 周，周一00:00起。赛季 ID 格式 "2026-W27"
· LV 升级: 每 10 ge 非免费消费 = 1 level。精度 NUMERIC(8,4)
· ★ 免费冲正不计入 LV（仅 ChargeAITurnWithMetaAndLv 调 AddConsumed）
· ★ 部分免费: Go 先 ClaimFreeBudget → 剩余→ChargeAITurnWithMetaAndLv，LV 只记真实部分
· 免费时段随机预算制:
    2h: 0.1%[1,1000] | 1%[1,100] | 33%[1,13] | ~66%[3,7]
    24h: 0.1%[1,1000] | 1%[1,100] | 33%[2,26] | ~66%[6,14]
· 上赛季等级向下取整 → 加成。窗口过期→未用完作废
· 原子性: chargeAITurnOnceMaybeLv 同一 serializable 事务；ClaimFreeBudget FOR UPDATE + INSERT

客户端 LV 3 层血条 + 事件驱动（2026-07-03 终极架构，login.js）
-----------------------------------------------------------------
· 不再轮询。SSE billing → _processBillingEvent → postMessage(qqq-lv-tick) → 驱动三条管线:
  ① _lvAccWge 本地累加 → _lvAnimate → rAF 10s 赛贝尔追赶（t²·⁵，前慢后快，保证 ≥1px 间隙）
  ② _balanceGe 乐观扣减 → 60s _fetchBalance 真理覆盖
  ③ _fetchLv → /api/qqq/lv → 2s 静默后纠正超标（部分免费时 billing ge_cost 虚高）
· 3 层血条: #555 轨道 → #ffffdd 中层(瞬间) → #c4b187 顶层(10s 追) / #8a6410(追赶中)
· 每次 billing 触发粒子爆发 + 光环扩散 + 升级时音频（assets/lv-up.mp3 / 里程碑 lv-up-milestone.mp3）
· 启动立即显示 LV 区（_lvShow），零等待服务器数据
· 免费预算也走事件驱动（billing free_window=true → fetchFreeBudget()）
· hover tooltip: 2026_27W0: Consolas #fbe9bc/80%；金额 #b58900 44px（服务器真理核心）
· 排行榜: 左=历史总排行25名 / 右=上赛季排行25名
· API: /api/qqq/lv /api/qqq/free-budget /api/qqq/leaderboard

Nginx 备用线路铁律 (US 已隐身, 2026-06-29)
---------------------------------------------
· direct.gh555.com:8444 已弃用 — US 防火墙阻止所有外部直连，仅 CN→US WireGuard 可达
· CN nginx 443 接管 AI 代理: /api/v3/ai/* → http://10.0.0.1:8080 (WireGuard→US Go)
· port 8444 纯 HTTP/1.1（listen 8444 ssl; 禁 http2）— 保留用于内部 WireGuard 访问
· 原因: Nginx HTTP/2 ↔ Go HTTP/1.1 SSE 存在协议转换 bug → ERR_HTTP2_PROTOCOL_ERROR
· sites-enabled/ 只保留 gh555 一个文件（include * 会匹配 .bak）
· 验证: openssl s_client -connect :8444 -alpn h2 → "No ALPN negotiated"

ContentGateway 超时常量（唯一真理源）
---------------------------------------------
· FETCH_DEADLINE_PRIMARY_MS  = 180000 (180s)
· FETCH_DEADLINE_FALLBACK_MS = 98000  (98s, CF 100s 留 2s)
· STREAM_WATCHDOG_MS         = 180000 (180s, 深度推理)
· AI_OUTPUT_WATCHDOG_MS      = 900000 (15min, AI 无产出→死循环)
· 改超时只改 content-gateway.js 一处。

孤儿 tool_calls 自愈系统
---------------------------------------------
· 双向扫描 conversation（200 轮）: ①请求前预检 ②楼层保存前修复 ③自动保存前修复
· 恢复阶段循环修复至干净（最多 10 轮 × 200 次 = 2000 次扫描）
· 成功后重置 _floorOnErrorCalled
· Go 服务端 sanitizeMessages 双向校验已同步


gaea goods 协议
-----------------
A 区宿主: core/gaea-host.js (完整 panel/tabs/services/commands/provides/uses)
注册接口: window.qqqGaea.register({ id, title, ... })
已注册: rage / wysiwyg / navigator / paste-image / file-explorer / search / git

搜索引擎 — ripgrep 单引擎
---------------------------------------------
· 引擎: ripgrep 14.1.1 (Rust · mmap · SIMD · PCRE2 JIT)
· 入口: shell/ipc-search.ts spawn rg.exe --json → 流式解析
· 查找: _findRipgrep() 三路 (userData/components/ → resourcesPath/ → engines/ripgrep/ → PATH)
· 打包: package.json build.files 含 engines/**, 打包前 components.py ensure ripgrep
· UI: goods/search/ (search.js + search-ui.html X区标签), ai-viewport.js 🔍 按钮

Monaco Worker 加载
------------------
· 基础 worker (workerMain.js): 自带 AMD loader, new Worker('qqq-asset://...') 直接加载
· 语言 worker: 裸 define(), 走 blob 拼接
· 依赖补全: convert_monaco_esm.py ESM→AMD (796 文件 → cache/monaco-deps/vs/)
· qqq-asset:// 协议: 先查 node_modules/min/vs/ → fallback cache/monaco-deps/vs/

组件系统 — 服务器驱动，全自动（2026-07-04 重构）
------------------------------------------------------------------------
· ★ 唯一真理源: engines/manifest.json（server-driven，改一行客户端自动重组）
· 加载器: shell/component-checker.ts（启动时读 manifest → 检查 → 下载 → 验证 → 目录迁移）
· 持久化: engines/.versions.json（per-component: version + install_to + verified_at）
· 下载日志: engines/.downloads.json（成败记录 + 5min 冷却，成功则永不再下除非版本升级）
· 原子写: 全部 JSON 走 tmp+rename，断电不丢。manifest 内存缓存，启动后零 IO
· 验证: spawnSync 直调二进制 + verify_args（禁 shell:true，防注入+误判）

等级分类:
  rank0 — IDE 自动管理。缺了静默后台下载，不阻塞启动。
          包含: ripgrep*, python。*bundled=true(绿色包自带,启动跳过)
  rank1 — 用户手动触发。不自动检查。
          包含: ffmpeg / yt-dlp / git

下载: 每个组件声明 srcs[]（URL 列表，顺序尝试）。
  ① CDN(gh555.com) → ② npm mirror → ③ python.org/github 官方

目录迁移 — 服务器改 install_to 字段→客户端物理重组目录:
  · _ensureOne 比较 .versions.json 旧 install_to 与 manifest 新值
  · 不一致→fs.rename(oldDir→newDir) 原子迁移；新目录已存在→旧内容合并移入
  · 效果: 服务器改一行 JSON，所有已部署客户端自动重组，不重新下载

Python: rank0，不阻塞启动。py-broker 启动时 getComponentBin('python')→已安装→启动，
  未安装→跳过。零 fallback 到系统 Python。唯一入口 getComponentBin。

查询: getComponentBin(name)/getComponentDir(name) 都是从 manifest 读路径，
  不硬编码。服务器改 install_to 所有调用处自动跟随。

LSP 已冻结 → 三级语法检查:
    L1 自动（零 ge）: 每次 edit/write/create 后 node --check / py_compile / JSON.parse
    L2 手动（~50 ge）: AI 调用 get_diagnostics 工具
    L3 全仓（~200 ge）: AI 重大重构后跑 run_command


目录骨架
--------
qqq-shell-v2/
├── shell/             壳层TS（main.ts + component-checker + 12 子模块）
├── server-app/        载荷层(热更)
│   ├── core/          IDE 平台 (shell/theme/editor/qoast/state-sdk/gaea-host/tab-manager/...)
│   ├── locales/       13 语言 (zh.json 唯一真理源)
│   ├── goods/         goods (rage+navigator+search+git+...)
│   └── ai-panel/      AI面板（29 个 JS 模块化拆分）
├── engines/           二进制组件(rank0:ripgrep+python / rank1:ffmpeg+yt-dlp+git) + manifest.json(服务器驱动)
├── op/                运维脚本
├── do/拓扑/           架构文档

构建与打包
-----------------------------------
· 开发: qide-shell.py 按 Q → esbuild build → dev-server :8090 + watch + electron
· 编译: shell/*.ts → esbuild → shell-out/（~35ms）
· 打包: npm run pack:win-x64 → dist-pack/qqq-shell-win-x64.zip
· ★ gh555.com/ = npm run pack 中间产物，已在 .gitignore，AI 禁止编辑其下任何文件
· 绿色包 = 最终 zip，pack.js 打包后自动 rm -rf unpacked 中间目录

零配置 + URL 路径归一 (2026-07-01)
-----------------------------------
· config.json 彻底删除。PRODUCTION_URL 硬编码于 shell/boot.ts: https://gh555.com/qqqide/
· 仅 env QQQIDE_URL / CLI --url= 可覆盖。无任何用户可见配置文件。
· 旧 /qqq-app/ 全链路消灭。nginx root /opt/dgs/web/qqqide/。零兼容代码。


UI 布局 — A/X/AI + 左右翼（三面板）
-------------------------------------------------------------------------------
· 中区 .qqq-middle: A区 + X区 + 中AI(389px)
· A区 #qqq-a-zone: gaea goods 宿主
· X区 #qqq-x-zone: 编辑器 + tab groups + output
· 中AI #qqq-ai-zone: 中面板(panel=1)，始终可见
· 左翼 #qqq-wing-left: 左面板(panel=0)，灯泡开关，默认关闭
· 右翼 #qqq-wing-right: 右面板(panel=2)，灯泡开关，默认关闭
· 灯泡 #qqq-bulb-1/2: 菜单行2 红色圆点 toggle

菜单栏 — 2026-06-25
----------------------------------------------------------
· 顶部两菜单: "qqq IDE" + "的梦gaea"（无 i18n）
· "开新窗口": min-width:210px，hover ▶ → 延迟 200ms 弹出最近主文件夹下拉→新窗口 ?restore=1

窗口快照系统 (win_snap:{normalizedPath} in qgs global) — 2026-06-25
------------------------------------------------------------------------
· 保存: beforeunload/file.exit/file.newWindow/从最近列表打开前
· 内容: mainFolder+auxFolders[]+editorTabs+editorPositions+layout+theme
· AI 面板状态不重复存储——onlyStore 已完整记录，新窗口 initQuests() 自动恢复
· 还原: tab-manager._restoreTabsFromSnapshot → qqqPendingEditorPositions → openInPane 消费
· 锁: ipc-misc.ts 窗口创建前检查 project lock


Card Queue — 终极 AI 面板架构（2026-06 重构，2026-06-10 加固）
-------------------------------------------------------------------
· 一张 quest = 一张 Card（完整 DOM 子树），创建后永不 innerHTML 清空
· 切换 quest = 纯 CSS display 显隐，零 DOM 销毁，零竞态
· timer 跑在 agent 上，切 quest 不停 timer（§36）
· 每张 Card: 最多 16 层已封顶 + 1 层在建（FLOOR_CAP_CAPPED=16 / BUILDING=1）
· 超限→最老楼层 DOM remove()。Card Pool 上限 10 张，LRU 驱逐
· 搜索: 同词高亮 + 滚动条标记叠层（暗红 5px 滚动条 + 青绿/黄色刻度线）
· 滚动: 上滚立即停，下滚到底恢复自动跟；scrollTop = scrollHeight 无 scrollIntoView
· 楼层 az 区（从下到上）:
    A3 时钟行 — 左=时钟+饼图，右=GE 成本
    A1 楼层统计 — Floor/House/Room 计数 + FILE/ROW 变更 + 审计/翻译按钮
    A4 文件快照块 — 实时文件变更瀑布列表，点击开 diff
    A2 财宝块 — AI 挖掘的 treasures，无财宝时隐藏
· aq 区 — 用户粉色气泡下方、AI 回复上方，垂直接缝指示层。元素命名 aq1 / aq2 / … 自上而下：
    aq1 — AI 等级 + 启动时间指示器（如 "A6 start in 2026-06-30 14:05:08"）
    aq2 — 预留
  - 建楼时: onToken 首 token 抵达 → 插入 aq1 → buffer chunk → rAF 延迟一帧渲染 → aq1 先上屏 (panel-send.js)
  - 重载时: _buildFloorDOM 从盘面 aiStartTime/tierLabel 渲染 aq1 (card-pool.js)
  - CSS: .msg-tier-indicator 浅色小字，无特殊定位
· A2 铁律: 始终在 A1 上方（insertBefore(a1Block) 无条件）
· A1 铁律: 始终在 A3 上方（insertBefore(clockBlock) 无条件）
· 财宝去重: 每条 treasure 以 text 前 80 字符为 key，全局只注入一次
· A1 行2 持久化: floorPayload.fileStats 入 SQLite，重启不归零
· ★ agentPool 已上移父窗口 parent.__qqq_agentPool（2026-06-29）
  → 三面板共享单一 agent 实例，根治多副本写盘竞态
· 配置常量位于 card-pool.js 头部，改一处全生效

q2 单行 + hover 展开
-----------------------------------------
· 招牌行: 有 quest → "q{n}.{title}"；无 quest → "~ New quest ~"（斜体灰）
· hover 展开→虚线框 + 左金边+渐变+外发光+悬浮层流光
· 下拉: 2 行弹性高度、默认 20 条、模糊子序列匹配；搜索+新建整合进下拉头部
· 三面板各自独立持久化: ai.uiStates.{panelId}
· 快捷键（焦点 + 非编辑区）: 1/2 上下滚 35%视口、q/w 前后楼层跳转
· 统一高亮色: #fdf6e3（暗 #3f3830）。一个 quest 同时只能在一个面板打开

彗星电子钟 — 跨面板建楼计时器（2026-07-05 修复）
-------------------------------------------------------------------
· 中央真理: 读 parent.__qqq_agentPool（共享 agent 实例），200ms 滴答
· 豆腐块（招牌行）: 仅当本面板 questActiveId 对应 agent 正在 sending 时显示时钟
  → 不跨面板串号：左面板(q1)不会因 q2 盖楼而显示时钟
· 下拉列表（hover）: 每个 quest 独立插入时钟，data-qid 绑定各自 questId
  → _tickCometClocks 读 data-qid → pool[qid]._floorStartPerf → 各算各的秒数
· _insertCometClock(prefixEl, qid): 如已存在同 qid 时钟→跳过；不同 qid→先 remove 再重建
  → 防静默覆盖 data-qid 导致"q2 显示 q81 的计时器"
· _tickCometClocks: agent 不存在或非 sending → 隐藏时钟（防 quest 删除后残影不消）
· 位置: panel-clock.js（_insertCometClock / _removeCometClock / _tickCometClocks / _ensureCometClockTicking）


持久化 — qgs + qgs.simple + qg + qgs.project + onlyStore + qgf 六入口
------------------------------------------------------------------------
  window.qgs('my-ns')                → SQLite 全局（云同步）
  window.qgs.simple('my-ns')         → 同 qgs 但零配置
  window.qg(rootDir, 'my-ns')        → FS 项目元信息（qqq/qg/ 原子写）
  window.qgs.project(dbPath, ns)     → SQLite 项目级（quest.sq3）
  onlyStore (only-store.js)          → SQLite 项目级偏好（only.sq3）
  window.qgf(rootDir, 'my-ns')       → FS 项目级 KV（qqq/qgf/ 预留）

背后引擎:
  qgs/qgs.project (state-sqlite.ts): sql.js WASM → 内存SQLite → 批量落盘
    全局: Data/alphal/global.sq3。项目: qqq/alphal/quest.sq3
  qg/qgf (shell/qg.ts ← tmp+rename 原子写): JSON/brotli/NDJSON
  onlyStore: 内存缓存+自动批量刷盘（500ms空闲→3s强制→beforeunload兜底），物理: qqq/alphal/only.sq3

何时用哪个: 全局跨项目→qgs。零配置→qgs.simple。项目元信息→qg。AI 对话→qgs.project。偏好→onlyStore
★ 渲染层不要直接用 bridge.state.* / bridge.fs 绕过入口


家目录 — 项目级 ({项目}/qqq/) 与程序级 ({程序}/Data/alphal/)
-----------------------------------------------------------------------
  项目级: {project_root}/qqq/（纯项目数据，跟项目走）
  程序级: {appRoot}/Data/alphal/（零 C 盘写入，绿色包解压即在）
  ★ 项目偏好 → {项目}/qqq/alphal/only.sq3；全局共享 → {程序}/Data/alphal/global.sq3

  {项目}/qqq/
    ├── .lock              30s心跳/60s过期文件锁
    └── alphal/
        ├── quest.sq3         AI 对话索引 SQLite（~200B/层）
        ├── only.sq3          项目偏好 SQLite
        ├── rule/project.txt  E-Flow custom 规则
        └── expert/           E-Flow standard 文档框架
  qqq/quests/             AI 对话正文（FS）
    ├── _index.json       quest 索引（跨面板共享、30s宽限期）
    └── q{n}.{title}/
        └── f{n}.{question}/
            ├── all.json  机器用（conversation + 计费 + blob_hash）
            ├── all.txt   人类读（完整母本）
            └── reasoning.txt  审计用（all.txt 截断工具结果到300字）
  qqq/timeline/           文件版本时间线
    ├── blobs/{sha256[:2]}/{sha256}.gz  内容不可变（SHA256+gzip）
    ├── timeline.wal       NDJSON 增量日志（≤99行）
    ├── timeline.db        SQLite 全量快照（每100条压缩）
    └── timeline.db.bak    备份（损坏自动恢复）


i18n — 唯一真理国际化机器
---------------------------
· 语言: server-app/locales/{lang}.json (13 种)，zh.json 唯一真理源
· 前端: data-i18n / data-i18n-placeholder / data-i18n-title, JS: _i(key, fallback)
· 特殊: contenteditable div 用 data-i18n-placeholder-attr
· 翻译机器: op/qky.py (PySide6 GUI, 差异翻译, 全并发)
· 运行时: core/i18n.js (autoload, detectLang → loadLang → updateDom)
· 英语兜底: en.json 编译进 _EN_BUILTIN 常量，离线永不显示 key
· AI iframe 桥接: translateUi() 调 parent.window.i18n.updateDom(document), 监听 message('qqq-lang-change')


IPC 管线 + 主进程安全
------------------------------
· 主进程 handlers: bridge.ai.* (8个工具 + hover)
  search_text/find_files/list_files → 主进程 fs (零 IPC 序列化)
  read/edit/create/delete/write_file → 主进程 fs (1 IPC)
· 渲染层 fallback: 优先走 bridge.ai.*，失败回退 fs 直接调用
· engineHost 裁剪: Rust 引擎仅 ping，fs 操作已移除 engineHost 中转
· read_file 主进程 50MB 守卫: stat()→超限返回 paginate 提示
· edit_file 三级匹配引擎: L1精确/L1b CRLF归一化/L2空白归一化/L3逐行 — 主进程+渲染层双份
· 编译: shell/main.ts → esbuild → shell-out/main.js (215KB, 47ms)
· 铁律 §20: 禁编译产物混入源码目录，改 *.ts 后跑 npm run build
· 主进程安全: 所有 webContents.send 前检查 isDestroyed()
  sync:broadcast / project-path 推送 / state:changed 均已加固
  global uncaughtException 拦截 Object has been destroyed


工作空间（Workspace）— 多项目隔离
-----------------------------------------------------
· _workspaceRoot = 主文件夹路径（normalized），同一窗口同一时刻一个
· bindMainProject(): 同 root 跳过 | 不同 → _teardown → _init
· _teardown: 保存快照 → 释放 quest 所有权 → 销毁 Card Pool → 清空 agentPool → 清理 DOM
· _init: 获取锁 → questStore.setProjectRoot → 新建 Card Pool → 迁移 alphal → initQuests
· IPC 隔离: _syncChannel() = 'qqq-sync:' + sanitize(_workspaceRoot)
  _broadcast 和 handleSyncMessage 均走 scoped channel，不同主文件夹物理隔离


跨面板通信 — IPC sync 桥
---------------------------------------------
· _broadcast 走 parent.qqqideBridge.sync.broadcast → 主进程 relay 到所有窗口
· 主进程 sync:broadcast 发到所有 webContents（含发送者），否则同窗口三面板无法互斥
· _handleSyncMessage 首行过滤 msg.windowId === _windowId 跳过自身广播
· _windowId 必须含 _panelId（sessionStorage key 独立），否则所有权系统失效

qwr 机器 — 全局唯一真理写机器
-------------------------------
位置: shell/main.ts。5层防御:
  ① per-file 串行队列 (_qw Map<path, Promise链>): 同文件排队，不同文件并行
  ② 全局命令屏障 (_co/_ac 双向门): 命令等写完成才执行，写出等命令完成才写
  ③ edit_file CAS 重读: 队列内重新读文件匹配，不依赖 AI 旧快照
  ④ 外部修改检测: read_file 记录 {mtime,size}，write_file 对比不匹配则拒绝
  ⑤ 快照自更新: edit/create→更新快照，delete→清除快照
命令路径全入 _qgc: qz:spawn / ghrun:exec / engine:invoke(spawn) 3入口统归管
涉及: edit_file / write_file / create_file / delete_file / run_command (5个)
保证: 零脏写、零丢失更新、零死锁、零误报


Timeline 文件版本时间线
--------------------------------------------------------------
· 独立 BrowserWindow + Monaco Diff Editor。旧系统全部删除。
· 存储: {projectRoot}/qqq/timeline/
  blobs/{sha256[:2]}/{sha256}.gz — 内容不可变（SHA256 行尾归一化 CRLF/LF/CR→\n）
  timeline.wal — NDJSON 增量日志（~100B/条，≤99 行）
  timeline.db — SQLite 全量快照（每 100 条压缩，.bak 同步备份）
  ★ file_seq: per-file 自增序号，AI 可用 WHERE file_seq=N 直查新快照
  ★ 恢复: 加载 .db（损坏→.bak→空库）→ 回放 .wal（≤99 行）→ 完整

触发（5 种时机 3 种 source）:
  ① 钩子 Q — AI写工具 → source='q'，不走冷却
  ② 钩子 Q — run_command（用户可开关，默认关）→ source='run-command'
  ③ 钩子 X — editor blur/Ctrl+S → source='editx'
  ④ Diff窗口编辑保存 → source='diff-edit'
  ★ editx + diff-edit 共享主进程真理机: 100s 冷却 + SHA256 去重

Diff窗口: 独立 BrowserWindow，一个文件单例（Map），Monaco 0.34.1 side-by-side
  底部三按钮: ☐仅差异 | 编辑/取消编辑 | ✕关闭
  编辑模式: 右侧可编辑→保存触发快照(source='diff-edit', 100s冷却+去重)
  防并发: _renderToken 竞态令牌 + model dispose + 15s 超时
  偏好: 仅差异模式项目级持久化(only.sq3:qqq.timeline.diffOnly)

跨十年保证: SHA256 blob 不可变 + .wal + .bak 双备份 + 永不删


AI 工具 (16个)
------------------------------------------------------------
文件: read_file, edit_file, create_file, delete_file, write_file (主进程 1 IPC, qwr保护)
搜索: search_text (regex), search_content (多关键词OR), find_files (glob), list_files,
      search_smart (BM25语义+Embedding混合重排, 三路并行)
  · search_smart: BM25倒排+符号匹配+Embedding语义重排(alpha=0.3)
    静默降级: Embedding失败→保留BM25结果
诊断: get_diagnostics (Monaco markers)
终端: run_command (硬超时 2h, stallMs=15min, qwr _qgc 命令屏障保护)
  · ssh/sshJump 参数: 远端命令自然书写，base64编码消除三层引号地狱。shell:false arg数组
网络: fetch_webpage (curl, 15s, ≤8000 chars)
视觉: get_vision_context (并行预分析: 多图片并发, Promise.all)
  · 生成/分析: IMAGE_GEN_URL/VISION_URL → Go → 模型供应商 → SSE 流。旧 Python 脚本已删除
Embedding: BM25+语义混合搜索，双线路(fallback)+归一化+15s超时


零摩擦原则（2026-07-05）
------------------------
· IDE 职责: 消除工具摩擦，不教 AI 思考
· AI 想做什么→工具干净执行一次成功。不搞「答案已找到」提示、不搞语义循环检测
· 反面: SSH 三层引号地狱让一次 DB 查询失败 28 次——工具添堵，不是 AI 蠢
· 正确: run_command 加 ssh 参数→base64 编码→零转义，AI 写远端命令和本地一样自然
· 判断标准: 新功能是在消除摩擦还是在教 AI 怎么做事？后者不做


Agent 状态机 + 发送管线（2026-06-29 单一真相源重构 v2）
--------------------------------------------------------------
· 状态机: idle(空闲) → sending(发送中) → stopping/fatal。agent._stopState 单一真理源。
· setStopState(newState) — 带状态转移校验表：
    idle → [sending, fatal]
    sending → [stopping, fatal, idle]
    stopping → [idle, fatal]
    fatal → [sending]（唯一出口："继续任务"）
  非法转移不阻断但 console.warn。agent-loop.js finally 块无条件复位。
· _sending: 纯派生 getter（defineProperty 仅 getter，零 setter，零存储）
  真理：!!(_activeAgent && _activeAgent._stopState === 'sending' && !_activeAgent._floorCompletedCleanly)
  _floorCompletedCleanly 守卫：防后台已完结但 stopState 未复位的 agent 假报 sending（按钮变 Stop）
· _capturedAgent / _capturedQuestId: 已消灭。一切 agent 引用直读 _activeAgent 或闭包捕获。
· sendMessage 管线重构（v2）：
  ① gate checks（stopState/stopping/fatal/recovery/主项目）— 纯读操作
  ② 草稿升级（_isDraft → questStore.create → agent 创建 → 改名落盘 → card 迁移）
  ③ null guard（此后 _activeAgent 必定非 null）
  ④ invariant check（_activeAgent 在 agentPool 中匹配 questActiveId）
  ⑤ setStopState('sending') — 同步锁，防同一面板并发
  ⑥ 闭包捕获 agent/qid（防 switchQuest 干扰后续异步操作）
  旧缺陷：草稿处理在 L155 深埋，agent 操作（L16 setStopState）先于草稿升级，_activeAgent=null 崩溃。
· fatal 态恢复（2026-07-03 重构）：
  同楼层追加制（不建新目录/不增 floorNum）。原始用户消息保留（粉色气泡仅一个）。
  失败：红框追加一行时间+原因，继续任务链接保留可再点。
  成功（house1 首 token）：继续任务链接消失，按钮启封，时钟启动，标准流式渲染接管。
  AI 看到完整上下文：原始问题 + _system:true 恢复消息（含全部错误历史）。
  红框 DOM（.msg-quest-error）是楼层 AI div 的子元素，追加式渲染（不清空 innerHTML）。
  原子改动列表：
  · card-pool.js startBuildingFloor 复用已有楼层 DOM+补建时钟
  · agent-loop.js send() 恢复模式不覆写 _lastUserInput
  · _buildConversationFlowHtml 聚合 _error 消息为单红框（不逐个渲染）
  · _buildFloorDOM 致命楼（floorFatal=true）跳过 aq1/时钟/A1/A4渲染
  · _renderQuestErrorBox 改为 per-floor 追加式（不清空，链接复用）
  · 新增 _hideRecoveryLink 只隐藏继续任务链接，不删红框历史
  · _finishRecovery 清除 _deferRenderUntilHouse1 防残留
  · sendMessage finally 块新增正常完结楼层的 _saveAgentQuestData 调用（确保 all.json 更新）


AI 面板核心机制
---------------
· 楼层模型: 一次回车=floor，多次 API=houses，一次工具调用=room。计费按楼累计，压缩≥6层保底
· 📎 文件附件截断: 50KB 硬帽，超则保留首尾各 20KB
· 引导注入: abort 当前 house → 确认回合(Flash/noTools) → AI 回复确认 → 继续工作
· 排队信封: 最多 50 条，AI 回复完自动消费下一条，_debounceSaveQueue 500ms 合并写盘
· E-Flow: q1首层楼100%触发，后续10%。四态机(none→pending→standard/custom)
· 项目锁: onlyStore.claimLock()（30s心跳/60s过期）
· 楼层归档:
    all.json → 机器用（conversation[]+计费+快照），禁 reasoning_content
    all.txt → 人类读（完整母本），建楼每5s流式写盘，完结 generateFloorTxt 覆盖
    reasoning.txt → 审计用（all.txt 截断工具结果到300字）
    quest.sq3 → 纯元数据索引（~200B/floor）
· 饼图+时钟: canvas 112px懒重绘，四字段全等跳过。中断/异常: finally 设停止态
· 输出截断（content-gateway.js 单一真理源）:
    OUTPUT_CAP_DEFAULT=200000 / OUTPUT_CAP_MAX=800000 / OUTPUT_CAP_FETCH=8000
    READ_FILE_CAP_KB=195（system-prompt.js+tools-defs.js 动态引用）
    安全网（ghrun 65536）远高于 AI 面

AI 悬浮预览层
--------------
· 表格: clipBox+wrapper 双层，zoom 不触发 reflow，D-pad 平移
· 图片: transform translate+scale 统一缩放+拖拽。关闭: Esc/右键/背景。跨窗口广播关闭


AI 上下文窗口防护（三层纵深）
--------------------------------------------------------------
参数唯一真理源 content-gateway.js:
  CTX_MAX_TOKENS(1,048,565) / COMPRESS_THRESHOLD(200k, 100-900k 可调)
  / MAX_RESPONSE_TOKENS(393216) / CHAR_PER_TOKEN(2.7)
  / COMPACT_MAX_TOKENS(65536) / ARCHIVE_MAX_CHARS(1M)

第一层 — 上下文压缩引擎（主防线，2026-06-27 单专家统一重构，2026-06-29 阈值修正）:
  每 house 前检查 _lastApiPromptTokens（★ 不再用 total）> 阈值→阻塞压缩。每层楼最多压一次。
  单专家统一压缩（tier 6, thinking=enabled, 64K max_tokens）:
    ★ Narrative-First: 先写 narrative（8K-48K chars）→ 再提取 facts → 最后 floors 摘要
    ★ 冷文本全量送入（不采样）: 模型 1M 窗口装得下 ~900K tokens 冷文本
  ★ 2026-06-29: prompt_tokens 取代 total_tokens 判断压缩（Completion 是输出，不算背包重量）
  全成功→splice conversation + _shiftConversationIndices 统一平移索引。败→原子回滚
  Facts 后处理: 同 type + 关键词重叠 >50% → 合并
  KEEP_TARGET = min(beforeTokens, budget) × 0.1，最少保留 6 层楼
  压缩失败→指数退避无限重试（2s→4s→...→60s），永不静默丢弃
  手动触发: 左下角按钮，≥50k 即可，压缩期间发送按钮⏳+disabled

第二层 — 孤儿tool_calls自愈: _saveAgentQuestData 修复后按 floor 标签重建所有楼层
第三层 — Go HTTP/1.1 隔离: dsTransport ForceAttemptHTTP2:false，nginx client_max_body_size 6200m

上下文拆解面板（hover 弹出，2026-06-29 v4）:
  只展示可验证精确数据。单位 tokens（chars/2.5 估算 ≈）。
  Permanent System / User×N / AI text×N / AI tool_calls JSON×N（★新增）
  / Tool Results×N / Compressed Summary / Error messages×N（★新增）
  / Local estimate total / API prompt_tokens(ref) / Available
  ★ Completion 行已移除（输出侧数字不属于背包）
  每间 house 后自动刷新。


all.json 写入系统（2026-06-29 共享 agentPool 重构 + wgeCost 命名归一）
---------------------------------------------
· ★ agentPool 上移至 parent.__qqq_agentPool（父窗口唯一真理源）
  → 一个 quest 一个 agent，跨面板读同一对象，零多副本竞态
· 单写者: 仅主面板(panelId=1)执行 auto-save，左/右面板不写盘
· ★ skipDomFlush（2026-07-05）: auto-save 时传 { skipDomFlush: true } 跳过 DOM 冲刷，防流式渲染中断。仅 onDone/onError 完结路径允许冲刷
· beforeunload: 三面板均执行 _saveAllBeforeUnload(force=true)，同一 parent pool 幂等写入
· 跨面板: 任一面板 _getOrCreateAgent(id) → 同一个 agent 对象
  → 建楼面板写 ag._houses → 自动对另两个面板可见 → 切换 quest 不再死卡片
· all.json houses[].wgeCost: 存 wge 值（1 ge = 10000 wge），与 JS 内部变量统一
· 计费事件处理统一: _processBillingEvent 为唯一真理源（_parseSSE + _callCompactAPI 共用）
  累加模式: _lastBilling 已存在 → wgeCost 相加（压缩多次 API 调用），元数据保留最新

★ 三面板隐藏 draft (2026-06-29 审计确认)
-----------------------------------------------------------------------
· 每面板各自一个 draft: _draft_p0 / _draft_p1 / _draft_p2（_isDraft(id) 判断）
· draft 文本+截图: questUIStates[_draftId] 内存 + onlyStore 持久化，切 quest/重启不丢
· draft agent 也在 parent.__qqq_agentPool 中（per-panel 独立 key），无跨面板冲突
· draft 发送时 _isDraft → questStore.create('') 升级为真实 quest → card 迁移


流式渲染 — 一次渲染 终身不变
-----------------------------------------------------------------------
铁律: 封顶不重建 DOM。流式阶段 appendChild 的节点即为最终节点，永不 innerHTML 替换。
  · onDone 仅做: 去 stream-para 类 + 移除空 _lastParaEl。零 DOM 重建。
  · 引导 Ack XML 清洗: 第一个 < 字符处截断（零漏过可能）。

增量解析器（代码围栏感知）:
  拆分前扫描 ^``` 行首标记奇偶性。奇数→围栏未闭合→禁止拆分，回退 _buf 等待闭合。
  拆分边界: \n\n = Markdown 段落边界，围栏是唯一例外。
  _splitCursor 只扫描上次位置后新增内容 + insertBefore(_lastParaEl) → 永远 1234 正序。
  _doStreamRender + onDone 非围栏路径用 _buf.slice(_splitCursor) 只取尾部未完成段
  （围栏路径 _buf 已在 onToken 重写为纯围栏内容，仍用全量 _buf）。

双层流看门狗（agent-loop _parseSSE）:
  · 流看门狗 60s: 无 SSE 数据→abort。产出看门狗 15min: 无 delta.content 或 tool_calls→abort
  · SSE error 提到 while 外层 throw。usage-only→返回 null→触发正常错误路径

文本工具调用回生:
  EnvelopeStripper.finalize() 统一解析 XML invoke/自闭合 tag/ReAct 三种格式 → textToolCalls[] → _parseSSE 优先执行


AI 网关 — CN 直连 + US 隐身 + 客户端代理 (2026-06-29 加固)
--------------------------------------------------------------------
线路: Client ──(主)──▶ direct-cn.gh555.com ──▶ CN nginx 443 ──▶ WireGuard ──▶ US Go:8080
       └──(备)──▶ cnk.gh555.com ──▶ Cloudflare CDN ──▶ CN nginx ──▶ WireGuard ──▶ US Go:8080

CN 服务器 (47.105.67.51, 阿里云 ECS):
  nginx 443 (通配证书: cnk + direct-cn + *.gh555.com)
  · /api/v3/ai/* → proxy_pass http://10.0.0.1:8080 (WireGuard → US Go)
  · / (其他) → proxy_pass http://127.0.0.1:8080 (CN 本地 Go)
  Go dgs/server 8080 50MB | PostgreSQL 18 | Redis 7
  阿里云自带 5Gbps DDoS 基础防护（免费，已生效）

US 服务器 (23.254.248.119):
  ★ 防火墙白名单 (iptables, 2026-07-02 加固): INPUT DROP，仅放行：
    · CN 公网 SSH (47.105.67.51 → :22)
    · WG 精确四端口 (10.0.0.2 → :22 SSH / :8080 dgs / :5432 PG / :6379 Redis)
    · WG UDP（隧道本身）
    · CF 回源 15 个 CIDR → :80/:443
    全世界其他 IP → DROP。重启自恢复: @reboot sleep 60 → iptables-restore。
  ★ searxng 8088: Docker bridge 隔离，仅监听 127.0.0.1，Go 内部调用，不外露
  ★ sshd 加固: MaxStartups=5:30:20 MaxSessions=8 MaxAuthTries=3 LoginGraceTime=30s
  nginx 443 (CF CDN→US 直达 Web) | dgs/server 8080
  PostgreSQL 18 5432 | Redis 7 6379 | searxng (Docker bridge, 127.0.0.1:8088)

客户端代理 (proxy/sp_tunnel.py, PyQt5 GUI):
  本地 SOCKS5 :10808 → SSH双跳(CN→US) → 智能路由
  · HOST_MAP: cnk.gh555.com → 47.105.67.51:443 (本地绕过 Cloudflare, 30ms)
  · DIRECT_HOSTS: cnk.gh555.com 等自家域名直连
  · 监控面板: 客户端 | CN | US 三栏, CPU/mem 滚动平均 600次=100分钟
  · 系统代理: 写入 Windows ProxyEnable/ProxyServer (127.0.0.1:10808)
  · 全局开关 + 名单模式 + 自动重连(传输层断开→3s重连)

DNS (Cloudflare, 2026-06-29):
  direct-cn.gh555.com  A → 47.105.67.51 (灰色云, DNS only)
  cnk.gh555.com        A → Cloudflare 代理 (橙色云, CDN+DDoS)
  direct.gh555.com     A → 23.254.248.119 (灰色云, US IP)


A4 文件快照 — 每层楼私有文件变更追踪
--------------------------------------
· 唯一入口: panel-a4.js 钩子 Q（拦截 executeTool）+ editor.js 钩子 X
· 钩子 Q: 捕获 before → 执行工具 → 捕获 after → _a4PersistToTimeline(both, trace, diff stats)
  trace 格式: q{id}/f{num}/h{num}/r{num}。★ floorNum 真理源: ag._currentFloorNum
  run_command: 用户可开关（默认关），开启后→captureChanged→噪声过滤→逐文件记录
· 钩子 X: editor blur/Ctrl+S，60s 冷却 → timeline.record
· DOM: aiDiv._a4Block（A4 豆腐块，A2 与 A1 之间），瀑布式文件列表，实时更新
· 点击→openDiffWindow（钩子 Q 已记全 before+after，不补记）
· 持久化: 实时写 timeline (SHA256+gzip+SQLite)；floor 完结仅打包 blob_hash 入 payload
· 容量: 512KB/文件上限，超限只记元数据

Git Goods + qh 滚动 + 面板 resume
--------------------------------------
· Git: goods/git/ (X区 tab + A区 panel)，Timeline 已独立 BrowserWindow
· qh 滚动: 标准文档 do/拓扑/qh滚动真理机器接入标准.md。接入: AI 视口下拉 + AI 面板聊天区
· 面板 resume: qqq/alphal/panel_re{l|c|r}.json，三面板物理隔离，主进程 tmp+rename 原子写


═══════════════════════════════════════════════════════════
已知架构漏洞（2026-07-03 审计追加）
═══════════════════════════════════════════════════════════

D1. search-ui.html iframe inline onclick — 状态栏按钮用 onclick="_expAll()" 在 iframe 内
    可能因作用域问题报 ReferenceError。已修复（2026-07-03）：改用 addEventListener + DOM API
    构建状态栏，每次搜索不 innerHTML 覆盖而是 appendChild 替换。

═══════════════════════════════════════════════════════════
已知架构漏洞（2026-06-29 审计）
═══════════════════════════════════════════════════════════

🔴 致命级
  A1. 卡片断层 — 跨面板不自动刷新 ✅ 已修复(2026-06-29)
      switchQuest 检测共享 agent._stopState === 'sending' → 强制重载 card
  A2. 时钟/饼图跨面板不可见 — timerID 无法跨 iframe 传递
      根治: 时钟数据写入共享 agent._floorTiming，各面板独立 renderLoop
  A3. SSE 断线无跨面板自愈 — SSE 连接耦合在建楼面板 iframe 生命周期
  A4. 单点 auto-save — 仅 panelId=1 写盘。主面板 crash→auto-save 停止

🟡 高危级
  B1. 面板生命周期脆弱 — 三 iframe DOM 事件/timer/observer 无限累积
  B2. 跨面板仅靠 ownership 互斥，无事务 ✅ 已修复(2026-06-29)
      sendMessage 前 agent._stopState 真理校验 + setStopState 同步锁
  B3. 面板级变量泄漏 ✅ 已修复(2026-06-29)
      _sending 改为纯派生 getter（零存储），_capturedAgent 消灭

🟢 改进级
  C1. 卡池快照不可变 ✅ 已修复(2026-06-29)
  C2. window.agentPool 隐式依赖 ✅ 已修复(2026-06-29)
  C3. tmp/test_replace.txt 残留 ✅ 已删除(2026-06-29)


═══════════════════════════════════════════════════════════
根治路线图
═══════════════════════════════════════════════════════════

  已做 ✅ 2026-06-29: C3/C2/B3+A1+C1+B2 + agent 单一真相源重构（setStopState 校验表、_sending 派生 getter、draft 管线前置、fatal 双兜底）
  下个窗口: A2（各面板独立 renderLoop 绘制时钟/饼图）
  架构演进: A4（auto-save 写者选举）+ A3（网络层与执行层解耦）+ B1（iframe 健康检查+自动复活）
