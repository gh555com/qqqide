qqq-shell-v2 铁律 — 不可违反的硬约束。违反即返工。
=============================================

§0 文档纪律
  - 极简。不写背景故事，只写结论。一段不超3行。
  - 只写100%确定事实。未落地不写，落地立刻写。
  - 同一件事只存一处。过时直接删，不留注释/废弃标记。

§1 四层不可逆（2026-07-17 新增 C 启动器层，2026-07-18 Bootstrap Config）
  - C 启动器(launcher/qqqide.exe) = 100% 托管更新。跑在一切之前，所有文件解锁可删可换。
    ★ Bootstrap Config: 唯一硬编码 URL → 下载 launcher-config.json → 一切行为配置驱动。
    服务器改 JSON 即全量推送，用户永不需要重新下载绿色包。
    启动前检查 latest.txt → 下载 r → 删旧 gh555.com/ → 解压覆盖 → 100% 精确一致。
  - 壳(Electron 22, joker.exe) = 窗口+IPC+spawn ghrun。不做业务。
  - ghrun(Rust) = 进程管理+热更新+下载校验。OS单例。
  - 载荷(server-app/) = 全部UI+业务逻辑+Monaco。热更Ctrl+R生效。
  - 归属有疑问→归载荷层。

§2 qz spawn — ghrun 统管所有子进程
  - 全部子进程经 ghrun spawn。管线: ghrun(Rust)→Node(child_process兜底)。
  - 四重防御: ①硬超时2h ②内存守卫2GB(异步execFile,首检5s后续15s) ③JobObject(Win)/setrlimit(Unix) ④失速看门狗(stallMs=15min无输出→杀)。
  - 唯一真理: SYSTEM_MAX_TIMEOUT 只改 qz-spawn.ts 一处。
  - 进程组隔离: Win=CREATE_NEW_PROCESS_GROUP / Unix=process_group(0)。父死子必死。
  - execSync→execFile 修复 UI 冻结。空 cmd 保护。guardMs=timeout+10s。
  - Shell元字符(|&;<>): _normalizeBrief 自动包裹 cmd /c(Win) / sh -c(Unix)，管道直通不再漏(2026-07-18)。

§3 配色 — 唯一真理配色机器
  - 唯一入口: core/qqqide-theme.js。一切组件禁私自定义颜色。
  - 色盘照抄 q2.html (Solarized Light/Dark 16色+语义变量)。
  - editor token 照抄 VS Code solarized 主题。
  - CSS 变量由 qqqide-theme.js 一次性注入 <html>，零回流。forced-color-adjust:none。
  - 主题切换: qqqideTheme.apply(dark)→data-theme属性+Monaco联动+iframe postMessage。

§4 编辑器
  - 用 Monaco 原生 API。不引入 VS Code extension host/vscode.* 命名空间。
  - Worker: 基础走 qqqide-asset:// URL，语言走 blob+loader.js。

§5 命名（永不可逆）
  - 变量名禁含 "su"（如 resume、exposure）。JSON: floor_id 禁 turn_id/session_id/round。
  - ★ 产品级固定英文名（2026-07-19）: 上下文背包 → Context Backpack / 格子 → Grid。
    一切场景（代码/文档/UI标签/对外介绍）以此两英文名为准。i18n 翻译基于此。

§6 C 启动器 100% 托管更新 + Bootstrap Config + 壳层冷更 + 载荷热更（2026-07-18）
  - ★ C 启动器（第一道门）: qqqide.exe 跑在一切之前。
    ★ Bootstrap Config: 唯一硬编码 URL → 下载 launcher-config.json
      → host/path/timeout/redirect/retry 全部配置驱动。
      配置缓存本地，服务器改 JSON 即全量推送。用户永不需要重下绿色包。
    流程: 加载配置 → WinHTTP 下载 latest.txt → 比对 .version
      → 版本不同 → 下载 r（7zCon.sfx+payload.7z）→ 删旧 gh555.com/
      → 解压 r -y → 写 .version → 启动 joker.exe
    → 覆盖整个 gh555.com/（二进制/shell-out/engines/webapp/DLL），100% 精确
    → 失败兜底: 保留旧版继续启动
  - 壳层冷更（第二道门）: bootstrap.js 启动时检查 staging/shell-out-next/
    → 原子替换 shell-out/（shell 增量更新，无需重下载整个 r）
  - 载荷热更: boot 完成 20s 后 backgroundCheckWebappUpdate → server-app.tar.gz
  - 按钮更新: update-ui.js 手动触发
  - 版本跟踪: gh555.com/.version（C 启动器）+ Data/shell-version + Data/webapp-version
  - 服务器产物: Deploy → version.json + r（全量）+ shell-out.tar.gz + server-app.tar.gz + launcher-config.json
  - latest.txt: 仅含版本号，CDN 边缘缓存友好

§7 环境 / §8 便携
  - dev端口8090。启动清除 ELECTRON_RUN_AS_NODE/VSCODE_* 环境变量。
  - 直接调 electron.exe 二进制。BrowserWindow frame:false。
  - 零C盘写入。所有数据写应用目录。通过 process.execPath 判断 dev/packaged。

§9 持久化 — 六入口
  - qgs/qgs.simple→global.sq3(全局) / qgs.project→quest.sq3(对话) / onlyStore→only.sq3(偏好) / qg/qgf→FS原子写(项目元信息)。
  - 禁直接 bridge.state.*/bridge.fs/localStorage。
  - 物理: 全局→Data/alphal/global.sq3 / 项目→qqq/alphal/{quest,only}.sq3。
  - Data/databases/ 是 Chromium IndexedDB，忽略。
  - 退出保护: before-quit/SIGINT/SIGTERM 自动 flushSync。

§10 goods 协议
  - A区宿主: core/gaea-host.js。一切 goods 经 window.qqqideGaea.register()。
  - 一个 goods 可提供: panel(A区)+tabs(X区)+services(后台)+commands。
  - 移除必须清理所有 panel/tabs/services。第一方用直接DOM，第三方用iframe隔离。
  - ★ allowMultiple (2026-07-19): false=禁止多开。跨窗口PID文件+脚本自保双重防多开。
    自启动+false→看门狗5s检测,进程死亡自动重启(简单高可用)。
    开发者: Python脚本导入 goods/_singleton.py 调 check_and_register() 即完成自保。

§11 LSP — 全部禁用
  - Monaco 内置诊断全禁(renderValidationDecorations:'off')，零红色波浪线/hover/补全。
  - 仅保留 tokenization+selectionHighlight+occurrencesHighlight+matchBrackets('near')。括号匹配作为纯视觉辅助保留。
  - lsp-bridge.ts 零引用零加载。manifest.json LSP条目已删除。
  - 替代: 三级语法检查—L1自动 node--check(0ge)+L2 get_diagnostics(~50ge)+L3全仓(~200ge)。

§12 qoast 唯一真理弹窗机 
  - 通 知唯一真理: qoast。全仓代码/文档/注释/变量名/UI标签禁出现 "toast" 那个词，我最恨 toast 那个词：我们项目没有 toast， 只有唯一真理滴 qoast 
  - 唯一入口: core/qqqide-qoast.js。一切通 知走 qqqideQoast.show()。
  - API: show(message,{duration,type,action})。默认9s，0=不关。
  - AI面板在iframe内→走 window.parent.qqqideQoast。

§13 Vision SSE — 禁轮询
  - 唯一通道: Go AIVisionStreamHandler。客户端 SSE: fetch+ReadableStream。
  - 多图片并行: Promise.all 并发视觉模型。context级联断开自动取消上游。

§13b 网关超时 — ContentGateway 唯一真理源
  - 所有超时只定义在 content-gateway.js。agent-gateway/agent-sse 全部引用，禁硬编码。
  - 主线180s/备线98s(CF 100s留2s)/流看门狗180s/产出看门狗15min。

§13c 网关线路切换 — 主备双线+防乒乓
  - 同URL重试3次(MAX_RETRIES=3)→切线路(MAX_LINE_SWITCHES=2防乒乓)。
  - HTTP/2错误优先切线路。502/503前半段重试同URL。
  - 400/422→禁重试禁切线路→直接null让auto-repair修复。AbortError→切线路恢复。
  - onError 延迟到 auto-repair 之后(_lastGatewayMessage机制)。

§13d Nginx 8444 — 禁 HTTP/2
  - port 8444 必须 listen 8444 ssl;(无http2)。sites-enabled/ 只留一个文件。
  - HTTP/2↔Go HTTP/1.1 SSE 协议转换有 bug→ERR_HTTP2_PROTOCOL_ERROR。

§13e 孤儿 tool_calls 自愈
  - _repairOrphanedToolCalls() 双向扫描(最多20轮)，自动清理不配对消息。
  - 每次 _callGateway 前预检。400/422后被动修复+重试。
  - auto-repair 成功后必须重置 _floorOnErrorCalled。

§13f 致命失败恢复 — ACTIVE/CAPPED 二态模型（2026-07-08 全域修正，废弃旧 2026-07-03 同楼层追加制）
  - **铁律1**：红字框仅 ACTIVE（链接"继续任务"可见，可累积错误行）和 CAPPED（链接整行 DOM 移除，一字不留）两种形态。不存在"已恢复"灰字。
  - **铁律2**：$sendBtn.disabled = true 在 fatal/红框相关路径中零出现。按钮永远可点。
  - 任意楼层中断 → 立即封顶密封，永不再建（不论 house 数）。红框位于 .floor-gap 容器（AI div 与下一楼层 user 气泡之间）。
  - 点击"继续任务"：永远创建 `nextFloorNum()` 新楼层，粉色气泡只显示"继续"。错误垒行到同一原始 fatal 楼层红框（`_recoveryOriginFloor`）。
  - 失败：红框追加一行（时间+原因），链接恢复为可点。成功（house1 首 token）：`_capRecoveryLink` 移除链接 DOM，按钮正常 Send。
  - Stop 按钮在 fatal+ACTIVE 态：`_capRedBoxAndSeal` 封顶所有活跃红框 → agent→idle → 用户自由打字发送。

§13a AI 网关 — Worker 纯代理+Go 权威计费
  - 线路: Client→Worker(CF edge,纯透传)→Go(唯一扣款权威)→模型供应商。
  - Worker 全路由(6端点)纯透传到 Go:8444。禁计费/模型映射/body改写。
  - Go: 直接 ChargeAITurnWithMeta 单步扣款。禁 lodge/flush 双轨(已删除)。
  - 经济账本即唯一限流(ge≤0→402)。Worker CPU时间趋零，永不超免费额度。

§14 主文件夹+文件锁
  - AI视口第1个豆腐块=主文件夹，显示★标记，不可删除。
  - AI首轮注入 vision context(⭐标记主项目)+末尾 REMINDER。
  - 主文件夹写 qqq/.lock(30s心跳,60s过期)→防同一项目多窗口。

§14b 统一 AI 模型包装器 — AiGateway
  - 唯一入口: ai-gateway.js(window.AiGateway)。一切AI调用必须经此，禁直接fetch第三方。
  - 客户端零暴露模型名/厂商名/API端点。tier 1-6数字传参，映射由AiGateway+Go双重保障。
  - 全服务双线路: chat/vision/generateImage/embed/searchWeb 均 primary+fallback。

§15 i18n — 唯一真理国际化机器
  - 唯一真理源: zh.json。开发者只写中文。其他12语言 ky.py 自动差异翻译。
  - HTML: data-i18n/data-i18n-placeholder。JS: _i(key,fallback)。
  - en.json 编译进 _EN_BUILTIN 兜底。语言持久化: qgs('qqqide.i18n')。
  - 菜单行1中字按钮切换。AI iframe 自动 postMessage 同步。

§16 AI 流式渲染 — 唯一真理渲染机+Flag白名单
  - live onDone 与重启恢复共用 _buildConversationFlowHtml(唯一函数)。
  - 数据清洗: EnvelopeStripper.finalize()一次性完成(信封/floor_summary/文本工具调用回生)。
  - 渲染规则用 flag 白名单(§16a)，禁字符串匹配。
  - ★ c 重构(2026-07-08): content(API完整回复)为唯一真理。onDone→innerHTML直接替换。
    流式(onToken→_doStreamRender)仅为实时预览，封顶时 content 覆盖，零调和补丁。
    流状态(_streamBuf/_streamParas等)归 agent 持有，跨面板共享。
  - 流式渲染代理到 agent._doStreamRender()。

§16a Flag 白名单 — conversation→显示的唯一判定
  - _guideAck+assistant→渲染 / _injected+user→渲染 / assistant+content+!tool_calls→渲染。
  - 其余一律不渲染。新增消息类型默认不渲染(safe by default)。

§17 版本锁定 — 核心依赖永不升级
  - Electron 22.3.27(最后Win7)/sql.js 1.11.0/Monaco 0.34.1(兼容Chromium108)/esbuild 0.19.12(33ms物理极限)/TypeScript 5.0.4。
  - 任何升级需同时证明: 解决实际功能缺口+在Electron22上测试通过。

§18 CooldownGuard — 唯一真理防抖机
  - 唯一入口: core/cooldown-guard.js。全自动: 所有button/[onclick]自动享有冷却。
  - 默认800ms，关闭/取消按钮400ms。data-no-cd跳过，data-cd="N"自定义。
  - 视觉: 白色高光蒙版+按钮左下4×4红点。键盘防穿透(capture phase)。
  - ★ 互补: _execSendBusy JS 层同步锁（panel-pipeline.js 模块级）。CooldownGuard 防 UI 连点，
    _execSendBusy 防 Enter 键连点触发的 async 竞态窗口（draft 晋升 await → 两 quest）。
    三层防线: panel-input.js → panel-send.js sendMessage() → panel-pipeline.js _executeSend()。
    锁在所有 await 之前同步设置，JS 单线程零竞态。所有路径 + finally 释放。

§19 光标铁律 — 禁改光标外貌
  - CSS/JS任何时候任何元素不得修改 cursor。无例外。
  - 按钮/链接依赖浏览器原生:hover。Sash也不设特殊光标。

§20 源文件唯一 — 禁编译产物混入源码
  - TS源码 shell/。编译产物 shell-out/。两者永不相交。
  - AI只改 shell/*.ts，改完跑 npm run build。绝不直接改 shell-out/*.js。

§21 双模更新 — 载荷热更+壳层冷更
  - 载荷: update-service.ts 自动拉取 server-app.tar.gz→原子替换→重载。零感知。
  - 壳层: bootstrap.js 启动检查 staging/shell-out-next/→原子替换→加载main.js。两次重启。
  - Electron 二进制永不更新(§17锁定)。

§22 输出截断
  - 64KB IPC截断是§2四重防御第四环。
  - AI面统一内容门: ContentGateway.gate()（≤50K全文/首20K+尾20K），一切工具+附件归一。
  - 改截断大小只改 CTX_CAP_CHARS 一处。

§26 三面板架构（左中右 iframe，同窗口）
  - 左翼(panel=0)+中(panel=1)+右翼(panel=2)，各自独立 iframe。
  - 左/右翼灯泡开关控制，默认关闭。开关时窗口自动伸缩。
  - _windowId 必须含 _panelId(sessionStorage key='qqq-ai-window-id-pN')。
  - 项目锁仅中面板申请，左右翼共享。

§27 时钟颜色状态机
  - 三合法状态: 工作中(绿#859900)/活跃段切换(绿/黄/橙)/停止(亮#000暗#fff)。
  - startFloorTimer 必须立即设绿色。stopFloorTimer/finally 必须设停止态。
  - timer 跑在 agent 上(ag._floorTimerId)，切 quest 不停 timer。
  - timer 回调三层自停: ①楼层完结 ②Card被驱逐 ③switchQuest切回时显式 clearInterval。
  - _activeAiDiv 是 agent↔Card DOM 永久绑定。切 quest 无条件重绑。
  - ★ 切换保护态: _switching=true→全屏蒙板接管鼠标+消息区dim+所有入口硬拦截。

§28 跨面板同步 — IPC sync 桥
  - _broadcast→parent.qqqideBridge.sync.broadcast→主进程 relay 到所有窗口(含发送者)。
  - _handleSyncMessage 首行过滤 msg.windowId===_windowId。禁 BroadcastChannel。

§28b quest 索引中心大脑
  - parent.__qqq_questIndex=[] — 三面板共享唯一引用，零拷贝。
  - 主面板扫盘→写共享数组→广播。侧面板直接读同一引用。
  - 30s宽限期: 不删除 createdAt<30s 且磁盘无目录的 quest。
  - 仅主面板扫盘。侧面板 never fs.list。

§29 Card Queue — AI 面板终极架构
  - 1 quest=1 Card(完整DOM)，创建后永不 innerHTML。切 quest=CSS display显隐。
  - 每Card: 16已封顶+1在建。超限→删最老DOM。Pool上限10,LRU驱逐。
  - _buildConversationFlowHtml 为唯一渲染函数(live+恢复共用)。
  - per-floor 状态归属 agent(禁全局): _floorTimerId/_floorStartPerf/_activeAiDiv/_allTxtPath/_a4Snapshots。
  - 面板级单例(禁经_activeAgent重定向): _allTxtPollTimer/_allTxtBlocked/streaming。
  - A4铁律: insertBefore(a1Block)无条件。A1铁律: insertBefore(clockBlock)无条件。
  - ★ all.json 唯一增量路径=auto-save(面板级持久timer,模块加载即启动,5s间隔)。
  - 三层防线: ①_restoreAgentFromStore回填 ②空houses+非新楼层→拒绝 ③stopState!=='sending'→拒绝。

§30 面板焦点
  - ★ 三面板共享 parent.__qqq_agentPool（父窗口唯一真理源），各自独立 cardPool。获焦点时 postMessage 通知父窗口。
  - 快捷键(焦点+非编辑区): 1/2上下滚17.5%, q/w前后楼层跳转。
  - q2单行制: 招牌行 hover展开下拉(2行弹性/默认20条/模糊子序列匹配)，统合搜索+新建。
  - 一个 quest 同时只能在一个面板打开(所有权§32+跨面板同步§28)。

§31 工作空间 — 多项目隔离
  - 一个窗口一个工作空间=一个主文件夹全部运行时状态。
  - IPC频道='qqq-sync:'+sanitize(_workspaceRoot)，不同主文件夹物理隔离。
  - bindMainProject: 同root跳过/不同root→_teardownWorkspace→_initWorkspace。并发锁 _bindLock。
  - Card Pool destroy()必须: 中止所有agent+移除所有card DOM+清空_cards/_lru/_activeId。

§32 所有权系统 — 父窗口注册表唯一真理源
  - parent.__qqq_questOwners(questId→panelId同步映射,零TTL,零IO)。
  - 面板 resume: bridge.fs.write 原子JSON→qqq/alphal/panel_re{l|c|r}.json(三面板独立)。
  - 流程: switchQuest→_parentGetQuestOwner→_parentReleaseQuest(旧)→_parentClaimQuest(新)。

§33 原子写入 — 绝不先删后改
  - state-sqlite/qg: rename失败→copy+unlink降级。绝不先 unlink(dest)。
  - 启动时 _cleanStaleTmp: tmp比主DB新→rename恢复;否则删除。
  - 腐蚀隔离: SQL"disk image is malformed"→rename为.corrupt.{ts}+空库重试,不丢数据不阻断。

§34 AI 面板快照 — 重启百分百还原
  - activeQuestId 存 panel_re{l|c|r}.json(bridge.fs.write原子写,断电安全)。
  - UI快照存 only.sq3(键 ai.uiStates), per-quest粒度: inputValue/caret/pendingImages/tier/scrollTop。
  - 编辑框用 selectionStart/End 保存恢复(零DOM walk)。
  - questUIStates 必须在 initQuests 之前从 onlyStore 加载。

§35 AI 面板模块化 — panel-*.js 拆分铁律
  - index.html 仅HTML+CSS壳(~400行)+script src引用，禁内联JS。
  - 每 panel-*.js ≤800行。超限即再拆。跨文件前向声明放 panel-state.js 顶部(var安全重复)。
  - 加载顺序固定: state→search→input→render→quest→floor→quest-ui→clock→alltxt→a4→send。
  - AI基础: content-gateway→system-prompt→embedding→ai-gateway→tools-defs。
  - 新增文件必须同步更新 index.html script顺序+service-worker缓存版本。

§35b SW 缓存失效 — 改静态文件必须升 CACHE_NAME (2026-07-10)
  - ★ 改 i18n/HTML/CSS/JS 等静态文件后，必须同步升 service-worker.js 的 CACHE_NAME（如 v12→v13）。
    否则重启不生效——SW 是 Chromium 持久化缓存，重启 Electron 也不清。
  - 调试确认: 看 DevTools Network 面板是否 from ServiceWorker，是则版本号没升。
  - fetch handler 必须跳过 pathname 含 /api/ 的请求（直接 return）。禁拦截跨域 API。
  - 违规即返工。

§36 Per-Agent 状态 — 禁全局 timer/A1 变量
  - _floorTimerId/_floorStartPerf/_floorCurrentTiming/_activeAiDiv 已移入 agent 对象。
  - startFloorTimer(aiDiv,ag)/stopFloorTimer(timing,ag) 必须传 agent 参数。
  - timer 回调用闭包捕获的 _ag，禁读全局 agent getter。
  - 切 quest 不停 timer。禁在 switchQuest/_unloadQuest 中调 stopFloorTimer。

§37 A4 文件快照+Timeline 版本时间线
  - 钩子Q: edit/write/create/delete→捕获before→执行→捕获after→_a4PersistToTimeline。
    run_command→默认关闭(设置→高级)，开启后 captureChanged→噪声过滤→逐文件记录。
    trace: agent-loop 埋点 window._qqqCurrentTrace→"q{id}/f{num}/h{num}/r{num}"。
    ★ trace捕获在 await 之前(防并行工具覆盖)。
  - 钩子X: editor blur/Ctrl+S→读上一版本 diff→timeline.record(source='editx')。
  - ★ 编辑类快照共享主进程真理机: 100s冷却+SHA256去重(改 ipc-timeline.ts 一处)。钩子Q不走冷却。
  - SHA256 行尾归一化(CRLF/LF/CR→\n)。存储: blobs/{sha256}.gz+WAL(≤99行)+SQLite+.bak。
  - 楼层完结仅打包 blob_hash 入 payload。旧 gzip 编解码等全部删除。

§38 Diff 窗口 — 独立 BrowserWindow
  - Monaco side-by-side+minimap+手动差异折叠。一个文件单例(Map)，再次点击复用。
  - 下拉标签: "#N 时间 +N -M xx [first] [before]"。beforeIdx≤afterIdx强保证。
  - 底部三按钮: ☐仅差异(only.sq3持久化)|编辑/取消编辑|✕关闭。
  - 编辑模式: 右侧可编辑(左侧只读)，差异块→箭头复制左侧到右侧。
  - 编辑保存(source='diff-edit')与X区(source='editx')共享100s冷却+去重。
  - 编辑器零LSP/零波浪线/零智能，只留 tokenization。Monaco加载15s超时保护。

§39-41 Git/目录重命名/孤儿自愈
  - §39: X区 Git tab+A区状态面板。Timeline 已迁出至独立窗口。
  - §40: rename 只改 DB(questStore.rename)，永不实时操作磁盘。启动/关闭时 lazyRenameScan 修正。
    仅中面板执行一次。跳过建楼中+30s内新创建的 quest。中心大脑锁防三面板重复。
  - §41: _repairOrphanedToolCalls 双向扫描。Go侧双防御: sanitizeMessages+defer recover。
    一名用户异常最多影响自己，绝不波及其他用户。

§42 E-Flow — 专家文档框架
  - 入口: panel-expert.js(四态机: none→pending→standard/custom)。
  - ★ 自动引导(2026-07-18): 新项目(mode='none')首次启动→自动创建骨架文件
    (index.md + arch/{topology,iron_law,env_var,facts}.md) + project.txt rule注册。
    模式自动设为 standard。AI 后续自然填充骨架。
  - q1首层100%触发(仅mode='none'且未引导时)，其余10%。防抖5min。仅中面板触发。
  - 触发信号: inject('[E-FLOW trigger]', {_system:true})。
  - 协议全文存服务器壳(system-prompt.txt)，客户端只发一行触发词。
  - 持久化: onlyStore.qqq.expert.mode。已锁定模式永不回退。
  - AUTO-DETECT: 启动时扫描 expert/index.md→standard / project.txt rule条目→custom。
    自动引导优先(先建standard骨架，再检测custom)，custom不覆盖已有standard。

§43-44 窗口缩放/项目资产
  - §43: 窗口UI锁死1.00 zoom。+/-/Ctrl+=只改editor fontSize(1-128)。按住=首tick ±1后跃10（18→28→38...），200ms定频防抖。
    持久化: qgs.simple editorFontSize.{root}。窗口创建无条件 setZoomFactor(1.0)。
  - §44: 项目资产（出战阵营/编辑器tabs/光标/AX布局）唯一真理源 only.sq3。
    key: ai.formation(辅文件夹路径数组) / editor.tabs+editor.positions / layout(aZoneW/outputH/outputVisible)。
    随项目目录迁移，清缓存/改路径不丢。旧 win_snap 机制已删除。

§45 唯一源码目录 — 防脑分裂
  - webapp 唯一真理源: server-app/。gh555.com/=npm run pack输出,已gitignore,禁止编辑。
  - shell/ TS源码→esbuild→shell-out/。dev读server-app/直出。Ctrl+R即时刷新。

§46b 中央建楼状态机 — 跨面板唯一真理源
  - parent.__qqq_buildingRegistry(questId→{stopState,panelId})。
  - ★ agent._stopState 单一真理源：idle→sending→stopping/fatal。setStopState() 带校验表（idle→[sending,fatal], sending→[stopping,fatal,idle], stopping→[idle,fatal], fatal→[sending]）。
  - ★ _sending 纯派生（defineProperty 仅 getter）：!!(_activeAgent && _activeAgent._stopState === 'sending' && !_activeAgent._floorCompletedCleanly)。零存储零 setter。
    _floorCompletedCleanly 守卫防后台已完结 agent 假报 sending。
  - ★ 每quest独立沙箱: _queue/_queuePaused 全部 per-agent。
  - 彗星电子钟: 广播同步本地状态 + 注册表兜底。计时读 agent pool。豆腐块仅显 questActiveId，下拉各 quest 独立。
  - 生命周期: registerBuilding→unregisterBuilding。finally 块兜底防僵尸。

§46c 预渲染 all.json — 一次渲染永久不变
  - 新增预计算字段: ai_html(优先_contentWrap.innerHTML)/question_clean/house_count/room_count。
  - 恢复时优先读 ai_html→innerHTML，零重新渲染。旧数据无字段时回退现场渲染。

§46d sendMessage 管线 — 草稿最先原则 + _execSendBusy 同步锁（2026-07-18 加固）
  - 执行顺序必须: gate checks → 草稿升级 → null guard → invariant → setStopState。
  - 草稿处理（_isDraft true）必须在一切 agent 操作之前（_activeAgent 为 null 直到草稿升级完成）。
  - 草稿升级: questStore.create → _getOrCreateAgent → 改名落盘 → renderTabs → card 迁移。
  - 此后 _activeAgent 保证非 null，禁在之后放置 _isDraft 分支。旧 L155 块已删除。
  - ★ _execSendBusy 同步锁（2026-07-18）：模块级 var，所有 await 之前同步设置。
    防 draft 晋升 await 期间二次 Enter → questStore.create 竞态 → 重复 quest 目录。
    三层防线: panel-input.js(Enter/Send按钮) → panel-send.js(sendMessage) → panel-pipeline.js(_executeSend闸门后)。
    排队排水前释放锁（line 797），防 _triggerQueueSend 死锁。所有错误路径 + finally + headless 路径均释放。

§46 开发基础设施 / §47 INFRA OPSEC / §48 AI 图片生成
  - §46: qide-shell.py 一站式(Q=build+dev-server+watch+electron)。
    改 server-app/→Ctrl+R。改 shell/*.ts→watch自动重编。npm run build=一次性esbuild。
  - §47: AI 禁输出内部域名/URL/IP/端口/API路径/服务器名。工具失败仅回复"该功能暂时不可用"。
  - §48: 模型 wan2.7-image-pro。尺寸 1K/2K(默认)/4K/自定义W*H。
    落盘 qqq/genera/。计费 ¥0.75/张。CSP: img-src 含 file: 白名单。

§49 服务器安全 — CN 前台 + US 隐身 (2026-06-29)
  - CN(47.105.67.51): 公网入口，nginx 443 通配证书。阿里云自带 5Gbps DDoS 基础防护。
  - US(23.254.248.119): iptables 白名单，仅接受 CN(47.105.67.51)+WireGuard(10.0.0.0/24)+wg0接口。
    全世界其他 IP → DROP。重启自恢复 @reboot cron。
  - AI 路由: CN nginx /api/v3/ai/* → WireGuard(10.0.0.1:8080) → US Go。非 AI 请求 CN 本地 Go 直接处理。
  - 客户端代理 HOST_MAP: cnk.gh555.com → 47.105.67.51:443，本地绕过 Cloudflare。

§50 登录推送 — 禁轮询为主线 (2026-06-29)
  - 主通道: qqqide:// 自定义协议。浏览器 → qqqide://auth → Electron 主进程 → IPC 推送。
  - 轮询仅兜底（协议未注册时），120s 超时，3s 间隔。
  - ide-finalize 必须返回 token（浏览器拿 token 推，服务端 poll 接口为 fallback 保留）。
  - 持久化: safeStorage OS 级加密 → Data/alphal/auth.enc。启动自动恢复，主动登出才删。
  - 禁新增轮询。新通知机制一律走 push→fallback 双轨。
  - DNS: direct-cn.gh555.com 灰色云直连 CN。cnk.gh555.com 橙色云经 Cloudflare。

§51 零配置 (2026-07-01)
  - 无 config.json。PRODUCTION_URL 硬编码 shell/boot.ts，仅 env/CLI 覆盖。
  - 禁新增用户可见配置文件。旧兼容代码删除干净，不给将来埋坑。

§52 AI 模型名保密 — 客户端与网络包中零暴露 (2026-07-04)
  - 客户端：tools-defs.js 工具描述禁含模型名/厂商名/OSS 地域。ai-gateway.js _tierToModel()
    仅输出「fast」「deep」opaque 标签，tier 数字传参。
  - 服务端：SSE billing 事件 model 字段仅回传 opaque 标签（req.Model），绝不回传真实模型名。
    嵌入/视觉/绘图/搜索/抠图 handler 的 JSON/SSE 响应中禁含 DashScope/DeepSeek/阿里云 模型名字符串。
  - 日志：服务器日志可含真实模型名（仅运维可见），但绝不进入任何发往客户端的响应体。
  - 违者即返工。

§53 AI 计费唯一真理机 + per-house 随机因子 (2026-07-04)
  - 服务端唯一真理源：gaea/internal/billing/billing.go。一切定价常量收敛于此，6 个 handler
    全部引用 billing 包，禁本地硬编码。
  - Chat 计费随机化：缓存命中 × rand[1.0, 3.0]，缓存未命中 × rand[0.7, 1.0]。
    每间 house 独立随机。tier 倍率保留（1.0~2.0），叠乘于随机因子之上。
  - 最终 geCost = 基础USD × random × tier × 汇率 × 10000。tier 1-3→Flash, tier 4-6→Pro。
  - 汇率：fx.go 每 6 小时自动更新。上游定价：无 API 可查，人工定期核对官网手动更新 billing.go。
  - 违者即返工。

§54 零摩擦原则 — IDE 不教 AI 做事 (2026-07-05)
  - IDE 唯一职责: 消除工具摩擦。AI 想做什么→工具干净执行，一次成功。
  - 不做: 「答案已找到」提示、语义循环检测、行为微调提示词——那是模型的事，模型越聪明自然解决。
  - 反面教材: SSH 三层引号 → 一条 SQL 试 28 次。不是 AI 蠢，是工具添堵。
  - 判断标准: 新功能是在消除摩擦还是试图教 AI 怎么做？后者不做。
  - 违者即返工。

§55 组件系统 — 服务器驱动、客户端全自动 (2026-07-04)
  - 唯一真理源: engines/manifest.json。组件定义(install_to/bin/verify_args/srcs)全在此文件。
  - 分两级: rank0(IDE 自动下载: ripgrep+python) / rank1(用户手动: ffmpeg+yt-dlp+git)。
  - 运行时: shell/component-checker.ts。启动→读 manifest→逐 rank0 检查→缺了顺序尝试 srcs。
  - 验证: spawnSync 直接调嵌入式二进制 + verify_args，禁 shell:true（防注入+误判）。
  - 原子写: _writeJson 走 tmp+rename，断电不丢。manifest 内存缓存，启动后零 IO。
  - 下载冷却: .downloads.json 记录成败。失败后 5min 冷却。成功则永不再下（除非版本升级）。
  - 目录迁移: 服务器改 install_to→客户端比较 .versions.json 旧值→fs.rename 原子重组。
    新目录已存在→旧内容合并移入，不覆盖不丢数据。
  - Python 铁律: 只用内嵌。getComponentBin('python') 唯一入口。
    无内嵌→py-broker 跳过(IDE 正常启动，下次启动自动装上)。永不 fallback 系统 Python。
  - 违者即返工。

§56 上下文压缩 — 每层封顶自动重组（V13, 2026-07-16 DE 消除，╔K...╚ 融入饼干，阀值压缩，sha256 考古）
  - 触发: 每层楼完结(onDone)→_rebuildBackpack()。建楼中不触发。阀值压缩在建楼中自动触发。
  - 机械筛: 纯本地确定性转换(方案三)。消息→Q/A/工具摘要行。零网络，零费用。
  - 包装盒: 15温柔(可复现工具,摘要一行)+5绝对(run_command/generate_image/remove_background/analyze_image/get_vision_context,╔K...╚包裹,16K硬帽)。
  - sha256 考古: 读工具(args有sha256)→@sha=xxx；写/落地工具(result有[sha256:])→➜ sha=xxx。12位hex，零额外IO。
  - 饼干: 单条_biscuit系统消息原地追加，对象永不变。仅content尾部增长→前缀缓存命中。每层格式: === FN YYYY-MM-DD HH:MM:SS UTC+8 === → Q → 包装盒 → A。
  - ★ DE 概念消除(V13): 不再有_deBlock消息/deEntries字段。绝对包装盒融入饼干。旧DE消息自动清除。
  - 阀值压缩: _tryAutoValveCompress(threshold)→剥离 ╔K...╚ 体部。手动(🗜️按钮,threshold=0强制)/自动(建楼中_lastApiPromptTokens>设定阈值)。阈值设定: 齿轮→设置→自动压缩阈值(100-1000k,默认600k)。
  - 背包定序: Z→biscuit(1条msg)→当前楼层消息。禁splice biscuit(仅删原始楼层)。
  - 持久化: ctx(biscuitLines/narrative等)存 per-quest ctx.json(原子tmp+rename)，不再写 quest.sq3。ctx.json 损坏→D 路径(_rebuildBackpack 扫描 conversation)自愈。
  - 错误: 未恢复fatal楼层不进饼干(原始消息保留)。已恢复→[ERR] [HH:MM]格式进饼干。_errorTime持久化→重启精确恢复。
  - 违者即返工。

§57 📎 附件分隔符 — 纵深防御，永无嵌套歧义（2026-07-14，2026-07-19 加固）
  - 分隔符: ASCII " (U+0022) 包裹路径。Windows NTFS 文件名禁 " → 分隔符与路径内容永不可能碰撞。
  - 正则: getInputChipPaths() 用 /[📎📁]"([^"]+)"/g 提取。📎📁 在路径中合法但被 [^"]+ 正确捕获，不会误判为前缀。
  - L1 源头守卫: insertChipAtCursor 拒绝路径中含 📎📁"" 的注入，qoast 告警。
  - ★ 准许多次注入同一文件（2026-07-19）: 删除了旧去重逻辑。同文件可出现在编辑框任意位置任意次数。
    万一误双击多了一个 chip，退格删掉即可，无需代码层防御。
  - ★ 管道去重（2026-07-19）: 编辑框准许多次注入，但 _executeSend 构建上下文时去重 allPaths，
    同一文件内容只喂一次给 AI，不浪费上下文空间。
  - 子弹按钮: 粘贴板→qqq/bullet/→insertChipAtCursor。单文件 3MB 硬帽，目录 20MB FIFO 轮转。
  - 饼干剥离: [File: ...]```...``` 正则全局匹配删除→子弹原文不进下一层楼。
  - 目录命名: qqq/quests/ 下目录名禁含 📎📁 字符（防 quest 路径被当做 chip 时歧义）。
  - 违者即返工。

§58 模块健康检查 — 启动时全量校验，静默失败→可见告警（2026-07-14）
  - 入口: panel-health.js（index.html 最后加载）。
  - 检查: 所有关键模块的预期导出（AgentLoop.prototype 扩展、全局单例）是否存在。
  - 失败: 红色横幅置顶告警，列出失败模块名。用户 Ctrl+R 热重载修复。
  - agent-loop.js 内置兜底 stub（_rebuildBackpack/_compressContext 等），防 typeof === 'function' 静默降级。
  - 违者即返工。

§59 自动语法门 — edit/create/write_file 后端内置语法检查（2026-07-14，2026-07-15 加固）
  - 位置: shell/ipc-edit.ts checkSyntaxSync()。JS→vm.Script（零spawn，100%可靠），JSON→JSON.parse。
  - L2 上下文验证: 空白归一化匹配后，短串（<30 chars）需位于行首或首字符非结构符（} ) ; ]），
    防 nc.indexOf(nf) 取首匹配错位。主进程+渲染层双份一致。
  - 失败: 还原文件（有原内容→还原，新文件→删除），返回语法错误。
  - 违者即返工。

§60 Goods 清单驱动路由 — qqqGaea.open() 统一入口（2026-07-14）
  - 入口: gaea-host.js open(id, opts)。
  - 路由: 查 manifest→有 panel→show(A区) / 有 tabs→ensure+activate(X区) / 动态 tab(provides:'tab:xxx')→消费者自行 addGaeaTab。
  - 消费者: 统一调 qqqGaea.open(id)，不再硬编码 show()/addGaeaTab()。goods 改注册形态→消费者自动跟随。
  - 违者即返工。

§61 iframe 主题切换 — CSS 选择器优先原则（2026-07-15）
  - 管线: 父窗口 postMessage(theme-change) → iframe JS 监听→设/删 data-theme 属性→CSS [data-theme="dark"] 生效。
  - ★ 核心教训: CSS 选择器语法错（如 `-[data-theme="dark"]` 多了个 `-`）→整个暗色块被浏览器静默丢弃。
    此前多次 AI 修主题失败，皆因去查 JS 消息管线/初始化时序，未先验证 CSS 规则是否有效。
  - 方法论: iframe 主题问题→第一步 F12 看暗色 CSS 块是否被浏览器解析（不是看 JS 消息是否到达）。
    选择器只要有一个字符错，浏览器零报错直接跳过整个 { } 块。
  - 违者即返工。
